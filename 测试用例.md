# SupKube 测试用例

> **版本**：v0.8.8-alpha（覆盖 v0.5 → v0.8.8 全部已交付功能）
> **目标读者**：QA 测试工程师 / 自动化测试工程师 / SRE
> **更新频率**：每次发版前必须同步新增测试用例

---

## 🎯 工程座右铭：回归测试是必做项目

> 这一节抄自《架构设计.md §11.1》，作为本文档的**底层公理**。
> 任何写测试用例 / 跑测试 / 审 PR 的人请先读这一段再做事。

**触发事故**（永远不忘）：v0.8.7.6 — "删除 RP 但没真删"
- 用户在 UI 删除 6 个 RP，以为干净了
- kubectl 一查：9 个 orphan VSC + Azure Blob 30+ MB 数据仍在持续计费
- 全程 UI 零提示
- 如果是生产客户，月底账单出现"删不掉的费用" → 无法解释

**为什么测试没接住**：v0.8.7 引入 Data Mover 时只验"功能跑通" + "Backup CR 消失"，**没有任何测试覆盖底层 K8s + BSL 资源是否真清干净**。回归测试矩阵没跟着新功能扩展。

**铁律**：

> **设计了流程 = 写了测试用例 = 把测试排进 backlog**
> 三件事缺一不可。任何 PR 没把对应 TC 更新或新增写进描述里 → 不准 merge。

**落地规则**：

1. 新功能（新页面 / 新 API / 新表单行为）→ 同 PR 内更新本文件，至少加 1 个 P0/P1 用例
2. 修历史 bug → `TC-REG-XXX` 必须新增对应回归用例 + 注明 sprint 版本
3. 客户提的 bug → ADR 修订段 + 本文件回归用例**双归档**
4. 测试用例按"重要紧急双轴"排序（见下）
5. 发版前 / 客户 UAT 前，**P0 全集 + 上版本新增 P1 全集 必跑**

---

## 📊 优先级矩阵（重要 × 紧急）

```
              紧急                  不紧急
            ┌──────────────────┬──────────────────┐
   重要     │ Q1: 立刻做        │ Q2: 排进 sprint  │
            │ ━━━━━━━━━━       │ ━━━━━━━━━━       │
            │ [P0] 阻断性       │ [P1] 关键        │
            │ 每次发版必跑      │ 必须通过         │
            ├──────────────────┼──────────────────┤
   不重要   │ Q3: 找人做        │ Q4: 删掉它       │
            │ ━━━━━━━━━━       │ ━━━━━━━━━━       │
            │ [P2] 应当通过     │ [P3] nice-to-have│
            │ 已知风险可延期    │ 抽样 / 砍掉       │
            └──────────────────┴──────────────────┘
```

| 等级 | 含义 | 跑的时机 |
|---|---|---|
| **P0** | 阻断性 — 失败则不能发布 | 每次发版 + 客户 UAT |
| **P1** | 关键功能 — 必须通过 | 每次发版（上版本新增的必跑）|
| **P2** | 应当通过 — 可商量延期 | 抽样 / 季度全量 |
| **P3** | nice-to-have | 团队闲时跑，可随时砍 |

**执行策略**：

- 项目时间充裕（Mars 不赶 backlog）→ Mars 自己按矩阵跑 Q1+Q2
- 赶 backlog → 排专人跑 Q1+Q2、Q3 抽样
- 客户 UAT → 客户跑 Q1+Q2 脚本，P0 失败立即回退

---

## 文档约定

### 用例编号

```
TC-<域>-<3 位数字>
```

| 前缀 | 域 |
|---|---|
| `TC-ENV` | 测试环境 / 部署 |
| `TC-AUTH` | 认证 / 授权 |
| `TC-DASH` | Dashboard |
| `TC-APP` | Applications |
| `TC-RP` | Restore Points (Backups) |
| `TC-ACT` | Activity / Actions |
| `TC-RST` | Restore 流程 |
| `TC-TS` | Transform Sets |
| `TC-POL` | Policies (Schedules) |
| `TC-IMP` | Import Policy (跨集群持续 DR) |
| `TC-STG` | Storage / Snapshot Locations |
| `TC-ADV` | Backup Advisor |
| `TC-UX` | UI / i18n / 暗色模式 |
| `TC-REG` | 回归测试（历史 bug）|
| `TC-PERF` | 性能 / 负载 |
| `TC-SEC` | 安全 |

### 优先级

| 等级 | 含义 |
|---|---|
| **P0** | 阻断性 — 失败则不能发布 |
| **P1** | 关键功能 — 必须通过 |
| **P2** | 应当通过 — 已知风险可商量延期 |
| **P3** | nice-to-have |

### 用例模板

```
### TC-XXX-NNN [Px] 用例标题
**类型**：Functional / Integration / Regression / Performance / Security / Negative
**Sprint**：v0.x.y（功能首次引入版本）
**前置条件**：...
**测试步骤**：
  1. ...
  2. ...
**预期结果**：
  - ...
**实际结果**：（测试时填写）
**通过/失败**：（测试时填写）
**备注**：可选
```

---

## 目录

0. [测试环境准备](#0-测试环境准备)
1. [P0 冒烟测试套件](#1-p0-冒烟测试套件)
2. [认证 / 授权 (TC-AUTH)](#2-认证--授权-tc-auth)
3. [Dashboard (TC-DASH)](#3-dashboard-tc-dash)
4. [Applications (TC-APP)](#4-applications-tc-app)
5. [Restore Points (TC-RP)](#5-restore-points-tc-rp)
6. [Activity (TC-ACT)](#6-activity-tc-act)
7. [Restore 流程 (TC-RST)](#7-restore-流程-tc-rst)
8. [Transform Sets (TC-TS)](#8-transform-sets-tc-ts)
9. [Policies (TC-POL)](#9-policies-tc-pol)
   - 9.5 [Import Policy (TC-IMP)](#95-import-policy-tc-imp)
10. [Storage / VSL (TC-STG)](#10-storage--vsl-tc-stg)
11. [Backup Advisor (TC-ADV)](#11-backup-advisor-tc-adv)
12. [UI / i18n / 暗色 (TC-UX)](#12-ui--i18n--暗色-tc-ux)
13. [回归测试 (TC-REG)](#13-回归测试-tc-reg)
14. [性能 / 负载 (TC-PERF)](#14-性能--负载-tc-perf)
15. [安全 (TC-SEC)](#15-安全-tc-sec)
16. [部署 / 升级 (TC-ENV)](#16-部署--升级-tc-env)
17. [测试报告模板](#17-测试报告模板)

---

## 0. 测试环境准备

### 0.1 必需依赖

| 组件 | 最低版本 | 验证命令 |
|---|---|---|
| Kubernetes | 1.24+ | `kubectl version` |
| Velero | 1.13.0+ | `kubectl -n velero get deploy velero -o jsonpath='{.spec.template.spec.containers[0].image}'` |
| CSI Snapshotter | 8.0+ | `kubectl get crd volumesnapshots.snapshot.storage.k8s.io` |
| MinIO (或其它 S3) | 任意 | `kubectl -n minio get pods` |
| Helm | 3.0+ | `helm version` |
| SupKube | 0.8.5-alpha+ | `kubectl -n supkube exec deploy/supkube-backend -- wget -qO- http://localhost:8080/api/v1/status` |

### 0.2 测试数据：test-app

每轮测试开始前部署 test-app 作为被保护应用：

```bash
cat <<'EOF' | kubectl apply -f -
apiVersion: v1
kind: Namespace
metadata:
  name: test-app
---
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: test-app
type: Opaque
stringData:
  POSTGRES_USER: testuser
  POSTGRES_PASSWORD: testpass
  POSTGRES_DB: testdb
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: test-app
spec:
  serviceName: postgres
  replicas: 1
  selector: { matchLabels: { app: postgres } }
  template:
    metadata: { labels: { app: postgres } }
    spec:
      containers:
        - name: postgres
          image: postgres:15
          envFrom: [{ secretRef: { name: postgres-credentials } }]
          ports: [{ containerPort: 5432 }]
          volumeMounts: [{ name: data, mountPath: /var/lib/postgresql/data }]
  volumeClaimTemplates:
    - metadata: { name: data }
      spec:
        accessModes: [ReadWriteOnce]
        storageClassName: csi-hostpath-sc
        resources: { requests: { storage: 5Gi } }
---
apiVersion: v1
kind: Service
metadata: { name: postgres, namespace: test-app }
spec:
  selector: { app: postgres }
  ports: [{ port: 5432 }]
  clusterIP: None
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: adminer, namespace: test-app }
spec:
  replicas: 1
  selector: { matchLabels: { app: adminer } }
  template:
    metadata: { labels: { app: adminer } }
    spec:
      containers:
        - name: adminer
          image: adminer:4
          ports: [{ containerPort: 8080 }]
---
apiVersion: v1
kind: Service
metadata: { name: adminer, namespace: test-app }
spec:
  type: NodePort
  selector: { app: adminer }
  ports: [{ port: 8080, nodePort: 30080 }]
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata: { name: csi-test-pvc, namespace: test-app }
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: csi-hostpath-sc
  resources: { requests: { storage: 1Gi } }
EOF
```

### 0.3 写入测试数据（用于验证恢复后数据是否一致）

```bash
# postgres pod 就绪后
kubectl -n test-app exec postgres-0 -- psql -U testuser -d testdb \
  -c "CREATE TABLE IF NOT EXISTS hello(msg text); INSERT INTO hello VALUES('first-backup-checkpoint');"

# 验证数据可读
kubectl -n test-app exec postgres-0 -- psql -U testuser -d testdb \
  -c "SELECT * FROM hello;"
```

### 0.4 测试收尾清理（强制项 — 防云账单）

> **决策 D-11（2026-05-30）**：**每一次**集群备份/还原测试结束后**必须**执行本清理。
> **真实教训**：restore 流程产生的 `VolumeSnapshotContent` 多带 `DeletionPolicy=Retain`，
> 仅 `kubectl delete ns` **不会**连带删除云端磁盘快照 → **静默累积账单**
> （2026-05-30 实测在 AKS 留下 2 个孤儿 Azure 快照）。

**① 应用层 + Velero CR**
```bash
kubectl delete ns test-app test-app-restore snaptest snaptest-restore 2>/dev/null
velero backup delete <test-backup>... --confirm   # 级联删云端 backup 数据 + 关联快照
velero restore delete <test-restore>... --confirm
kubectl -n velero delete cm -l supkube.io/kind=transform-set
```

**② 孤儿 VolumeSnapshotContent（Retain 策略，velero 不会自动清）**
```bash
# 列出测试还原残留的 VSC（VolumeSnapshot 在测试 ns 里的）
kubectl get volumesnapshotcontent -o custom-columns=\
NAME:.metadata.name,POLICY:.spec.deletionPolicy,NS:.spec.volumeSnapshotRef.namespace
kubectl delete volumesnapshotcontent <orphan-vsc>...
```

**③ 云端磁盘快照（最容易漏 → 直接花钱）**
```bash
# Azure（AKS 节点资源组 MC_<rg>_<cluster>_<region>）
az snapshot list -g "<MC-resource-group>" --query "[?contains(name,'snapshot-')].name" -o tsv
az snapshot delete -g "<MC-resource-group>" -n <snapshot-...>
# AWS: aws ec2 describe-snapshots --owner-ids self ... ; aws ec2 delete-snapshot --snapshot-id <id>
```

**④ 验收（必须为 0 / 仅剩非测试项）**
```bash
velero backup get | grep <test-prefix> || echo "OK: no test backups"
az snapshot list -g "<MC-resource-group>" --query "length([?contains(name,'snapshot-')])" -o tsv  # 期望 0
```

> 仪表板《决策》区 D-11 设有提醒项；CI/每日盘点应核对"无测试残留云快照"。

#### ⚠ Trap 警告：LoadBalancer Service finalizer (docker-desktop / 无 cloud LB provider 集群)

**症状**: `kubectl delete ns <restored-ns>` 后 ns **永远卡在 `Terminating`** (天级别等不完)。
`kubectl get ns <name> -o jsonpath='{.status.conditions}'` 显示:
```
"reason":"SomeFinalizersRemain"
"message":"... finalizers remaining: service.kubernetes.io/load-balancer-cleanup in 1 resource instances"
```

**真因**: 还原的备份带 `Service.spec.type: LoadBalancer` (如 Adminer / 各种 dev UI), K8s service controller
对 LB type 自动加 finalizer `service.kubernetes.io/load-balancer-cleanup`, **等 cloud provider cleanup
external LB**。docker-desktop / K3s 单机 / KubeEdge / 任何无 LB controller 的集群 → **永远等不到** → ns 死锁。

**真实教训**: 2026-05-31 demo 反向恢复, `reverse-restored-2` + `reverse-restored-local` 两 ns
卡 2 天 15 小时, 直到手动 patch finalizer 才解。

**手动救援** (一次性, 当下解套):
```bash
# 1. 找出 ns 内 LB type service
kubectl -n <stuck-ns> get svc | grep LoadBalancer
# 2. patch finalizer = null
kubectl -n <stuck-ns> patch svc <lb-svc-name> -p '{"metadata":{"finalizers":null}}' --type=merge
# 3. ns 几秒内自然消失
```

**根治方案** (跨集群还原前置):
- **PRD-002 v1.4 §4.1 新增 builtin Transform `strip-loadbalancer-to-clusterip`** (适用 TransformSet `bundle-azure-to-onprem` / `bundle-cross-cluster-to-local`), 还原前自动把 `Service.spec.type: LoadBalancer → ClusterIP`。
- 跨集群还原到本地集群 (docker-desktop / K3s) 必勾该 Transform, 否则演练后清不掉 ns。
- PRD-001 v2 Preflight Checklist 应检测目标集群是否有 LB controller, 无 → ⚠ warning 提示加 Transform (#104 follow-up)。

---

## 1. P0 冒烟测试套件

每次发版后 **第一时间运行**。任意一条失败 = 阻断发布。

| TC ID | 用例 | 预期耗时 |
|---|---|---|
| TC-ENV-001 | Helm install 完整流程跑通 | 3 min |
| TC-AUTH-001 | 单 provider (local password) 登录成功 | 1 min |
| TC-DASH-001 | Dashboard 首屏加载，所有卡片渲染 | 30 sec |
| TC-APP-001 | Applications 列表展示，test-app 出现 | 30 sec |
| TC-RP-001 | 创建一个 Backup（CSI 模式）→ Completed | 5 min |
| TC-RST-001 | 从该 Backup 恢复到新 ns → Completed | 5 min |
| TC-RST-005 | Pre-flight 检测到 NodePort 冲突 | 1 min |
| TC-RST-006 | 一键修复 + 恢复成功 | 5 min |
| TC-ACT-001 | Activity 页显示上述所有 Action | 30 sec |
| TC-TS-001 | 4 个内置 Transform Set 已种入 | 30 sec |
| **TC-REG-010** | **删除 RP 后底层 K8s + BSL 资源全清**（v0.8.8 加入，每次发版必跑）| 5 min |
| **TC-REG-011** | **Backup 错误消息必须可见**（v0.8.8.1 加入，§11.3 沉默错误用例）| 3 min |
| **TC-POL-006** | **L2 Policy 触发两个 Backup**（v0.8.9 双 Schedule 模型）| 8 min |
| **TC-APP-006** | **应用页一键 Snapshot 按钮 → label/annotation 契约**（v0.8.9.2 加入，回归 Custom=hourly footgun）| 4 min |
| **TC-POL-007** | **Plan-B 配对控制器：snapshot 完成 → export 自动触发**（v0.8.10 加入，回归 ADR-026 契约）| 5 min |
| **TC-IMP-001** | **Import Policy Continuous 60s 跨集群 sync**（v0.9.1.13 加入，双集群拓扑核心）| 8 min |
| **TC-IMP-003** | **Import fingerprint enforce + 篡改拒绝**（v0.9.1.13 加入，回归 ADR-038 安全契约）| 4 min |
| **TC-IMP-004** | **sharedSecret 不匹配的 enforce / warn / disabled 三态**（v0.9.1.13 加入）| 6 min |

**冒烟测试总时长**：约 70 分钟

---

## 2. 认证 / 授权 (TC-AUTH)

### TC-AUTH-001 [P0] 单 provider 本地密码登录
**类型**：Functional · **Sprint**：v0.8.5
**前置条件**：默认 values.yaml（仅 staticPasswords，无 connectors）
**步骤**：
1. 隐私窗口打开 `http://localhost:30888`
2. 观察登录页只有一个 "👤 Username & Password" 按钮
3. 点击按钮 → 跳转 Dex 表单
4. 输入：email `admin@supkube.local` / password `admin`
5. 点 Login → 回到 SupKube
**预期**：
- 跳转到 `/dashboard`
- 右上角显示紫色头像 + "admin"
- localStorage 含 `supkube.idToken`、`supkube.user`

### TC-AUTH-002 [P1] 登录页错误密码处理
**类型**：Negative · **Sprint**：v0.8.5
**步骤**：
1. 在 Dex 登录页输入正确 email + 错误密码
2. 提交
**预期**：
- 停留在 Dex 页面
- 显示 Dex 自带的 "Invalid credentials" 错误
- 不会跳转回 SupKube

### TC-AUTH-003 [P1] 多 provider 渲染
**类型**：Functional · **Sprint**：v0.8.5 step 2
**前置条件**：values.yaml 配置 GitHub connector
```yaml
auth:
  dex:
    connectors:
      - type: github
        id: github
        name: "Login with GitHub"
        config: { clientID: "...", clientSecret: "...", redirectURI: "..." }
```
helm upgrade 后
**步骤**：
1. 隐私窗口打开 `localhost:30888`
2. 观察登录页
**预期**：
- 显示 2 个按钮：👤 Username & Password、🐙 Login with GitHub
- 检查 backend env：`kubectl -n supkube exec deploy/supkube-backend -- printenv AUTH_PROVIDERS_JSON` 含 2 条目

### TC-AUTH-004 [P1] OIDC redirect 完整流程
**类型**：Integration · **Sprint**：v0.8.5
**前置条件**：已配真实的 GitHub OAuth App + 已 logout
**步骤**：
1. 点击 "Login with GitHub"
2. 浏览器跳转到 github.com（地址栏验证）
3. 完成 GitHub 授权
4. 浏览器跳回 `localhost:30888/dex/callback?code=...`
5. 自动再跳到 `localhost:30888/auth/callback?code=...&state=...`
6. SPA 处理 callback，跳到 `/dashboard`
**预期**：
- 整个流程不出现 401 / 500
- 右上角显示 GitHub 用户名（不是 "admin"）
- localStorage 含新 token

### TC-AUTH-005 [P1] Session 过期跳转
**类型**：Functional · **Sprint**：v0.8.5
**步骤**：
1. 登录成功
2. 在 DevTools Console：`localStorage.setItem('supkube.idToken', 'invalid-token')`
3. 刷新页面或点任意菜单
**预期**：
- 自动跳转到 `/login?reason=expired`
- 登录页提示 "Your session has expired"
- localStorage 中的失效 token 被清掉

### TC-AUTH-006 [P2] Logout 功能
**类型**：Functional · **Sprint**：v0.8.5
**步骤**：
1. 登录成功
2. 点右上角头像 → "退出登录"
**预期**：
- 跳转到 `/login`
- localStorage 中 `supkube.idToken`、`supkube.refreshToken`、`supkube.user` 全部清空
- 直接访问 `/dashboard` 应再被 redirect 到 `/login`

### TC-AUTH-007 [P1] K8s Secret 引用机制
**类型**：Integration · **Sprint**：v0.8.5 step 2.5
**前置条件**：
```bash
kubectl create secret generic supkube-github-test -n supkube \
  --from-literal=GITHUB_CLIENT_ID=test-id \
  --from-literal=GITHUB_CLIENT_SECRET=test-secret
```
values.yaml:
```yaml
auth.dex.envFromSecrets: [{secretName: supkube-github-test}]
auth.dex.connectors:
  - type: github
    id: github
    config:
      clientID: "$GITHUB_CLIENT_ID"
      clientSecret: "$GITHUB_CLIENT_SECRET"
      redirectURI: "..."
```
**步骤**：
1. helm upgrade 应用
2. 验证 Dex pod 拿到 env
**预期**：
- `kubectl -n supkube exec deploy/supkube-dex -- printenv | grep GITHUB` 显示 2 个 env 变量
- Dex 启动 log 包含 `config connector: github`（说明 $VAR 解析成功）
- 没有 "invalid client config" 报错

### TC-AUTH-008 [P2] CSRF state 校验
**类型**：Security · **Sprint**：v0.8.5
**步骤**：
1. 进入 `/login`，让 SPA 加载 providers
2. DevTools Console：`sessionStorage.removeItem('supkube.oauthState')`
3. 点 Login 完成 IdP 流程
4. Dex 跳回 `/auth/callback?code=...&state=ORIGINAL_NONCE`
**预期**：
- 回调处理失败，显示 "state mismatch — possible CSRF attempt"
- 不会拿到 token

---

## 3. Dashboard (TC-DASH)

### TC-DASH-001 [P0] 首屏加载
**类型**：Functional · **Sprint**：v0.5+
**前置条件**：已登录、有至少 1 个 Backup、1 个 Restore
**步骤**：
1. 访问 `/dashboard`
**预期**：
- 顶部 KPI 卡片：Restore Points / Storage Profiles / Compliance Score（数字正确）
- 中部图表：BackupSuccessTrend / ProtectionCoverage / StorageUsage 渲染无错
- 底部 Recent Restores 列表正常显示
- 整页加载 < 2 秒

### TC-DASH-002 [P1] Recent Restores 跳转
**类型**：Functional · **Sprint**：v0.8.0
**步骤**：
1. Dashboard 上的 Recent Restores 卡片点 "View All"
**预期**：
- 跳转 `/activity?type=Restore`
- Activity 页 Type 筛选器自动选中 "Restore"

---

## 4. Applications (TC-APP)

### TC-APP-001 [P0] 应用列表展示（v0.8.5 列改造）
**类型**：Functional · **Sprint**：v0.8.5
**步骤**：
1. 访问 `/applications`
**预期**：
- 表头：名称 · 状态 · 组件 · 标签 · 还原点 · 最近备份 · ⋮（**6 列**，"工作负载" 已重命名为 "组件"）
- test-app 行：
  - 名称：test-app
  - 状态：✓ 合规（已有 Backup 时）
  - 组件：⚙ 数字（应 >= 20，不是 v0.7 的 2-3）
  - 标签：有标签 chip
  - 还原点：📁 数字（紫色超链接）
  - 最近备份：backup 名 + 时间

### TC-APP-002 [P1] 组件数与 Velero 备份资源数一致
**类型**：Integration · **Sprint**：v0.8.5
**前置条件**：刚完成一次 test-app 的 backup
**步骤**：
1. 在 Activity 页打开该 Backup Action 详情
2. 看 Artifacts 数量（例如 31）
3. 在 Applications 页看 test-app 的"组件"列
**预期**：
- 两个数字相同或差异 ≤ 2（差异来自 SupKube 排除的 kube-root-ca.crt 等）

### TC-APP-003 [P1] 还原点列点击跳转
**类型**：Functional · **Sprint**：v0.8.5
**前置条件**：test-app 有 >= 1 个 Backup
**步骤**：
1. test-app 行的"还原点"单元格点击紫色数字
**预期**：
- 跳转到 `/backups?namespace=test-app&intent=restore`
- 顶部出现 intent banner "挑选一个还原点恢复 test-app"
- 列表只显示 test-app 的 Backup

### TC-APP-004 [P2] 应用详情抽屉
**类型**：Functional · **Sprint**：v0.5+
**步骤**：
1. test-app 行 ⋮ → 详情
**预期**：
- 右侧抽屉打开
- 显示工作负载列表、Labels、Last Backup 时间
- 点 [Backup Now] / [Restore] / [Create Policy] 触发对应跳转

### TC-APP-005 [P2] 系统命名空间过滤
**类型**：Functional · **Sprint**：v0.5+
**步骤**：
1. 访问 `/applications`
**预期**：
- 不显示 `kube-system / kube-public / kube-node-lease`
- 显示 `default / velero / minio / supkube`

### TC-APP-006 [P0] 一键 Snapshot 按钮（v0.8.9.2 加入，每次发版必跑）
**类型**：Functional · Regression · **Sprint**：v0.8.9.2
**前置条件**：test-app 已部署且至少有 1 个绑定的 PVC（CSI driver = `csi-hostpath`），登录账户 ≥ editor 且 namespace scope 覆盖 test-app。

**步骤**：
1. 访问 `/applications`
2. test-app 行 ⋮ → **Snapshot**（即 `applications.snapshot`，不是 `Backup`）
3. 在弹出的对话框中：标题应包含 ns 名 `test-app`；输入备注 `TC-APP-006`；点击 **立即快照**
4. 等待 toast 出现，记录返回的 `backupName`
5. 跳转到 `/backups`，等待该 backup 进入 **Completed**
6. 在 `kubectl -n velero get backup <backupName> -o yaml` 查看：
   - `metadata.labels`:
     - `supkube.io/manual-snapshot: "true"`
     - `supkube.io/source-namespace: test-app`
   - `metadata.annotations`:
     - `supkube.io/triggered-by: app-manual-snapshot`
     - `supkube.io/created-by-user: <当前登录用户>`
     - `supkube.io/comment: TC-APP-006`
   - `spec.snapshotVolumes: true` · `defaultVolumesToFsBackup: false` · `snapshotMoveData: false`
   - `spec.ttl: 24h0m0s`
7. 回到 `/backups`，确认 Policy 列显示紫色徽章 **📸 即时快照**（不是 `(manual)` 灰字）
8. 鼠标悬停徽章：tooltip 应包含 "由 \<user\> 在应用页一键创建" + 备注 `💬 TC-APP-006`
9. 顶部 Type 筛选器选 **📸 即时快照**，列表应只剩这一条
10. 在该 backup 的 ⋮ → **Restore** 走完一次恢复（验证它和正常 RP 同样可恢复）

**预期**：
- 步骤 3 提交后无须前端构造任何 spec 字段，POST 到 `/api/v1/applications/test-app/snapshot`，body 仅含 `{ comment }`
- 步骤 4 返回 `{ backupName, namespace, triggeredBy: "app-manual-snapshot", user, ttl: "24h0m0s", watchUrl }`
- 步骤 6 所有 label / annotation / spec 字段精确匹配
- 步骤 7 Policy 列显示 📸 即时快照徽章（紫色背景 `#f3eefe`）
- 步骤 9 筛选器准确过滤
- 步骤 10 恢复成功（与 TC-RST-001 等价）

**回归动机**：
- v0.8.9.2 引入"一键 Snapshot"为独立 endpoint。如果未来有人把它合回 `CreateBackup` 通用入口，前端必须再次承担"hard-code 安全默认值"职责 → v0.7 时代 Custom=hourly 类型的 footgun 就会复活。
- 该用例锁定 endpoint 行为契约：默认值（CSI / 24h / 默认 BSL）必须由后端硬编码，label / annotation 必须存在，否则 Restore Points 页的徽章 + 筛选器立即失效。

**自动化建议**：用 `curl -X POST /api/v1/applications/test-app/snapshot -d '{"comment":"smoke"}'` 在 CI smoke 阶段跑，断言响应 + `kubectl get backup -l supkube.io/manual-snapshot=true` 至少一条。

### TC-APP-007 [P1] 一键 Snapshot 受保护 namespace 拒绝
**类型**：Negative · **Sprint**：v0.8.9.2
**步骤**：
1. 在浏览器 devtools / curl 直接构造请求：`POST /api/v1/applications/kube-system/snapshot`
2. 同样测试 `velero` / `supkube`
**预期**：
- 返回 `400` + `error: "refusing to snapshot protected namespace ..."`
- 前端如能触发该路径（理论上 Applications 页已过滤掉受保护 ns，但 API 防御层不能依赖前端）：弹出 `applications.snapshotProtectedNs` 文案 `不允许对受保护命名空间执行快照`

---

## 5. Restore Points (TC-RP)

### TC-RP-001 [P0] 创建 CSI 模式 Backup
**类型**：Functional · **Sprint**：v0.6
**前置条件**：test-app 已部署，StorageClass `csi-hostpath-sc` 可用
**步骤**：
1. 进入 `/backups` → [Create Restore Point]
2. 填表：
   - Name: `test-app-backup-csi-001`
   - Included Namespaces: `test-app`
   - TTL: `720h`
   - Storage Location: `default`
   - Include Volumes: ON
   - Volume Backup Mode: CSI
3. 提交
**预期**：
- 新行出现，phase 经过 `InProgress` → `Completed`（< 5 分钟）
- itemsBackedUp / totalItems = N/N（完整数量）
- 在 K8s 上 `kubectl get volumesnapshots -n test-app` 有新的 VS

### TC-RP-002 [P0] 创建 Filesystem 模式 Backup
**类型**：Functional · **Sprint**：v0.6
**步骤**：同上但 Volume Backup Mode 选 Filesystem
**预期**：
- phase = Completed
- 不创建 VolumeSnapshot
- `kubectl get podvolumebackups -n velero` 有新条目

### TC-RP-003 [P1] Backup 失败原因可见
**类型**：Negative · **Sprint**：v0.7.11
**前置条件**：人为构造失败（例如 BSL 凭据错）
**步骤**：
1. 触发一个 Backup
2. 等到 phase=Failed
3. 进入 BackupDetail 或 Activity 详情
**预期**：
- failureReason 字段不为空
- 错误消息可读

### TC-RP-004 [P0] 级联删除走 DeleteBackupRequest
**类型**：Integration · **Sprint**：v0.7.9
**前置条件**：存在一个 Completed Backup
**步骤**：
1. Backup 行 ⋮ → 删除
2. 确认对话框点击 [Delete]
3. 30 秒后刷新列表
**预期**：
- 确认对话框列出 4 条级联删除内容（tarball / VolumeSnapshot / PVB / Backup CR）
- 删除后：
  - `kubectl get deletebackuprequests -n velero` 看到对应请求
  - `kubectl get backups -n velero` 不再有该 Backup
  - BSL 上的 tarball 已删除
  - 关联 VolumeSnapshot 已删除

### TC-RP-005 [P1] 排序：默认创建时间倒序
**类型**：Functional · **Sprint**：v0.7.8
**步骤**：
1. 创建 3 个 Backup，间隔 30 秒
2. 访问 `/backups`
**预期**：
- 列表按创建时间倒序，最新在最上

### TC-RP-006 [P1] 命名空间 chip 筛选（来自 Applications 页跳转）
**类型**：Integration · **Sprint**：v0.7.13
**步骤**：
1. 从 Applications 页 test-app 行的 ⋮ → 恢复 进入
**预期**：
- 顶部出现紫色 chip `📦 test-app ×`
- 列表只显示 test-app 的 Backup
- 点 chip 的 × → 筛选取消，列表显示全部
- URL 变成不带 query

### TC-RP-007 [P2] Source 列 Local/Imported 判断
**类型**：Functional · **Sprint**：v0.7.2
**前置条件**：BSL 上有从另一个集群同步过来的 Backup（手动准备或单集群跳过）
**步骤**：
1. 访问 `/backups`
2. 看 Source 列
**预期**：
- 本集群创建的 = 🏠 Local
- 跨集群同步过来的 = 🌐 Imported
- 悬停在 Source 上有 tooltip 显示 K8s 版本指纹

---

## 6. Activity (TC-ACT)

### TC-ACT-001 [P0] Action 列表展示
**类型**：Functional · **Sprint**：v0.8.0
**前置条件**：至少 1 个 Backup + 1 个 Restore
**步骤**：
1. 访问 `/activity`
**预期**：
- 顶部 Action Duration 柱状图渲染
- KPI 一行 7 个数字（total / completed / failed / skipped / avg / live / retired）
- Actions 列表按时间倒序，每张卡片显示：状态徽章 + Type + Phase 多行清单 + refs + 时间

### TC-ACT-002 [P1] Action 详情抽屉
**类型**：Functional · **Sprint**：v0.8.0
**步骤**：
1. 点任意 Action 卡片
**预期**：
- 右侧抽屉打开
- 显示 Type / Status / Phases / Refs / Artifacts table / 时间
- 失败 Restore 的话还显示详细 Errors 分组

### TC-ACT-003 [P1] Phase 实时进度
**类型**：Functional · **Sprint**：v0.8.0
**步骤**：
1. 触发一个新 Backup
2. 立即进入 Activity 页面观察这个 Action 卡片
**预期**：
- Phase 列表实时更新：
  - Validating (✓) → Snapshotting Application Components (⏳) → Uploading to Storage Profile (⏳) → Finalizing (⏳) → All phases completed successfully. (✓)
- 5 秒内有 phase 状态更新（轮询）

### TC-ACT-004 [P1] 失败 Restore 详细错误展示
**类型**：Integration · **Sprint**：v0.8.3 + v0.7.11
**前置条件**：手动制造 PartiallyFailed 的 Restore（例如不修 NodePort 冲突直接恢复）
**步骤**：
1. 在 Activity 找这个 Restore Action
2. 点开详情抽屉
**预期**：
- "健康" 行显示 N 个错误
- 下方 "Errors (N)" 块按 Namespace 分组展开
- 每条错误消息可读，能定位到具体资源

### TC-ACT-005 [P1] 排序：失败 Restore 排在最顶
**类型**：Regression · **Sprint**：v0.8.3
**前置条件**：刚做过一次 FailedValidation 的 Restore（无 startTime）
**步骤**：
1. 刷新 Activity 页
**预期**：
- 这个 FailedValidation 的 Restore 出现在列表**最顶**（按 creationTimestamp fallback 排）
- 不会因为 startTime 为空被沉到底部

### TC-ACT-006 [P1] 轮询频率自适应
**类型**：Performance · **Sprint**：v0.8.0
**步骤**：
1. 有 running Action 时打开 DevTools Network
2. 观察 `/api/v1/actions` 请求频率
3. 所有 Action 完成后再观察
**预期**：
- 有 running 时：每 5 秒一次
- 无 running 时：每 30 秒一次

### TC-ACT-007 [P2] Type 筛选器
**类型**：Functional · **Sprint**：v0.8.0
**步骤**：
1. 选 Type=Backup
**预期**：列表只显示 Backup actions
2. 选 Type=Restore
**预期**：列表只显示 Restore actions

### TC-ACT-008 [P2] /restores 旧 URL 重定向
**类型**：Regression · **Sprint**：v0.8.0
**步骤**：
1. 浏览器地址栏直接输入 `localhost:30888/restores`
**预期**：
- 自动跳转到 `/activity?type=Restore`

---

## 7. Restore 流程 (TC-RST)

### TC-RST-001 [P0] 基础 in-place 恢复
**类型**：Functional · **Sprint**：v0.7.10
**前置条件**：test-app 有完成的 Backup
**步骤**：
1. test-app 行的 ⋮ → Restore
2. 抽屉打开，默认目标 ns = test-app（原 ns）
3. 弹出黄色警告 + checkbox
4. 勾选 "我已了解 — 删除并重建该命名空间"
5. 点 [恢复]
**预期**：
- 后端先 kubectl delete ns test-app 并等到完全消失
- 然后创建 Velero Restore CR
- 跳到 Activity 页能看到 Restore Action
- 完成后 `kubectl get ns test-app` 重新存在，所有资源恢复

### TC-RST-002 [P0] cross-ns 恢复 + Pre-flight Check 自动触发
**类型**：Integration · **Sprint**：v0.7.12 + v0.7.10
**步骤**：
1. test-app 行 ⋮ → Restore
2. 抽屉目标 ns 改为 `test-app-restore`（不存在）
3. 等待 Pre-flight 自动运行（约 1-2 秒）
**预期**：
- Pre-flight Check 区块自动出现
- 列出冲突卡片：
  - 🔴 NodePort 已被占用 — Service test-app/adminer
  - 🔴 clusterIP 已被占用 — Service test-app/adminer
  - 🟡 PVC csi-test-pvc 绑定固定 PV
  - 🟡 PVC postgres-data-postgres-0 绑定固定 PV
- 顶部摘要：2 阻断 / 2 警告
- 因为有 blocker，[恢复] 按钮 disabled

### TC-RST-003 [P0] 一键修复后恢复成功
**类型**：Integration · **Sprint**：v0.8.4 + v0.7.12
**前置条件**：续 TC-RST-002 的场景
**步骤**：
1. 4 个冲突逐个点 [一键修复]
2. 每个卡片底部出现绿色 ✓ "已由 preflight-fix-... 修复"
3. [恢复] 按钮自动变可点
4. 点 [恢复]
5. 等待 Restore Action 完成
**预期**：
- Restore phase = Completed（**不是** PartiallyFailed）
- 0 个 error
- `kubectl get ns test-app-restore` 存在，所有资源就位
- `kubectl -n test-app-restore exec postgres-0 -- psql -U testuser -d testdb -c "SELECT * FROM hello;"` 看到 first-backup-checkpoint 这条数据

### TC-RST-004 [P1] 创建新 namespace 内联表单
**类型**：Functional · **Sprint**：v0.7.10
**步骤**：
1. Restore Drawer → "新建命名空间"
2. 输入 `my-restored-test-app`
3. 点 Create
**预期**：
- toast 提示 "命名空间 my-restored-test-app 创建成功"
- 目标 ns 自动选中刚建的这个
- `kubectl get ns my-restored-test-app` 存在
- 该 ns 带 label `supkube.io/managed-by=supkube`

### TC-RST-005 [P1] 受保护 namespace 创建/删除拒绝
**类型**：Negative · **Sprint**：v0.7.10
**步骤**：
1. Restore Drawer → 新建命名空间 → 输入 `kube-system`
2. 点 Create
**预期**：
- 报错 "namespace kube-system is protected"
- ns 未创建

### TC-RST-006 [P1] Spec 清单组件选择
**类型**：Functional · **Sprint**：v0.7.10
**步骤**：
1. 打开 Restore Drawer
2. Spec 清单加载完毕（test-app 应该有 7+ 个组件）
3. 取消勾选其中 2 个组件
4. 提交恢复
**预期**：
- Velero Restore CR 的 `spec.includedResources` 只包含勾选的资源类型
- 实际恢复时跳过取消勾选的

### TC-RST-007 [P1] Apply Suggested Fix 累积合并
**类型**：Regression · **Sprint**：v0.8.3
**前置条件**：cross-ns 恢复出现 4 个冲突
**步骤**：
1. 依次点 4 个冲突的 [一键修复]
**预期**：
- 4 次点击后 K8s 上 `kubectl -n velero get cm -l supkube.io/auto-generated=true` 只有 **1 个** ConfigMap（不是 4 个）
- 这个 CM 的 `data.rules.yaml` 包含 4 条 rule
- Restore.spec.resourceModifier 指向这个 CM

### TC-RST-008 [P1] 详细错误展示（PartiallyFailed）
**类型**：Integration · **Sprint**：v0.7.11
**前置条件**：通过 TC-RST-002 但**不**修复冲突，直接勾选 "忽略阻断" 后恢复
**步骤**：
1. Restore 完成（phase=PartiallyFailed）
2. 进入 Activity，点该 Restore 的详情抽屉
3. 看 Errors (N) 区块
**预期**：
- 显示 "Errors (N)" 标题
- 每条错误来源分组（Namespace: test-app-restore）
- 错误消息包含 "provided port is already allocated" 或类似

### TC-RST-009 [P2] existingResourcePolicy 选项
**类型**：Functional · **Sprint**：v0.7.10
**步骤**：
1. cross-ns 恢复（不勾选覆盖，跨 ns）
2. "当目标资源已存在时" radio 选 "更新已存在资源"
3. 提交
**预期**：
- Velero Restore CR 的 `spec.existingResourcePolicy = "update"`

### TC-RST-010 [P2] Transform Set 显式选择
**类型**：Functional · **Sprint**：v0.8.2
**步骤**：
1. Restore Drawer 的 "变换集" 下拉选 `strip-clusterip` 内置模板
2. 提交
**预期**：
- Velero Restore CR 的 `spec.resourceModifier.name = "strip-clusterip"`

---

## 8. Transform Sets (TC-TS)

### TC-TS-001 [P0] 内置模板自动种入
**类型**：Functional · **Sprint**：v0.8.4
**前置条件**：fresh install（清掉所有 cm 后重启 supkube-backend）
**步骤**：
1. `kubectl rollout restart deploy/supkube-backend -n supkube`
2. 等 5 秒
3. 查询 Transform Sets
**预期**：
- 至少 4 个内置模板：
  - strip-nodeport（JSON Patch on `/spec/ports/0/nodePort`）
  - strip-clusterip（mergePatches `{"spec":{"clusterIP":null,"clusterIPs":null}}`）
  - strip-loadbalancer-ip（mergePatches）
  - strip-pv-binding（mergePatches `{"spec":{"volumeName":null}}`）
- 每个的 ConfigMap **data 字段只有 1 个 key**（`rules.yaml`）
- description 在 annotation 而非 data

### TC-TS-002 [P1] Transform Sets 列表页
**类型**：Functional · **Sprint**：v0.8.2
**步骤**：
1. 访问 `/transform-sets`
**预期**：
- 卡片网格展示
- 内置 4 个卡片有 "BUILT-IN" 标签
- 每个卡片显示规则数 + patches 数 + 规则预览（前 2 行）

### TC-TS-003 [P1] 创建自定义 Transform Set
**类型**：Functional · **Sprint**：v0.8.2
**步骤**：
1. 点 [Create Transform Set]
2. 右侧抽屉打开编辑器
3. 填写 name / description
4. Add Rule：groupResource = `services`
5. Add Patch：operation=remove, path=`/spec/ports/0/nodePort`
6. 点 Save
**预期**：
- toast 提示创建成功
- 列表多出新卡片
- `kubectl -n velero get cm <new-name>` 存在
- data.rules.yaml 内容符合 Velero ResourceModifier schema

### TC-TS-004 [P1] 内置模板禁止编辑/删除
**类型**：Negative · **Sprint**：v0.8.2
**步骤**：
1. 点内置模板的 ⋮
**预期**：
- 菜单中 Edit / Delete 是 disabled
- 只 View / Clone 可用
2. 尝试通过 API 直接 PUT/DELETE
**预期**：
- API 返回 403 "built-in Transform Sets cannot be modified/deleted"

### TC-TS-005 [P2] Clone 内置模板
**类型**：Functional · **Sprint**：v0.8.2
**步骤**：
1. 点内置 strip-nodeport 的 ⋮ → Clone
2. 编辑器打开，name 自动填 strip-nodeport-copy
3. 改 name 为 my-strip-nodeport，Save
**预期**：
- 新建一个非内置 Transform Set
- 规则与原始一致
- 现在 Edit/Delete 可用（因为不是内置）

### TC-TS-006 [P1] YAML preview 实时同步
**类型**：Functional · **Sprint**：v0.8.2
**步骤**：
1. 在编辑器中改规则
2. 展开 "View as YAML" details
**预期**：
- YAML 实时反映当前表单状态
- 格式符合 Velero ResourceModifier 文档

### TC-TS-007 [P1] 一键修复批量端点
**类型**：Integration · **Sprint**：v0.8.3
**步骤**：通过 TC-RST-007 验证 — 批量合并行为
**预期**：同 TC-RST-007

### TC-TS-008 [P1] 启动迁移：删除 v0.8.2 broken CM
**类型**：Regression · **Sprint**：v0.8.3
**前置条件**：手动构造一个有 3 个 data key 的"假"Transform Set ConfigMap：
```bash
kubectl create cm broken-test -n velero \
  --from-literal=description=test \
  --from-literal=rules.yaml=test \
  --from-literal=rules.json=test
kubectl label cm broken-test -n velero supkube.io/kind=transform-set
```
**步骤**：
1. `kubectl rollout restart deploy/supkube-backend -n supkube`
2. 看日志：`kubectl -n supkube logs deploy/supkube-backend | grep migration`
**预期**：
- log 含 `migration: deleted broken v0.8.2 CM "broken-test"`
- `kubectl -n velero get cm broken-test` 不存在

---

## 9. Policies (TC-POL)

### TC-POL-001 [P1] 创建 Policy
**类型**：Functional · **Sprint**：v0.7.7
**步骤**：
1. 进入 `/policies` → [Create New Policy]
2. 填表：name / cron / namespace / TTL / mode
3. 提交
**预期**：
- `kubectl -n velero get schedule <name>` 存在
- 列表显示新条目，下次触发时间正确

### TC-POL-002 [P1] Pause / Resume
**类型**：Functional · **Sprint**：v0.7.7
**步骤**：
1. Policy 行 ⋮ → Pause
**预期**：
- Schedule 的 `spec.paused=true`
- UI 状态显示已暂停
2. ⋮ → Resume
**预期**：
- `spec.paused=false`
- 状态恢复

### TC-POL-003 [P1] Run Once
**类型**：Functional · **Sprint**：v0.7.7
**步骤**：
1. Policy 行 ⋮ → Run Once
**预期**：
- 立刻创建一个新 Backup CR
- Backup 名包含 `<schedule-name>-<timestamp>`
- 5 分钟内 phase=Completed

### TC-POL-004 [P1] 删除 Schedule 不删 Backup
**类型**：Functional · **Sprint**：v0.7.7
**步骤**：
1. 删除一个 Policy
**预期**：
- Schedule CR 消失
- 由它创建的 Backup CR 仍然存在

### TC-POL-005 [P1] 真编辑表单（待 v0.8.5 step 5 上线后启用）
**类型**：Functional · **Sprint**：v0.8.5 step 5
**步骤**：
1. Policy 行 ⋮ → Edit
**预期**：
- 表单 prefill 当前 Schedule 的所有可改字段
- 可改：cron、TTL、includedNamespaces、labelSelector、storageLocation、volume mode
- 不可改：name（K8s immutable）
- Save 后 Schedule CR 字段更新

### TC-POL-006 [P0] L2 Policy 触发两个 Backup（双 Schedule 模型）
**类型**：Functional · **Sprint**：v0.8.9
**背景**：v0.8.9 引入双 Schedule 模型，一次 L2 Policy 触发 = 两个 Backup
（Snapshot 半 + Export 半），各自独立 retention，UI 显示为两个 Restore Point
匹配 Kasten K10。详见 ADR-025。

**前置条件**：
- Velero 已装 + node-agent + Azure 插件
- BSL `myazureblob` Available
- test-app namespace 里有 PVC

**测试步骤**：
1. UI → Policies → Create
2. 模式选 **L2 Snapshot + Export**
3. Snapshot retention 24h，Export retention 720h
4. Export Storage Profile 选 `myazureblob`
5. Data Path 选 **Data Mover**
6. Frequency 选 **On Demand**（避免自动运行干扰测试）
7. Resources 选 `test-app`
8. Save

**验证步骤 A — 创建产生 2 个 Schedule**：
```bash
kubectl -n velero get schedules -l supkube.io/policy-name=<policy-name>
# 应出现 2 行：
#   <policy-name>           snapshot 半 (snapshotMoveData=false, ttl=24h)
#   <policy-name>-export    export   半 (snapshotMoveData=true,  ttl=720h)
```

**验证步骤 B — Run Once 产生 2 个 Backup**：
1. 点该 Policy 行 ⋮ → Run Once
2. ~10s 后 `kubectl -n velero get backups -l supkube.io/policy-name=<policy-name>` 应有 2 个 Backup
3. 一个 label `policy-role=snapshot`，spec.snapshotMoveData=false
4. 一个 label `policy-role=export`，spec.snapshotMoveData=true
5. 两个 Backup 时间戳后缀相同（time.Now().UTC() snapshot 共享）

**验证步骤 C — UI Restore Points 显示两个独立 RP**：
1. 进 Restore Points 页
2. 应看到两行新 RP，同 namespace、policy 列同名
3. 一行 TYPE chip = **📸 Snapshot**（蓝色）
4. 一行 TYPE chip = **🚚 Exported**（紫色）
5. Profile 列：Snapshot 显示 cluster default BSL，Exported 显示 `myazureblob`

**验证步骤 D — Edit 还原为双 retention 字段**：
1. Policy 行 ⋮ → Edit
2. 表单应显示双 retention（Snapshot 24h + Export 720h）
3. Data Path pill 应显示 Data Mover
4. 修改 Export retention 为 360h → Save
5. `kubectl -n velero get schedule <policy-name>-export -o jsonpath='{.spec.template.ttl}'` = 360h

**验证步骤 E — Delete 删除两个 Schedule**：
1. Policy 行 ⋮ → Delete
2. `kubectl -n velero get schedule -l supkube.io/policy-name=<policy-name>` 应为空

**反向验证（L1 模式仍单 Schedule）**：
6. 重复 1-2 但模式选 **L1 Snapshot only**
7. `kubectl get schedules -l supkube.io/policy-name=<l1-policy>` 应只有 1 行，label `policy-role=snapshot`

**RBAC 检查（editor 只能在 namespace scope 内操作）**：
- editor scope=test-app 可以创建 / Run Once / Delete 这个 Policy
- editor scope=other-ns 不能（403）

### TC-POL-007 [P0] Plan-B 配对控制器：snapshot 完成 → 导出半自动触发（v0.8.10 加入，每次发版必跑）
**类型**：Functional · Regression · **Sprint**：v0.8.10
**背景**：v0.8.10 引入 `internal/policypair/` 控制器。export 半 Schedule 永久 paused，由 SupKube 监听 snapshot 半 Completed → 立刻 fire export 半。详见 ADR-026。
**前置条件**：v0.8.10 fresh 安装（旧 v0.8.9 policy 不会自动迁移；先 delete 旧 policy）

**步骤**：
1. UI 创建一个 L2 dual policy `tc-pol-007`，frequency = On Demand
2. 验证 export 半 Schedule paused：
   ```bash
   kubectl -n velero get schedule tc-pol-007-export -o jsonpath='{.spec.paused}{"\n"}'
   # 期望：true
   ```
3. UI 点 ⋮ → Run Once
4. 立即 `kubectl -n velero get backup -l supkube.io/policy-name=tc-pol-007 --sort-by=.metadata.creationTimestamp`
   - **应只有 1 个 Backup**（snapshot 半），名字带 14 位时间戳后缀
5. 等 snapshot 半 phase = Completed（约 30s-2min）
6. 在 snapshot 完成后 10-20s 内再查：
   - **应出现第 2 个 Backup**（export 半），名字格式 `tc-pol-007-export-<同样的 14 位后缀>`
   - **关键：两半 annotation 必须完全对称**（v0.8.10 烟测发现的两个 bug）：
     ```bash
     # ───── 对称配对引用（两半互指）─────
     kubectl -n velero get backup <snapshot-name> -o jsonpath='{.metadata.annotations.supkube\.io/dual-rp-paired}'  # 必须 = <export-name>，不可为空
     kubectl -n velero get backup <export-name>   -o jsonpath='{.metadata.annotations.supkube\.io/dual-rp-paired}'  # 必须 = <snapshot-name>，不可为空
     # ⚠️ 历史 bug：v0.8.10 首次烟测时 ServiceAccount 缺 patch verb，导致
     # snapshot 半的 paired-to 永远写不进去。修复在 supkube-helm/.../rbac.yaml。
     # 如果你看到 snapshot 的 paired-to 为空 → 立刻查 backend pod 日志，期望看到
     # "cannot patch resource backups" → RBAC ClusterRole 漏 patch verb。

     # ───── 对称 policy-run-instant（两半同值）─────
     SNAP_TS=$(kubectl -n velero get backup <snapshot-name> -o jsonpath='{.metadata.annotations.supkube\.io/policy-run-instant}')
     EXP_TS=$(kubectl -n velero get backup <export-name>    -o jsonpath='{.metadata.annotations.supkube\.io/policy-run-instant}')
     [ "$SNAP_TS" = "$EXP_TS" ] && [ -n "$SNAP_TS" ] && echo "OK: same instant"
     # ⚠️ 历史 bug：v0.8.10 首次烟测时只在 export 半写 policy-run-instant，
     # 导致 snapshot 半的 Action Details 抽屉 "Policy Run At" 不显示。
     # 修复在 internal/policypair/policypair.go 的 annotate() 同时写两个键。

     # ───── 仅 export 半的标识（不必对称）─────
     kubectl -n velero get backup <export-name>   -o jsonpath='{.metadata.annotations.supkube\.io/triggered-by}'  # = dual-pair-controller
     kubectl -n velero get backup <export-name>   -o jsonpath='{.metadata.annotations.velero\.io/csi-volumesnapshot-content-retain-policy}'  # = retain（Plan C 预留）
     ```
   - **事件 fire 延迟**：
     ```bash
     kubectl -n supkube get events -l supkube.io/source=policypair --field-selector reason=PolicyPairFired -o json \
       | jq -r '.items[-1].note'
     # 应包含 "triggered ~Ns after snapshot-half completion"，N 应 ≤ 30 秒
     # 注：N 高于 60s 提示控制器 patch annotation 卡了（重试中），不是 fire 本身延迟
     ```
7. UI Activity 页：应出现两个独立 Action card
8. 打开任一 card 的详情抽屉：
   - 顶部 role chip 显示 📸 Snapshot half 或 🚚 Export half
   - "Paired with" 紫色 banner 链接到另一半
   - 点击 banner 链接 → 抽屉切到 peer，drawer 不关闭
   - "Policy Run At" 显示两半相同的时间戳（= snapshot creationTimestamp）
9. UI Restore Points 页：两条 RP 行，Type 列各为 📸 Snapshot / 🚚 Exported

**预期**：
- 步骤 4 只有 1 个 Backup（export 半未触发）
- 步骤 6 时间差 = snapshot 半 Completed → export Backup 创建 = ~10-20s（≪ v0.8.9 的 ~7 分钟）
- 所有 annotation / label 精确匹配

**回归动机**：
- ADR-026 锁定的核心契约：snapshot 半失败 → 不触发 export 半；snapshot 半成功 → 立即 + 一次 触发 export 半
- 如果未来有人改 `internal/policypair/scanOnce()` 漏掉 `shouldSkip` 防重入逻辑，会产生重复 export Backup —— 此用例每次发版跑能立刻发现
- Plan C 升级路径依赖 `retain` annotation 一定要在 —— 此用例锁定它

### TC-POL-008 [P1] Plan-B：snapshot 半失败时不触发 export 半
**类型**：Negative / Functional · **Sprint**：v0.8.10
**前置条件**：和 TC-POL-007 同
**步骤**：
1. 创建 L2 dual policy `tc-pol-008`，Resources 选一个**不存在**的 namespace（如 `nonexistent-ns`）让 snapshot 半 PartiallyFailed
2. Run Once
3. 等 snapshot 半 phase = PartiallyFailed（或 Failed）
4. 等 30s（控制器至少扫描 2-3 次）
5. `kubectl -n velero get backup -l supkube.io/policy-name=tc-pol-008` 应**仍只有 1 个 Backup**
6. 该 backup 的 annotation `supkube.io/dual-rp-no-peer` 应被打上，值为 `snapshot-half-phase=PartiallyFailed` 之类
7. `kubectl -n supkube get events -l supkube.io/source=policypair --field-selector reason=PolicyPairSkipped` 应能查到说明 event

**预期**：失败半 → 静默跳过 export 触发 + 记录原因 event；不留任何脏数据上 BSL。

**回归动机**：违反这条 = 客户在恢复时拿到"半成功"的数据，比"完全没有 export"更危险。座右铭 §11.4（默认值 = UX）。

---

## 9.5 Import Policy (TC-IMP)

> **背景**：v0.9.1.13 引入 Import Policy CRD — 跨集群 / 共享 BSL 模型的"持续 DR"机制。源集群跑 Snapshot Policy 备份到共享 BSL；目标集群跑 Import Policy 持续/定时 pull 那些备份，并通过 fingerprint 校验 source 真实性与数据完整性。详见 PRD-009 v2 §4.5 / PRD-007 §4.4 / ADR-038。
>
> 取代历史方案：之前依赖 Velero `backupSyncPeriod=60s` 全量扫描 BSL，无法区分 source cluster、无 HMAC 校验、配置粒度只到 BSL（不可按 ns / policy 收敛）。
>
> 4 个用例覆盖：Continuous 实跨集群 sync（TC-IMP-001）/ Scheduled cron（TC-IMP-002）/ fingerprint enforce + 篡改拒绝（TC-IMP-003）/ 跨集群 sharedSecret 不匹配的 enforce / warn / disabled 三态（TC-IMP-004）。

### TC-IMP-001 [P0] Continuous 60s 模式真实跨集群 sync
**类型**：Integration · Functional · **Sprint**：v0.9.1.13

**前置条件**：
- 2 个 cluster A / B 均装 SupKube `v0.9.1.13+`（`kubectl -n supkube exec deploy/supkube-backend -- wget -qO- http://localhost:8080/api/v1/status` 看 buildStamp 含 `v0.9.1.13` 或更新）
- 双集群 helm install 都带 `--set fingerprint.sharedSecret=<同一值>`（任意 base64 32 字节，例如 `openssl rand -base64 32` 一次生成，双方 `--set` 同值）
- 共享 BSL profile `X` 双方都已配置（Storage Locations 页同名同 bucket，凭据可写）
- A 集群已建 Snapshot Policy，备份 ns `demo-app` → BSL `X`，每 5 min 一次（或可 Run-once 触发）

**测试步骤**：
1. B 集群 UI → Policies → **New** → **Action Type = Import**
2. 表单：
   - `sourceBSL` = `X`
   - `mode` = `Continuous`
   - `continuousInterval` = `60s`
   - `fingerprintMode` = `enforce`
   - `sourceClusterID` = 空（接受任意源）
3. 提交。等 ≥ 90s（让 B 端 Import controller 至少完成一轮 watch + reconcile）。
4. A 集群手动 Run-once snapshot policy（Policies 行 ⋮ → Run Once）→ 等 A 端 Backup `phase=Completed`（~60s 内）。
5. B 集群 Restore Points 页点 refresh。

**预期**：
- B 集群在源 Backup completed 后 **60s + ε** 内 Restore Points 列表新增一行
- Type 列显示 **"Imported"** chip
- hover Type chip 显示 source cluster name（即 A 集群 control-plane node 名）
- fingerprint chip = **✅ valid**
- Velero CR 验证：
  ```bash
  kubectl --context=B -n velero get backup <bk-name> -o yaml
  # labels 必须包含：
  #   supkube.io/imported=true
  #   supkube.io/source-cluster=<A cluster-id>
  #   supkube.io/fingerprint-status=valid
  ```
- B 集群 `kubectl -n supkube get importpolicy <name> -o yaml`：
  - `status.phase=Active`
  - `status.lastSyncAt` 在过去 60s 内
  - `status.importedCount` ≥ 1

**自动化**：待 e2e/import_policy_test.go (Phase 1.x)
**关联**：PRD-009 v2 §4.5 / PRD-007 §4.4 / ADR-038 / task #88 + #157-163

---

### TC-IMP-002 [P1] Scheduled cron 模式
**类型**：Functional · **Sprint**：v0.9.1.13

**前置条件**：同 TC-IMP-001（A/B 双集群 + 共享 BSL X + 同 sharedSecret + A 有 Snapshot Policy）。

**测试步骤**：
1. B 集群建 ImportPolicy：
   - `mode` = `Scheduled`
   - `schedule` = `"*/5 * * * *"`
   - `sourceBSL` = `X`, `fingerprintMode` = `enforce`
2. 等到下一个 5 min 边界（例如挂表到 `14:05:00`）。

**预期**：
- B 集群在该边界 ±2s 内：
  ```bash
  kubectl --context=B -n supkube get importpolicy <name> -o jsonpath='{.status.lastSyncAt}'
  # 应等于 14:05:00 ±2s
  ```
- `status.importedCount` 在过去 5 min A 端有新 backup 时严格 +1（或 +N，按 A 端新 backup 数）
- `kubectl -n supkube get importpolicy <name> -o yaml` 中 `status.phase=Active`，`status.nextSyncAt` 指向下一个 5 min 边界

**反向验证**：
- 把 cron 改为非法值（如 `"abc"`）→ `status.phase=Invalid`，`status.lastError` 含 `ERR_CRON_PARSE`，UI Policies 行显示红色 ⚠

**自动化**：待 e2e/import_policy_test.go (Phase 1.x)
**关联**：PRD-009 v2 §4.5 / PRD-007 §4.4 / ADR-038 / task #88 + #157-163

---

### TC-IMP-003 [P0] fingerprint enforce + 篡改拒绝
**类型**：Negative · Security · Regression · **Sprint**：v0.9.1.13

**前置条件**：TC-IMP-001 全部跑过且通过（双集群已能正常 import 至少 1 条 RP）。

**测试步骤**：
1. A 集群 mc 命令篡改一个已 backup 的 fingerprint 文件：
   ```bash
   # <bk> 替换为目标 backup 名（A 端 kubectl -n velero get backup 拿）
   mc cat X/backups/<bk>/.supkube-fingerprint.json \
     | jq '.tarballSHA256="ffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffff"' \
     | mc pipe X/backups/<bk>/.supkube-fingerprint.json
   ```
2. B 集群 ImportPolicy 行 ⋮ → **Run Once**（强制立即一次 sync，跳过 60s 间隔）。

**预期**：
- B 集群 `kubectl -n supkube get importpolicy <name> -o yaml`：
  - `status.rejectedCount` +1（基线之上）
  - `status.lastError` 含 **`ERR_FINGERPRINT_HMAC_INVALID`**
- 该 backup **不被 import**：
  ```bash
  kubectl --context=B -n velero get backup | grep <bk>
  # 应为空（不应出现）
  ```
- UI Activity 页显示一条 importpolicy 失败事件，reason = `ImportRejected`，message 含 `fingerprint mismatch`

**清理**：用合法 fingerprint 覆盖回去（或在 A 端重跑一次 snapshot 生成新 backup + 新 fingerprint），避免污染后续用例。

**回归动机**：fingerprint enforce 是跨集群 DR 的**核心安全契约**——如果未来有人改 `internal/importpolicy/verify.go` 把 HMAC 校验降级成 warning，攻击者向共享 BSL 注入伪造 backup 就能在 B 端被自动 restore。此用例每次发版必跑。

**自动化**：待 e2e/import_policy_test.go (Phase 1.x)
**关联**：PRD-009 v2 §4.5 / PRD-007 §4.4 / ADR-038 / task #88 + #157-163

---

### TC-IMP-004 [P0] 跨集群 sharedSecret 不匹配（enforce / warn / disabled 三态）
**类型**：Negative · Security · **Sprint**：v0.9.1.13

**前置条件**：
- A 集群安装 `--set fingerprint.sharedSecret=AAAA<...>`（任意值 A）
- B 集群安装时**故意用了不同的** `--set fingerprint.sharedSecret=BBBB<...>`（值 B ≠ A）
- 双方都对 BSL `X` 可读；A 已经在跑 snapshot policy 至少产出 1 条 backup

**测试步骤**：
1. B 集群建 ImportPolicy：
   - `sourceBSL` = `X`, `mode` = `Continuous` (60s), `fingerprintMode` = **`enforce`**
   - 等 90s 让 B 端跑过至少 1 轮 sync
2. 编辑 ImportPolicy（kebab → Edit），改 `fingerprintMode` = **`warn`**，再等 90s
3. 再编辑改 `fingerprintMode` = **`disabled`**，再等 90s

**预期**（按阶段验证 `kubectl -n supkube get importpolicy <name> -o yaml` 的 status）：

| 阶段 | importedCount | rejectedCount | lastError | UI Restore Points |
|---|---|---|---|---|
| **enforce** | = 0 | > 0 | 含 `ERR_FINGERPRINT_HMAC_INVALID` | A 端的 backup 完全不出现 |
| **warn** | > 0（被接受） | 维持上阶段值 | 空 | RP 行 fingerprint chip 标黄 **"⚠ 未签名 / 签名无效"**，悬浮 tooltip 给出 secret mismatch 提示 |
| **disabled** | 继续增长 | 不再增加 | 空 | RP 行无 fingerprint chip（也不报警）；接受但**完全不校验** |

**关键验证**（warn 模式）：
```bash
kubectl --context=B -n velero get backup <bk> -o jsonpath='{.metadata.labels.supkube\.io/fingerprint-status}'
# 应 = "invalid"（接受但标记）
```

**回归动机**：三态切换是客户做"先看是否有 import 流量、再决定要不要锁紧 secret"的标准 ramp-up 路径。enforce / warn / disabled 任何一态的语义错误都会被客户立刻发现。

**自动化**：待 e2e/import_policy_test.go (Phase 1.x)
**关联**：PRD-009 v2 §4.5 / PRD-007 §4.4 / ADR-038 / task #88 + #157-163

---

## 10. Storage / VSL (TC-STG)

### TC-STG-001 [P1] BSL 创建 + 凭据 Secret 自动创建
**类型**：Functional · **Sprint**：v0.5
**步骤**：
1. 进入 `/storage` → [Create Storage Location]
2. 填表：name / provider=aws / bucket / region / endpoint=http://minio.minio:9000 / accessKey / secretKey
3. 提交
**预期**：
- BSL CR 创建
- 关联 Secret 自动创建
- BSL 状态 Available

### TC-STG-002 [P1] BSL Verify 功能
**类型**：Functional · **Sprint**：v0.5
**步骤**：
1. BSL 行 ⋮ → Verify
**预期**：
- 返回 status / provider / bucket / phase 信息
- BSL 不可达时显示对应错误

### TC-STG-003 [P1] VSL 创建
**类型**：Functional · **Sprint**：v0.6
**步骤**：
1. 进入 `/snapshot-locations` → Create
2. 填 provider=csi
3. 提交
**预期**：
- VSL CR 创建

---

## 11. Backup Advisor (TC-ADV)

### TC-ADV-001 [P1] 评分逻辑
**类型**：Functional · **Sprint**：v0.7.5
**步骤**：
1. 访问 `/advisor`
**预期**：
- test-app（有 PVC + StatefulSet + 服务）→ High Tier（>= 70 分）
- velero / supkube ns → 自动排除（不出现）
- 仅 ConfigMap 的小 ns → Low / Skip

### TC-ADV-002 [P2] 因子展示
**类型**：Functional · **Sprint**：v0.7.5
**步骤**：
1. 在 Advisor 页展开 test-app 的因子明细
**预期**：
- 显示每个因子的 reason + delta
- i18n 切换中英文，文字也跟着切

### TC-ADV-003 [P2] Schedule 推荐 cron 解释
**类型**：Functional · **Sprint**：v0.7.5
**步骤**：
1. 看 High Tier 应用的推荐 cron
**预期**：
- 显示 "每 6 小时" / "Every 6 hours" 等人类可读形式
- 不只是 `0 */6 * * *` 原始表达式

---

## 12. UI / i18n / 暗色 (TC-UX)

### TC-UX-001 [P0] 中英文切换
**类型**：Functional · **Sprint**：v0.7.6
**步骤**：
1. 右上角点 "中" → 切到 "EN"
2. 浏览每个页面
**预期**：
- 所有标签、按钮、提示翻译为英文
- 切换后保存到 localStorage（刷新仍生效）

### TC-UX-002 [P0] 暗色模式
**类型**：Functional · **Sprint**：v0.7.6
**步骤**：
1. 右上角点 ☾ 图标
**预期**：
- 整页变暗（背景深色、文字浅色）
- `<html>` 加上 `class="dark"`
- 所有 ECharts 图表配色也跟着变（蓝/红/灰柔和调）

### TC-UX-003 [P1] 暗色模式持久化
**类型**：Functional · **Sprint**：v0.7.6
**步骤**：
1. 切到暗色
2. 刷新页面
**预期**：
- 仍是暗色（不会闪一下白屏）

### TC-UX-004 [P1] 暗色模式覆盖完整性
**类型**：Functional · **Sprint**：v0.7.6
**步骤**：
1. 暗色模式下浏览所有页面
2. 注意有无白色 / 浅色块没覆盖
**预期**：
- 没有"明亮的卡片"刺眼
- 所有 h1-h6 标题颜色正确
- 表格、抽屉、对话框、表单全部暗色

### TC-UX-005 [P2] Restore Drawer 的 radio 对齐
**类型**：UI · **Sprint**：v0.8.3
**步骤**：
1. 打开 Restore Drawer
2. 看 "当目标资源已存在时" 两个 radio
**预期**：
- 两个 radio 圆圈水平对齐到同一 X
- 两段文字也对齐到同一起始 X
- 切换选中状态没有横向抖动

### TC-UX-006 [P2] Login 页品牌渲染
**类型**：UI · **Sprint**：v0.8.5
**步骤**：
1. 访问 `/login`
**预期**：
- 紫色渐变背景
- SupKube 品牌卡片居中（最大 420px）
- 阴影柔和
- footer 显示 Documentation 链接 + 版本号

---

## 13. 回归测试 (TC-REG)

### TC-REG-001 [P0] v0.5.1 BASE_URL bug
**类型**：Regression
**背景**：早期版本前端 axios baseURL 硬编码到 `localhost:8080`，导致从 K8S 服务外访问失败
**步骤**：
1. 通过 NodePort 30888 访问 SupKube
2. DevTools Network 看 `/api/v1/*` 请求
**预期**：
- 所有 API 请求走 `/api/v1/*` 相对路径
- 不出现 `localhost:8080` 跨域请求

### TC-REG-002 [P0] v0.7.9 删除残留 bug
**类型**：Regression
**背景**：直接 `kubectl delete backup <name>` 会被 BackupSyncController 重建
**步骤**：
1. 通过 UI 删除一个 Backup
2. 等 90 秒（让 sync controller 跑过一轮）
**预期**：
- Backup CR 不重新出现
- BSL 上的 tarball 已删除

### TC-REG-003 [P0] v0.7.10 cleanupBeforeRestore 解决 Velero 默认陷阱
**类型**：Regression
**背景**：Velero 默认 existingResourcePolicy=none 导致 in-place 恢复变 no-op
**步骤**：
1. test-app 已有 Backup，里面 hello 表只有 first-backup-checkpoint
2. 现在改数据：`INSERT INTO hello VALUES('marker-after-backup');`
3. UI 触发 in-place 恢复 + 勾选"删除并重建"
**预期**：
- 恢复完成后查 hello 表
- 只看到 first-backup-checkpoint
- **不**看到 marker-after-backup（说明真的回滚了）

### TC-REG-004 [P0] v0.8.2 ConfigMap 单 key bug
**类型**：Regression
**背景**：Velero 要求 ResourceModifier ConfigMap 的 data 字段只有 1 个 key
**步骤**：
1. 创建一个 Transform Set
2. 查看其 ConfigMap：`kubectl -n velero get cm <ts-name> -o yaml`
**预期**：
- data 字段只有 `rules.yaml` 一个 key
- description 和 rules-json 都在 annotation

### TC-REG-005 [P1] v0.8.3 hasBlockers 修复后状态
**类型**：Regression
**背景**：所有 blocker 都点修复后，"我已了解忽略阻断" checkbox 不应该再是必勾的
**步骤**：
1. cross-ns Restore → 检测到 2 blocker + 2 warning
2. 把 2 个 blocker 全部 [一键修复]
**预期**：
- "我已了解 — 忽略阻断" checkbox **不再显示**
- [恢复] 按钮自动变可点
- 不需要勾任何确认

### TC-REG-006 [P0] v0.8.4 Merge/Strategic Patches 消除假性 PartiallyFailed
**类型**：Regression
**背景**：v0.8.3 之前用 JSON Patch remove 失败的资源会报 "remove for nonexistent key" 错误
**步骤**：
1. cross-ns Restore + 4 个一键修复 + 提交
**预期**：
- Restore phase = Completed（**不是** PartiallyFailed）
- error count = 0
- 不再出现 "unable to remove nonexistent key: volumeName" 类错误

### TC-REG-007 [P0] v0.8.5 双 URL OIDC 设计
**类型**：Regression
**背景**：早期实现 backend 通过 `localhost:30888` 访问 Dex，pod 内不通
**步骤**：
1. 登录流程跑通（TC-AUTH-001 也覆盖）
2. 检查 backend 环境变量
**预期**：
- OIDC_ISSUER_URL = `http://localhost:30888/dex`（公开 URL）
- OIDC_DISCOVERY_URL = `http://supkube-dex:5556/dex`（集群内 URL）
- backend 验签从 JWKS 拿成功

### TC-REG-008 [P1] v0.8.5 bcrypt 哈希经过验证
**类型**：Regression
**背景**：早期 demo 哈希是从记忆抄的，根本不对应 `admin`
**步骤**：
1. fresh install
2. 用 `admin@supkube.local` / `admin` 登录
**预期**：
- 登录成功（不是 "Invalid credentials"）

### TC-REG-009 [P1] Activity 列表排序：失败 Action 不沉底
**类型**：Regression · **Sprint**：v0.8.3
**步骤**：
1. 触发一个 FailedValidation 的 Restore（无 startTime）
2. 等 1 秒后立即看 Activity 页
**预期**：
- 这个最新的失败 Restore 在列表**最顶**
- 不会因为 startTime 为空被排到底部

### TC-REG-012 [P1] 刚触发的 Backup 不显示 "UNKNOWN" 灰色徽章
**类型**：Regression · **Sprint**：v0.8.9.1
**背景**：v0.8.9 在 Activity 页发现已触发的 backup 在 Run Once 后 1-3 秒窗口内
显示 UNKNOWN 灰色徽章 —— 实际上 Velero Backup CR 处于"刚创建、controller
还没 ack"的瞬时空 phase。后端 `mapVeleroPhaseToStatus("")` 之前返回
`unknown`，让 UI 看上去像出了什么问题。修复后空 phase → `running`，与
"New" 状态合并语义（反正下一个状态就是 New 然后 InProgress）。

**测试步骤**：
1. 进 Policies 页，找一个最近没跑过的 Policy
2. 点 ⋮ → Run Once
3. 立刻切到 Activity 页
4. 找到刚才那条 action（最新的一行）

**预期结果**：
- ✅ 状态徽章不是灰色 UNKNOWN — 应该是黄色/橙色 RUNNING（即使 phase 是空）
- ✅ 几秒后状态变成 InProgress → WaitingForPluginOperations → Completed
  整个生命周期内**没有**任何一秒出现 UNKNOWN
- ✅ kubectl 角度看，backup CR 一旦 phase=Completed，Activity 立刻反映

**反向验证**：
- 一个真的状态异常的 backup（比如手动 kubectl create 一个 BackupSpec 完全空的）
  应该被认作 Failed 或 InProgress，**不**作为永久 UNKNOWN 卡住

### TC-REG-011 [P0] Backup PartiallyFailed / Failed 时错误消息必须可见
**类型**：Regression · **Sprint**：v0.8.8.1
**背景**（本文件开篇座右铭 §11.3 沉默错误的活体案例，每次发版必跑）：
v0.8.3 引入 Restore 错误详情后，Backup 这边留了一个 "TODO later" 没人记得 —
> `// Backups don't have a corresponding "BackupResults" path yet`

6 个月后用户跑出一个 PartiallyFailed backup，UI 显示 "Health: 2 errors"
但**没办法**点开看具体错误是什么 — DataUpload CR 里其实有完整消息
（`repository not initialized in the provided storage`），UI 视而不见。
这是 §11.3 "沉默错误是头号产品风险" 的精确触发场景。

**前置条件**：
- 集群有 Velero v1.18 + node-agent + Azure Blob BSL
- 至少一个能制造失败的场景，最简单：

  ```bash
  # 先建一个 Data Mover 备份让 Kopia repo 初始化
  # 然后手动清空 BSL 容器里的 kopia/ 目录
  # 再触发一次 Data Mover backup — DataUpload 会报 "repository not initialized"
  ```

**测试步骤**：
1. 制造一个 PartiallyFailed Backup（按前置条件方法或其它方法）
2. UI → Activity 页 → 点开这个 PartiallyFailed 行
3. Action Details 抽屉看 "Health: N errors"
4. **关键**：下方应该出现一个 "Errors (N)" section 列出**每个**失败 DataUpload / PodVolumeBackup 的：
   - 来源类型（DataUpload / PodVolumeBackup）
   - 命名空间 + sourcePVC
   - CR 名字（可 kubectl 反查）
   - **完整的 status.message 错误文本**（多行 monospace pre 块）

**预期结果**：
- ✅ 每个 error 都显示完整的 message 文本
- ✅ message 是真实 status.message，**不是** count、不是 "unknown"
- ✅ kubectl 看到的内容和 UI 看到的内容**字字一致**：
  ```bash
  curl -s -H "Authorization: Bearer demo-admin-token" \
    http://localhost:30888/api/v1/backups/<name>/errors | jq '.errors'
  kubectl -n velero get dataupload -l velero.io/backup-name=<name> \
    -o jsonpath='{range .items[*]}{.status.message}{"\n"}{end}'
  # 两者输出应该一致
  ```
- ✅ 若 Backup.status.errors > 0 但没有失败的 DataUpload/PVB（错误在 BSL 里），
  UI 显示 fetchError breadcrumb 指向 v0.9 计划，**不**沉默显示空

**反向验证（防止退化）**：
5. 创建一个 Completed Backup
6. 在 Activity 详情看不应该有 "Errors" section
7. action.errors === 0 时连 fetch 都不发（DevTools Network 检查无 /errors 请求）

**RBAC 检查**：
- viewer 角色应该能调 `/backups/<name>/errors`（与 GetBackup 同级权限）
- editor / admin 同样能调

### TC-REG-010 [P0] 删除 RP 后底层 K8s + BSL 资源必须全清
**类型**：Regression · **Sprint**：v0.8.8
**背景**（这是本文件开篇座右铭的触发事故，每次发版必跑）：
v0.8.7.6 用户在 UI 删了 6 个 RP，以为干净了。kubectl 一查发现
- 9 个 orphan VolumeSnapshotContent（deletionPolicy=Retain）
- Azure Blob `velero-dr/kopia/test-app/` 30+ MB Kopia 数据持续计费
- 4 个 orphan VolumeSnapshot + 4 个 snapcontent-* VSC
- 全程 UI 零提示

根因：Velero v1.18 Data Mover 故意把临时 VSC 设 deletionPolicy=Retain，
Velero **没有** 在父 Backup 删除时回收它们的逻辑（upstream #7838）。
v0.8.8 加了 SupKube 自己的 orphan-gc 包补这个漏。

**前置条件**：
- 集群装了 Velero v1.18 + node-agent + Azure 插件
- 至少一个可用的 Azure Blob BSL（如 `myazureblob`）
- test-app namespace 里有 PVC（如 csi-test-pvc）

**测试步骤**：
1. 通过 SupKube UI 创建一个 **L2 + Data Mover** 备份策略，target=myazureblob
2. 点 Run Once 触发一次备份，等到 status=Completed（应 ~1 min）
3. 验证一次刚备份的数据成功落进 Azure Blob 的 `kopia/<ns>/` 目录
4. 在 Restore Points 页**删除该 RP**
5. **等 90 秒**（让 Velero DeleteBackupRequest 跑 + SupKube post-delete GC 跑）
6. 跑以下 kubectl 检查：
   ```bash
   kubectl get vsc 2>&1 | wc -l                # 应为 0 或仅其它非该 backup 的
   kubectl get vs -A 2>&1 | wc -l              # 同上
   kubectl -n velero get backup 2>&1 | wc -l   # 该 backup 已消失
   kubectl -n velero get dataupload 2>&1 | wc -l  # 同
   kubectl -n velero get podvolumebackup 2>&1 | wc -l  # 同
   ```
7. 检查 Azure Blob `kopia/<ns>/` 目录 —— Kopia 仓库维护需异步等几小时，但**不应**出现 deleted RP 独有的新 chunk（这个验证较弱，主要看 K8s 这边）
8. 进 Settings → 集群健康 → 看 "最近一次扫描" 应该显示有删除条目

**预期结果**：
- ✅ 集群里**没有**任何 `velero.io/backup-name=<deleted-backup>` 的 VSC / VS / PVB / DataUpload
- ✅ Activity 页应该有一条 `OrphanCleanup` 事件，类型 Normal
- ✅ Settings 集群健康面板 "最近一次扫描" 显示删除条目和耗时

**额外验证（自动 GC 周期）**：
9. 关闭 Settings 集群健康面板的"自动清理"toggle
10. 重复 1-4 步
11. 等 90 秒后**不**会自动清理
12. 点"立即清理"按钮 → 应该立刻清完
13. 重新打开自动清理 toggle

**手动注入孤儿验证 GC 工作**：
```bash
cat <<EOF | kubectl apply -f -
apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshotContent
metadata:
  name: tc-reg-010-fake-orphan
  labels:
    velero.io/backup-name: definitely-doesnt-exist
spec:
  deletionPolicy: Retain
  driver: hostpath.csi.k8s.io
  source: { snapshotHandle: fake-handle }
  volumeSnapshotRef: { name: fake, namespace: test-app }
EOF
curl -X POST -H "Authorization: Bearer demo-admin-token" \
  http://localhost:30888/api/v1/admin/cleanup/orphans
kubectl get vsc tc-reg-010-fake-orphan   # 应该 NotFound
```

**RBAC 检查**（防止 v0.8.8 RBAC 漏权限重现）：
- SupKube ServiceAccount 应有 `volumesnapshotcontents`、`volumesnapshots`、`datauploads`、`podvolumebackups` 的 **delete + patch** 权限：
  ```bash
  kubectl auth can-i delete vsc --as=system:serviceaccount:supkube:supkube
  # 应 yes
  ```

---

## 14. 性能 / 负载 (TC-PERF)

### TC-PERF-001 [P2] Applications 列表大量 namespace
**类型**：Performance
**前置条件**：创建 50 个空 namespace
**步骤**：
1. 访问 `/applications`
2. 记录 `/api/v1/applications` 响应时间
**预期**：
- 响应时间 < 3 秒
- 列表渲染流畅，无卡顿

### TC-PERF-002 [P2] Activity 历史 100+
**类型**：Performance
**前置条件**：cluster 中有 100+ Backup + Restore CR
**步骤**：
1. 访问 `/activity`
2. 记录响应 + 渲染时间
**预期**：
- 响应 < 5 秒
- 滚动流畅

### TC-PERF-003 [P2] 组件计数效率
**类型**：Performance · **Sprint**：v0.8.5
**前置条件**：50 个 namespace，每个有 5-10 个资源
**步骤**：
1. 监控 `/api/v1/applications` 调用时 backend 发出的 K8s API 调用数
**预期**：
- 不论多少 namespace，K8s list 调用数总数固定（~14 次，每个 GVR 一次）
- **不是** 每 ns × 每 GVR

---

## 15. 安全 (TC-SEC)

### TC-SEC-001 [P0] 未认证请求被拒绝
**类型**：Security · **Sprint**：v0.8.5
**步骤**：
1. logout
2. 不带 token 直接 curl `/api/v1/backups`
**预期**：
- 返回 401
- 没有数据泄露

### TC-SEC-002 [P0] 无效 token 被拒绝
**类型**：Security · **Sprint**：v0.8.5
**步骤**：
1. 用 fake JWT 调 `/api/v1/backups`
**预期**：
- 返回 401 + "invalid token: ..."

### TC-SEC-003 [P1] OIDC state nonce 校验
**类型**：Security · **Sprint**：v0.8.5
**步骤**：见 TC-AUTH-008
**预期**：state mismatch 触发回调失败

### TC-SEC-004 [P1] 受保护 namespace 拒绝删除
**类型**：Security · **Sprint**：v0.7.10
**步骤**：通过 API 尝试 cleanup-before-restore 目标为 kube-system
**预期**：拒绝 + 错误消息

### TC-SEC-005 [P2] localStorage token 不应包含 refreshToken 明文（待考虑）
**类型**：Security
**注**：v0.8.5 当前简化为存 localStorage。v0.9 会考虑 httpOnly cookie，届时此 TC 期望更新

### TC-SEC-006 [P0] CORS 配置
**类型**：Security · **Sprint**：v0.5
**步骤**：
1. 从非同源页面尝试 cross-origin 请求 SupKube API
**预期**：
- OPTIONS 预检返回正确 CORS 头
- 不允许任意 Origin（理想上限制到已知前端域名 — 当前 v0.8.5 仍是 `*`，是已知 limitation）

### TC-SEC-007 [P0] RBAC enabled=false 时所有人是 admin
**类型**：Security · **Sprint**：v0.8.5 step 3
**前置条件**：`auth.rbac.enabled=false`（默认）
**步骤**：
1. 任意用户登录
2. 调 `/auth/me`
**预期**：
- response 中 `user.role = "admin"`
- 所有写操作都能成功

### TC-SEC-008 [P0] RBAC 启用后 editor 跨 ns 写入被拒
**类型**：Security · **Sprint**：v0.8.5 step 3
**前置条件**：
```yaml
auth.rbac:
  enabled: true
  bindings:
    - user: admin@supkube.local
      role: editor
      namespaces: [test-app]
```
登录为 `admin@supkube.local`
**步骤**：
1. 调 `POST /api/v1/backups` 带 `includedNamespaces: [other-ns]`
**预期**：
- 返回 403
- response.error = "namespace not in your scope"
- response.namespace = "other-ns"
- response.scope = ["test-app"]

### TC-SEC-009 [P0] RBAC editor 同 ns 写入成功
**类型**：Security · **Sprint**：v0.8.5 step 3
**前置条件**：同 TC-SEC-008
**步骤**：
1. 调 `POST /api/v1/backups` 带 `includedNamespaces: [test-app]`
**预期**：
- 返回 201
- Velero Backup CR 创建

### TC-SEC-010 [P0] RBAC viewer 无法写入
**类型**：Security · **Sprint**：v0.8.5 step 3
**前置条件**：
```yaml
auth.rbac:
  enabled: true
  defaultRole: viewer
```
**步骤**：
1. 未绑定的用户登录
2. 尝试创建 Backup
**预期**：
- 返回 403 "insufficient role"
- response.required = "editor"
- response.current = "viewer"

### TC-SEC-011 [P0] RBAC admin-only endpoint 拒绝 editor
**类型**：Security · **Sprint**：v0.8.5 step 3
**前置条件**：登录为 editor 用户
**步骤**：
1. 调 `POST /api/v1/namespaces`
**预期**：
- 返回 403
- required = admin / current = editor

### TC-SEC-012 [P0] 权限表完整性 — 新 endpoint 必须在表里
**类型**：Security · **Sprint**：v0.8.5 step 3
**步骤**：
1. fail-closed 校验：调一个**故意拼错的** path，例如 `GET /api/v1/typo-nonexistent`
**预期**：
- 返回 403 "endpoint not in RBAC table (this is a SupKube bug — please report)"
- 不是 404（因为 RBAC middleware 在 Gin 路由匹配后但 handler 前 — 路由匹配失败的话 401 才对，但这个用例可以用 OPTIONS 等手段命中 RBAC middleware 但不在表里）

### TC-SEC-013 [P1] 多 binding 累积取最高 role
**类型**：Security · **Sprint**：v0.8.5 step 3
**前置条件**：
```yaml
bindings:
  - group: g1
    role: editor
    namespaces: [ns-a]
  - group: g2
    role: editor
    namespaces: [ns-b]
  - group: g3
    role: admin
```
用户 groups: [g1, g3]
**步骤**：
1. `/auth/me`
**预期**：
- user.role = "admin"（高优先级覆盖）
- user.namespaceScope = null（admin 跳过 ns 检查）

### TC-SEC-014 [P1] 多 editor binding 累积 ns
**类型**：Security · **Sprint**：v0.8.5 step 3
**前置条件**：
```yaml
bindings:
  - group: g1
    role: editor
    namespaces: [ns-a]
  - group: g2
    role: editor
    namespaces: [ns-b, ns-c]
```
用户 groups: [g1, g2]
**步骤**：
1. `/auth/me`
**预期**：
- user.role = "editor"
- user.namespaceScope = [ns-a, ns-b, ns-c]（并集，去重）

### TC-SEC-015 [P1] 前端 canDo 与后端权限一致
**类型**：Integration · **Sprint**：v0.8.5 step 3
**步骤**：
1. 登录为 viewer
2. 浏览 /backups 页
**预期**：
- [Create Restore Point] 按钮 disabled，hover 显示 "您的角色不允许此操作"
- [Delete Selected] 按钮 disabled
- 列表本身可看（read 允许）

### TC-SEC-016 [P1] 用户徽章显示正确角色
**类型**：UX · **Sprint**：v0.8.5 step 3
**步骤**：
1. 登录为 admin / editor / viewer 三种身份
2. 看右上角用户徽章
**预期**：
- admin：紧贴用户名的红色 ADMIN chip
- editor：黄色 EDITOR chip
- viewer：绿色 VIEWER chip
- 颜色和角色严格对应（红=最高权限警示，绿=最低）

### TC-SEC-017 [P2] Cross-ns Restore 同时检查源 + 目标
**类型**：Security · **Sprint**：v0.8.5 step 3
**前置条件**：editor 绑定 namespaces: [ns-target]，但 backup 来自 ns-src
**步骤**：
1. 尝试创建 Restore：`namespaceMapping: { ns-src: ns-target }`
**预期**：
- 拒绝（源 ns 不在 scope，editor 不能恢复别人的备份到自己的 ns）

### TC-SEC-018 [P2] 静态用户的 user binding 生效
**类型**：Security · **Sprint**：v0.8.5 step 3
**前置条件**：
```yaml
bindings:
  - user: admin@supkube.local
    role: admin
```
**步骤**：
1. 用 admin@supkube.local 登录（Dex 静态密码）
**预期**：
- user.role = "admin"（即使该用户的 groups claim 为空）

---

## 16. 部署 / 升级 (TC-ENV)

### TC-ENV-001 [P0] Helm install 完整流程
**类型**：Functional · **Sprint**：v0.5
**步骤**：
```bash
helm install supkube ./supkube-helm/supkube -n supkube --create-namespace
kubectl rollout status deploy/supkube-backend -n supkube
kubectl rollout status deploy/supkube-frontend -n supkube
kubectl rollout status deploy/supkube-dex -n supkube
```
**预期**：
- 3 个 Deployment 全部 Available
- 访问 `localhost:30888` 见 SupKube 登录页

### TC-ENV-002 [P1] Helm upgrade --reset-values
**类型**：Functional
**背景**：早期 `--set` 注入的 user-supplied values 会粘住，不带 --reset-values 升级后镜像 tag 不变
**步骤**：
1. 旧版部署
2. `helm upgrade supkube ./supkube-helm/supkube -n supkube --reset-values`
**预期**：
- 镜像 tag 切换到最新 values.yaml 里的版本
- pods 滚动更新

### TC-ENV-003 [P1] Helm uninstall 清理彻底
**类型**：Functional
**步骤**：
1. `helm uninstall supkube -n supkube`
**预期**：
- 所有 SupKube 创建的 Deployment / Service / ConfigMap / Secret / RBAC 都被清除
- velero ns 不受影响
- BSL / Velero CR 不受影响

### TC-ENV-004 [P1] 启动时迁移：旧 Transform Set 清理
**类型**：Regression · **Sprint**：v0.8.3
**步骤**：见 TC-TS-008

### TC-ENV-005 [P2] RBAC 权限充足性
**类型**：Functional · **Sprint**：v0.7.10+
**步骤**：
```bash
SA=system:serviceaccount:supkube:supkube
for verb in get list create delete; do
  for res in backups restores schedules backupstoragelocations deletebackuprequests downloadrequests; do
    echo -n "$verb $res: "
    kubectl auth can-i $verb $res.velero.io --as=$SA
  done
done
```
**预期**：所有都返回 `yes`

### TC-ENV-006 [P2] Dex 可选部署
**类型**：Functional · **Sprint**：v0.8.5
**步骤**：
1. values.yaml `auth.dex.enabled=false`
2. helm upgrade
**预期**：
- Dex Deployment / Service / ConfigMap 都不被渲染
- backend 仍然启动（虽然 OIDC discovery 会失败，因为 issuer 无效）

### TC-ENV-007 [P1] nginx ConfigMap 改动触发 rollout
**类型**：Functional · **Sprint**：v0.8.5
**步骤**：
1. 改 nginx ConfigMap 内容
2. helm upgrade
**预期**：
- frontend pod 滚动重启（因为 `checksum/nginx` annotation 变了）

---

## 17. 测试报告模板

### 17.1 单轮测试汇总

```markdown
# SupKube v0.8.x 测试报告

**版本**：v0.8.5-alpha
**测试日期**：YYYY-MM-DD
**测试者**：张三 / 李四
**测试环境**：Docker Desktop K8s 1.28 + Velero 1.13.2 + MinIO 2024-xx

## 冒烟测试结果

| TC ID | 用例 | P | 结果 | 备注 |
|---|---|---|---|---|
| TC-ENV-001 | Helm install | 0 | ✅ Pass | |
| TC-AUTH-001 | 本地密码登录 | 0 | ✅ Pass | |
| TC-DASH-001 | Dashboard 首屏 | 0 | ✅ Pass | |
| TC-APP-001 | Applications 列表 | 0 | ✅ Pass | |
| TC-RP-001 | CSI Backup | 0 | ✅ Pass | 耗时 1m23s |
| TC-RST-001 | in-place Restore | 0 | ✅ Pass | |
| TC-RST-005 | Pre-flight 检测 | 0 | ✅ Pass | |
| TC-RST-006 | Apply Fix + 恢复 | 0 | ❌ Fail | 见 BUG-001 |
| TC-ACT-001 | Activity 列表 | 0 | ✅ Pass | |
| TC-TS-001 | 内置模板 | 0 | ✅ Pass | |

## 详细用例结果

（按章节顺序列出每条 TC 的结果）

## 发现的 Bug

### BUG-001 [P0] Apply Fix 后恢复仍然报错
- 复现步骤：TC-RST-006
- 预期：Restore Completed
- 实际：phase=PartiallyFailed，错误：...
- 截图/日志：附件 1
- 影响：阻断 P0 用例
```

### 17.2 Bug 报告字段约定

| 字段 | 说明 |
|---|---|
| ID | BUG-NNN（数字递增） |
| 优先级 | P0 / P1 / P2 / P3 |
| 状态 | Open / Investigating / Fix-in-progress / Fixed / Verified / Won't-fix |
| 关联 TC | TC-XXX-NNN |
| 重现率 | 100% / 偶现 / 一次出现 |
| 影响版本 | v0.8.x-alpha |
| 修复版本 | v0.8.y-alpha（修复后填） |

### 17.3 测试退出条件

发布前必须满足：

- ☑ 所有 **P0** 用例 100% Pass
- ☑ 所有 **P1** 用例 ≥ 95% Pass，剩余 5% 有明确 workaround
- ☑ 所有 **回归** (TC-REG-*) Pass
- ☑ 所有 **冒烟** 子集 < 30 分钟内跑完
- ☑ 没有 P0 Open bug
- ☑ P1 Open bug ≤ 3 个，且都有 workaround

---

## 18. CSI 平台适配走查（TC-CSI）

> **背景**：SupKube 的备份/还原能力依赖底层 CSI 驱动正确实现 `VolumeSnapshot`。不同平台/驱动的快照对象生命周期、存储级删除策略、binding mode 有差异 → 每个目标平台必须独立走查，通过后写入 `USER_MANUAL.md §5.X 平台支持矩阵`。
> **方法学起源**：2026-05-30 双驱动实测（详见 ADR-031）。
> **走查产出**：填本章 §18.2 模板 → 录入 §18.1 跟踪表 → 同步进 USER_MANUAL 支持矩阵。

### 18.1 平台走查跟踪表

| 平台 | CSI 驱动 | 已测版本 | 走查日期 | 走查者 | 结果 | 报告 |
|---|---|---|---|---|---|---|
| **Azure AKS** | `disk.csi.azure.com` | k8s 1.34 + Velero 1.18.0 | 2026-05-30 | Mars / Claude | ✅ Pass | §18.3.1 |
| **Docker Desktop**（csi-hostpath） | `hostpath.csi.k8s.io` | Velero 1.18.0 | 2026-05-30 | Mars / Claude | ✅ Pass（测试驱动） | §18.3.2 |
| AWS EKS | `ebs.csi.aws.com` | — | — | — | 🟡 Planned | — |
| GCP GKE | `pd.csi.storage.gke.io` | — | — | — | 🟡 Planned | — |
| Alibaba ACK | `diskplugin.csi.alibabacloud.com` | — | — | — | 🟡 Planned | — |
| Tencent TKE | `com.tencent.cloud.csi.cbs` | — | — | — | 🟡 Planned | — |
| VMware Tanzu | `csi.vsphere.vmware.com` | — | — | — | 🟡 Planned | — |
| Red Hat OpenShift | varies | — | — | — | 🟡 Planned | — |
| Rancher | varies | — | — | — | 🟡 Planned | — |
| KubeSphere | varies | — | — | — | 🟡 Planned | — |
| K3s（默认 local-path） | `local-path-provisioner` | — | — | — | ⚠ Limited | — |
| KubeEdge | varies (edge) | — | — | — | 🔬 Research | — |

### 18.2 走查模板（TC-CSI-PLATFORM）

每个平台走查产出一份独立报告。复制本模板填空：

**前置条件**
- K8s 版本：___
- Velero 版本：v1.18.x（chart 12.0.1+）
- CSI 驱动：___
- VolumeSnapshotClass：___（DeletionPolicy=Delete / Retain）
- StorageClass：___（VolumeBindingMode=Immediate / WaitForFirstConsumer）
- BSL：___（provider + bucket + Available?）
- kubectl 上下文：___

**步骤**
1. 创建测试 ns + PVC（SC=指定）+ pod 写入 `proof.txt`（含时间戳 + 平台名 + 驱动名）
2. 创建 CSI 快照备份：
   ```bash
   velero backup create snaptest-<platform>-<ts> \
     --include-namespaces <ns> \
     --snapshot-volumes=true \
     --default-volumes-to-fs-backup=false \
     --storage-location <bsl>
   ```
3. **实时**抓 VS/VSC 生命周期（每 3s 轮询）→ 记录：
   - VS/VSC 创建时间、消失时间（差值 = 对象存活窗口）
   - `VSC.spec.deletionPolicy`
   - `VSC.status.snapshotHandle`（云磁盘快照 ID）
   - `VSC.status.readyToUse`
4. backup `Phase=Completed` 后（等 ~30s）：
   - 确认 VS/VSC 对象**已消失**
   - 用云端 API 确认**存储级快照仍存在**（命令见 §18.4）
5. **还原**到新 ns：
   ```bash
   velero restore create snaptest-<platform>-restore \
     --from-backup <backup-name> \
     --namespace-mappings <src>:<src>-restore --wait
   ```
6. 验证 `kubectl -n <dst> exec writer -- cat /data/proof.txt` = 源 sentinel
7. **必清**（防云账单，详见 §0.4）：删 ns + `velero backup delete` + 孤儿 VSC（Retain）+ 云磁盘快照

**通过标准**
- ✅ VS/VSC 对象在 backup 完成后被移除（Velero 故意，跨驱动一致）
- ✅ 存储级快照保留到 backup 生命周期（生产驱动；test 驱动如 csi-hostpath 行为可能不同）
- ✅ 还原成功，proof.txt 字节一致
- ✅ 清理后云端快照计数归 0

### 18.3 已走查平台报告

#### 18.3.1 Azure AKS · `disk.csi.azure.com` · 2026-05-30

- 集群：aks-jumborca-dev / k8s 1.34.7 / Velero 1.18.0（chart 12.0.1，#102 升级后）
- VSClass：`azuredisk-vsc` (DeletionPolicy=Delete)
- 备份 `snaptest-csi-132557` Completed；Azure 快照 `snapshot-47bb930a-...`（1 GiB）实证保留 → backup delete 后清理
- 还原 `snaptest-restore-test` Completed，`proof.txt` 字节一致 ✓
- **关键发现**：v1.18 binary 必须配 v1.18 CRD（"Queued" phase 缺失会让所有 backup 卡 New）→ 见 #102 frankenstein 教训

#### 18.3.2 Docker Desktop · `hostpath.csi.k8s.io` · 2026-05-30

- 集群：docker-desktop / Velero 1.18.0
- VSClass：`csi-hostpath-snapclass` (DeletionPolicy=Delete)
- 实时捕获：VS=1 VSC=1 @20:19:33 → VS=0 VSC=0 @20:19:37（~4s 后被 Velero 移除）
- 还原 `snaptest-dd-restore` Completed，`proof.txt` 字节一致 ✓
- **关键发现**：csi-hostpath 是测试驱动，`.snap` 文件在 backup delete 时**不真删**（累积孤儿）；行为代表 Velero 对象生命周期，**不**代表生产驱动的存储级删除

### 18.4 各平台云端快照查询 / 删除命令

| 平台 | 查询 | 删除 |
|---|---|---|
| Azure | `az snapshot list -g <MC-rg>` | `az snapshot delete -g <MC-rg> -n <name>` |
| AWS | `aws ec2 describe-snapshots --owner-ids self` | `aws ec2 delete-snapshot --snapshot-id <id>` |
| GCP | `gcloud compute snapshots list` | `gcloud compute snapshots delete <name>` |
| Alibaba | `aliyun ecs DescribeSnapshots` | `aliyun ecs DeleteSnapshot --SnapshotId <id>` |
| Tencent | `tccli cbs DescribeSnapshots` | `tccli cbs DeleteSnapshots --SnapshotIds <id>` |
| VMware | vCenter UI / `govc snapshot.tree` | vCenter UI / `govc snapshot.remove` |
| csi-hostpath（开发自检） | `kubectl exec -n kube-system <csi-hostpathplugin> -- find /csi-data-dir -name '*.snap'` | （孤儿，需重启 plugin pod） |

---

## 19. AI Backup Advisor MVP (TC-AI-MVP)

> 对应 **PRD-011**（AI Backup Advisor MVP）+ ADR-037（统一数据采集架构）+ ADR-043（评分细则 v1.0.0）。覆盖 PRD-011 的 17 条 DoD（#1-#13 + H1/H2/H5 的 #14-#17）。
> **核心铁律（写进每个相关用例）**: ① 分数由 Go 规则引擎算、LLM 不参与打分、可复现（finding #2）; ② 置信度三档不用百分比（finding #3）; ③ 缺失数据用 status 枚举 `confirmed/optional/missing/unable_to_confirm/source_conflict` 显式建模，不得用 0 冒充"已确认无"; ④ 非自治、应用建议只预填不写。
>
> **权威评分矩阵（PRD-011 §4.2，Mars 2026-06-03 D-WAIT-002 拍板，喂 ADR-043 v1.0.0）—— 本节全部测试值以此为唯一来源，不杜撰**：
> 100 分制 · 4 维 · 标准对标：
> - **维度①备份覆盖与合规 25**：应用分类覆盖度 10（分级差异化+100%核心纳管=10 / 统一未分级或<5%僵尸=5 / 未分级有漏备=0）· RPO 达标率 10（满足/优于=10 / 基本满足窗口过大=5 / 不满足=0）· 元数据与配置备份 5（含 Manifest/拓扑/配置=5 / 仅纯数据=0）
> - **维度②韧性 3-2-1-1-0 共 35**：介质与存储多样性 10（≥2 种介质=10 / 单介质=5）· 异地与跨云 10（跨厂商或>100km=10 / 同厂商跨 region=6 / 同 AZ 同源=0）· 空气隔离与网络离线 15（真 Air-Gap 或专网备完即断=15 / 有隔离但同网络域/同 AD=8 / 无隔离生产可直挂=0）
> - **维度③防勒索与安全 20**：不可变存储 10（WORM Mode==COMPLIANCE=10 / Governance 或仅 IAM=6 / 可覆盖删除=0）· 加密与凭证 5（AES-256+TLS1.3+KMS 独立轮换=5 / 有加密但密钥同管=2）· 访问控制 5（MFA+RBAC+二次审批+审计异地不可篡改=5 / 单密码无 MFA 无审计=0）
> - **维度④成功率与可恢复性 20**：备份执行成功率 5（滑动窗口公式）· 恢复演练通过率 15（全自动沙箱+近3月100%=15 / 定期人工演练有报告=10 / 仅局部文件恢复测试=5 / 只备不练=0）
>
> **三个落地公式**：① 成功率滑动窗口 = 最近 14 天 / 最近 30 次 `success/total × 5`；Tier 1 核心资产**连续失败 > 3 次 → 该项直接计 0**。② WORM 断言 = `ObjectLockEnabled==Enabled` 且 `Mode==COMPLIANCE` 且 `RetainUntilDate > now+30d` 才给满分 10（Governance 给 6）。③ 安全级别 4 档：**90-100 极高韧性级 / 75-89 合规风险低 / 60-74 脆弱级 / <60 高危级**。
> **两条校准硬阈值**：无备份封顶 30（`last_backup_at == nil`）；高分校准 30（算分 ≥90 但 `last_backup_succeeded == false`）→ 均强制下调 30 + 落"高危级"。

### TC-AI-001 [P1] 5 模块采集与缺失标注
**类型**：Functional · **Sprint**：v0.11.x · **DoD**：1
**步骤**：
1. 对一个含 StatefulSet + PVC、但探测不到 VolumeSnapshotClass 的 ns 执行 `POST /api/v1/ai/analyze`
2. 检查返回 DSL 里的 `fact` 节点
**预期**：
- M1~M5 五模块字段全部落为 `fact` 节点 `{value, status, source, evidence_refs}`
- "是否支持 snapshot" = `{value:null, status: unable_to_confirm, ...}`，**不是** `false`/`0`
- 采集不到的字段 status ∈ `{missing, unable_to_confirm}`，绝无用空值冒充"已确认无"

### TC-AI-002 [P0] 规则引擎逐子项分档精确评分（4 维矩阵参数化）
**类型**：Unit (Go test, 参数化/table-driven) · **Sprint**：v0.11.x · **DoD**：2
> 对齐 PRD-011 §4.2 新 4 维矩阵（替代旧 5 维"层数×20"/"3-2-1 差份扣分"模型）。每个子项的**每一档**至少一个 fixture，逐项断言子项得分**精确**等于矩阵，再断言维度小计与总分。
**步骤**：
1. 为下表每个子项的每一档准备 fixture `AppContext`，调用纯函数 `Score(ctx) ScoreResult`
2. 断言 `ScoreResult.Breakdown[dim][subitem]` 子项得分精确等于矩阵档值
**预期**（子项档值精确命中，全部来自 §4.2，不杜撰）：
- **维度① 备份覆盖与合规（满分 25）**
  - 应用分类覆盖度：分级差异化+100%核心纳管→**10** / 统一未分级或<5%僵尸→**5** / 未分级有漏备→**0**
  - RPO 达标率：满足或优于→**10** / 基本满足但窗口过大→**5** / 不满足→**0**
  - 元数据与配置备份：含 Manifest/拓扑/配置→**5** / 仅纯数据→**0**
- **维度② 韧性 3-2-1-1-0（满分 35）**
  - 介质与存储多样性：≥2 种介质→**10** / 单介质→**5**
  - 异地与跨云：跨厂商或>100km→**10** / 同厂商跨 region→**6** / 同 AZ 同源→**0**
  - 空气隔离与网络离线：真 Air-Gap/专网备完即断→**15** / 有隔离但同网络域或同 AD→**8** / 无隔离生产可直挂→**0**
- **维度③ 防勒索与安全（满分 20）**
  - 不可变存储：WORM Mode==COMPLIANCE→**10** / Governance 或仅 IAM→**6** / 可覆盖删除→**0**（详见 TC-AI-018）
  - 加密与凭证：AES-256+TLS1.3+KMS 独立轮换→**5** / 有加密但密钥同管→**2**
  - 访问控制：MFA+RBAC+二次审批+审计异地不可篡改→**5** / 单密码无 MFA 无审计→**0**
- **维度④ 成功率与可恢复性（满分 20）**
  - 备份执行成功率：滑动窗口公式 `success/total × 5`（边界见 TC-AI-016）
  - 恢复演练通过率：全自动沙箱+近3月100%→**15** / 定期人工演练有报告→**10** / 仅局部文件恢复测试→**5** / 只备不练→**0**
- 满档 fixture（各子项均取最高档）总分 = 100；最低档 fixture（不触发硬阈值前提下）维度小计 = 各子项最低档之和
- 单测覆盖率 ≥ 90%，**每个子项每一档**至少 1 个用例

### TC-AI-003 [P0] 评分确定性可复现（纯函数 · finding #2）
**类型**：Unit · **Sprint**：v0.11.x · **DoD**：3
> 铁律：引擎是纯函数 `Score(AppContext) → ScoreResult`，同输入永远同输出；新鲜度/滑动窗口用**采集快照里的时间戳**而非 `time.Now()`。
**步骤**：
1. 同一份 `AppContext`（含一个固定的采集快照时间戳 `snapshot_at`）连续运行 `Score()` 100 次
2. 用反射/序列化对每两次 `ScoreResult` 做深度相等断言
3. 在两个不同的 wall-clock 时刻各跑一次（如 mock 系统时钟前后），结果仍须相等
**预期**：
- 100 次结果**完全一致**：总分、4 维及各子项 `Breakdown`、命中的扣分项 ID 列表、`scoreRulesVersion`（= `"v1.0.0"`）全等
- 不同 wall-clock 时刻输出相同 → 证明无 `time.Now()` 依赖（滑动窗口"最近 14 天/30 次"以 `snapshot_at` 为基准计算）
- 引擎包内 grep 无 `time.Now()` / `rand.` / `map` 无序遍历影响输出顺序

### TC-AI-004 [P0] 分数→安全级别 4 档边界映射（90/75/60）
**类型**：Unit (参数化) · **Sprint**：v0.11.x · **DoD**：4
> 对齐 §4.2 安全级别分档表（4 档，**替代**旧 5 档 SAFE/LOW/MEDIUM/HIGH/CRITICAL + 阈值 90/70/60/40）。硬阈值 90 / 75 / 60。
**步骤**：
1. 喂入边界分数：100, 90, 89, 75, 74, 60, 59, 30, 0
**预期**（边界严格按矩阵：90 含、75 含、60 含）：
- 90–100 → **极高韧性级**（对标 NIST 勒索防护，分钟级拉起）；故 100→极高、90→极高、89→合规风险低
- 75–89 → **合规风险低**（满足 ISO 27001 基本审计）；故 75→合规风险低、74→脆弱级
- 60–74 → **脆弱级**（无不可变防护，易被勒索连同生产加密）；故 60→脆弱级、59→高危级
- <60 → **高危级**（伪备份，随时面临永久丢失）；故 30→高危级、0→高危级
- 边界值（90/75/60）归属上档，参数化逐一断言

### TC-AI-005 [P0] 校准硬阈值 ①：无备份封顶 30
**类型**：Unit · **Sprint**：v0.11.x · **DoD**：5
> §4.2 校准硬阈值之一：`app.last_backup_at == nil`（从未备份）→ 即使算分 ≥30 也强制下调到 30 + 落"高危级"。
**步骤**：
1. 构造一个 `AppContext`：`last_backup_at == nil`，但其它配置使原始算分明显 >30（如算分 70）
2. 调 `Score()`
**预期**：
- 最终 `totalScore == 30`（强制封顶），安全级别 = **高危级**
- `ScoreResult` 标记该次命中 `calibration: "no_backup_cap_30"`
- 解释含"从未发生备份/无实际备份落地"，且保留原始算分供审计（如 `rawScore: 70`）

### TC-AI-016 [P0] 校准硬阈值 ②：高分校准 30（算分≥90 但最近失败）
**类型**：Unit · **Sprint**：v0.11.x · **DoD**：5
> §4.2 校准硬阈值之二：算分 ≥90 但 `app.last_backup_succeeded == false`（最近一次失败）→ 强制下调到 30 + 落"高危级"（配置看似完善但无实际备份落地）。
**步骤**：
1. 构造配置看似完善、原始算分 ≥90（如 95）但 `last_backup_succeeded == false` 的 fixture
2. 调 `Score()`
**预期**：
- 最终 `totalScore == 30`，安全级别 = **高危级**
- `ScoreResult` 标记 `calibration: "high_score_recalibrate_30"`
- 解释含"配置看似完善但最近一次备份失败、无实际备份落地"
- 反例：算分 88（<90）且最近失败 → **不**触发本阈值（边界断言：阈值是 ≥90）

### TC-AI-006 [P1] LLM 解释为合法 inferred 节点（finding #3）
**类型**：Functional · **Sprint**：v0.11.x · **DoD**：6
**步骤**：
1. 取一个有扣分项的评分结果，触发 LLM 解释
**预期**：
- 输出为 `inferred` 节点 `{conclusion, basis, confidence, evidence_refs}`
- `confidence` ∈ `{high, medium, low}`，**绝不**出现百分比数字

### TC-AI-007 [P0] LLM 不得改分（validator 以引擎为准丢弃 LLM 数字）
**类型**：Functional · **Sprint**：v0.11.x · **DoD**：7
> 铁律：LLM 不参与打分。validator 校验 LLM 输出里若出现与引擎分数/子项分不一致的数字，一律以引擎为准并丢弃 LLM 的数字。
**步骤**：
1. 引擎对某 fixture 算出 `totalScore=72`、维度②=20
2. 注入 mock LLM 响应，文本里塞入与引擎不一致的数字（如"综合得分 88 分，韧性 30 分"）
**预期**：
- validator 丢弃/纠正 LLM 文本里的数字，最终对外 `totalScore` 仍 = 引擎的 **72**，维度分仍 = 引擎值
- 对外 `inferred` 节点不携带任何与引擎冲突的分数数字
- 审计记录标注该次纠正（记录被丢弃的 LLM 原始数字 + 引擎权威值）

### TC-AI-008 [P0] AirGap 模式 0 出网
**类型**：Security · **Sprint**：v0.11.x · **DoD**：8
**步骤**：
1. 默认配置（Ollama 本地）下执行完整 analyze
2. 全程网络抓包
**预期**：
- 无任何外部出站连接（评分 + 解释 + 嵌入 + 向量检索全本地）

### TC-AI-009 [P1] SaaS provider opt-in 经 sanitize
**类型**：Security · **Sprint**：v0.11.x · **DoD**：9
**步骤**：
1. Settings 显式启用 DeepSeek/Claude provider，执行 analyze
**预期**：
- 出网载荷经单一 `sanitize.go` 脱敏
- 生成 SanitizeReport 可审计（对应 SECURITY.md §6 / ADR-033 §6）

### TC-AI-010 [P1] A/B 双面知识库物理隔离
**类型**：Functional · **Sprint**：v0.11.x · **DoD**：10
**步骤**：
1. 导入一条 B 面（客户本地）规范条目
2. 执行 analyze 并检查检索来源
**预期**：
- A 面（出厂）与 B 面（本地）位于独立 collection
- 检索结果来源可标，B 面**绝不**污染/混入 A 面

### TC-AI-011 [P0] 应用建议只预填不写（非自治）
**类型**：Functional · **Sprint**：v0.11.x · **DoD**：11
**步骤**：
1. 对某建议点"应用建议"
2. 监控集群资源是否被修改
**预期**：
- 仅跳转 Policy Wizard 并预填参数
- **集群任何资源 0 写入**，需客户确认保存才生效

### TC-AI-012 [P2] 建议可追溯到证据
**类型**：Functional · **Sprint**：v0.11.x · **DoD**：12
**步骤**：
1. 展开任一建议的"为什么"
**预期**：
- 可追溯到 `fact` 节点（采集事实）+ KB 知识条目 ID + 推导链 + 置信度

### TC-AI-013 [P2] 业务梳理只画 K8s 可推导依赖
**类型**：Functional · **Sprint**：v0.11.x · **DoD**：13
**步骤**：
1. 对含 Service/Ingress/PVC/ConfigMap 引用的 ns 看业务梳理图
**预期**：
- 只画客观可推导依赖（Svc→Endpoints→Pod、Ingress→Svc、PVC→Pod 等）
- 不臆测业务语义依赖；无法确认的标 `unable_to_confirm`，不画实线

### TC-AI-014 [P2] 端到端性能与降级
**类型**：Performance · **Sprint**：v0.11.x · **DoD**：6 度量
**步骤**：
1. 中等集群本地 Ollama 下对单应用 analyze（同步）
2. 模拟 LLM 不可用
**预期**：
- 端到端 ≤ 60s
- LLM 不可用时仍返回规则引擎分数（仅缺自然语言解释），不崩溃

### TC-AI-015 [P2] Call Home opt-in 与单向出站（PRD-012）
**类型**：Security · **Sprint**：v0.12.x · **DoD**：PRD-012
**步骤**：
1. 默认状态检查 Call Home 开关
2. 显式 opt-in 后触发，抓包验证流向
**预期**：
- 默认**关闭**，未 opt-in 不外发任何数据
- 发送前生成 SanitizeReport 可预览
- 仅单向出站，**无入站监听**；全程不修改客户集群
> 注：PRD-012 Blocked（待 Mars 提供 Case API 规格），自动建 Case 回执用例待规格补全后细化。

### TC-AI-017 [P0] 备份成功率滑动窗口公式 + Tier1 连续失败>3 次归 0
**类型**：Unit (参数化) · **Sprint**：v0.11.x · **DoD**：2
> §4.2 公式①：成功率 = 最近 14 天 / 最近 30 次快照 `success/total × 5`（确定性，窗口以 `snapshot_at` 为基准，非 `time.Now()`）。惩罚：**Tier 1 核心资产连续失败 > 3 次 → 该项直接计 0**。
**步骤**：
1. 参数化喂入备份历史窗口：
   - a) 30 次全 success → `30/30×5 = 5`
   - b) 最近 30 次中 15 success → `15/30×5 = 2.5`
   - c) 0 success → `0`
   - d) Tier 1 应用，末尾**连续 4 次失败**（>3）→ 该项强制 **0**（即使整体成功率仍较高）
   - e) Tier 1 应用，末尾连续 **3 次**失败（=3，未超阈值）→ 仍按公式 `success/total×5`，**不**归 0（边界）
   - f) 非 Tier 1 应用连续 4 次失败 → 仅按公式算，**不**触发归 0 惩罚（惩罚仅对 Tier 1）
2. 窗口边界：插入一条 14 天外/第 31 次的旧记录 → 不计入窗口
**预期**：
- a–c 子项得分精确 = 公式值；d 强制 0 并标记 `penalty: "tier1_consecutive_fail_gt3"`
- e/f 不触发归 0（连续失败=3 与非 Tier1 是边界反例）
- 窗口只取最近 14 天/30 次，超窗记录不影响结果；同窗口两次计算结果完全一致（确定性）

### TC-AI-018 [P0] WORM 不可变存储断言（COMPLIANCE=10 / Governance=6 / 0）
**类型**：Unit (参数化) · **Sprint**：v0.11.x · **DoD**：2
> §4.2 公式②：断言 `ObjectLockEnabled==Enabled` 且 `Mode==COMPLIANCE` 且 `RetainUntilDate > now+30d` 才给满分 10；Governance 模式或仅 IAM 给 6；可覆盖删除给 0。`now` 用采集快照戳 `snapshot_at`，非 `time.Now()`（可复现）。
**步骤**：参数化喂入存储 `GetObjectLockConfiguration` 采集结果：
1. a) `Enabled` + `Mode==COMPLIANCE` + `RetainUntilDate = snapshot_at+60d`（>30d）→ **10**
2. b) `Enabled` + `Mode==COMPLIANCE` + `RetainUntilDate = snapshot_at+10d`（<30d）→ **不**给满分（降档，断言三条件全满足才 10）
3. c) `Enabled` + `Mode==GOVERNANCE` → **6**（即使保留期足够）
4. d) 仅 IAM 策略保护、无 Object Lock → **6**
5. e) `ObjectLockEnabled==Disabled`（可覆盖删除）→ **0**
**预期**：
- 子项得分精确命中 10 / 6 / 0；**Governance 模式只给 6、COMPLIANCE 且保留期>30d 才 10**
- b 档（COMPLIANCE 但保留期不足 30d）不得给 10，证明三条件是合取
- 用快照戳计算保留期 → 同输入两次结果一致

### TC-AI-019 [P0] 缺失字段走 unable_to_confirm 扣分路径（不得用 0 冒充"已确认无"）
**类型**：Unit + Functional · **Sprint**：v0.11.x · **DoD**：1, 2
> 铁律③：缺失字段用 status 枚举 `confirmed/optional/missing/unable_to_confirm/source_conflict` 显式建模。探测不到的能力**不得记 0 冒充"已确认无"**，须走"无法确认"扣分路径并在解释中说明。
**步骤**：
1. 构造一个探测不到 `VolumeSnapshotClass` 的 ns → "是否支持 snapshot" `fact = {value:null, status:unable_to_confirm, source:"k8s", evidence_refs:[]}`
2. 调 `Score()` 与 LLM 解释
**预期**：
- 相关 `fact` 节点 `status == unable_to_confirm`，`value == null`，**不是** `false`/`0`
- 评分按"无法确认快照能力"扣分路径处理（与"已确认无快照=0"路径**可区分**：用例对比两种 fixture，断言两者命中的扣分项 ID / 解释文案不同）
- `inferred` 解释里**显式说明**"无法确认快照能力"而非默认当作没有
- `source_conflict`（两来源冲突）与 `missing`（应有却没采到）也各有 fixture，断言不被坍缩成 0

---

## 附录 A：用例追溯矩阵

随各 Sprint 引入的核心 TC：

| Sprint | 引入的 P0/P1 TC |
|---|---|
| v0.5 MVP | TC-RP-001/002, TC-STG-001/002, TC-ENV-001 |
| v0.6 CSI | TC-RP-001 (CSI 模式), TC-STG-003 |
| v0.7.2 fingerprinting | TC-RP-007 |
| v0.7.5 Advisor | TC-ADV-001/002 |
| v0.7.6 i18n + dark | TC-UX-001/002/003 |
| v0.7.7 Policies | TC-POL-001/002/003/004 |
| v0.7.9 cascade delete | TC-RP-004, TC-REG-002 |
| v0.7.10 Restore Drawer | TC-RST-001/002/004/005/006 |
| v0.7.11 Detail errors | TC-RST-008, TC-RP-003 |
| v0.7.12 Pre-flight | TC-RST-002 |
| v0.7.13 routing | TC-RP-006, TC-APP-003 |
| v0.8.0 Activity | TC-ACT-001~008 |
| v0.8.2 Transform Sets | TC-TS-001~006, TC-RST-010 |
| v0.8.3 fixes | TC-REG-004/005, TC-ACT-005, TC-RST-007, TC-TS-008 |
| v0.8.4 patch types | TC-REG-006, TC-RST-003 |
| v0.8.5 OIDC | TC-AUTH-001~008, TC-REG-007/008, TC-APP-001/002 |
| v0.9.1.13 Import Policy | TC-IMP-001/002/003/004 |
| v0.11.x AI Backup Advisor MVP | TC-AI-002/003/004/005/007/008/011/016/017/018/019（P0）, TC-AI-001/006/009/010（P1）；评分矩阵对齐 PRD-011 §4.2 4 维（D-WAIT-002 / ADR-043 v1.0.0） |
| v0.12.x Call Home | TC-AI-015 |

---

## 附录 B：自动化优先级建议

将测试用例自动化时建议的优先级（节省人工回归时间）：

| 优先级 | TC 类别 | 工具建议 |
|---|---|---|
| 1 | 冒烟测试（P0）| Playwright + 真集群 + 真 Velero |
| 2 | 回归测试（TC-REG-*）| 同上 |
| 3 | 后端 API 单元（部分）| Go test + envtest |
| 4 | 前端组件 | Vitest + Vue Test Utils |
| 5 | 性能基准 | k6 + Prometheus |

---

*本文档随每次发版更新。新增功能必须同步新增对应 TC，否则不能合并 PR。*
