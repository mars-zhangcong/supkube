apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: "2026-05-23T07:21:10Z"
  generation: 1
  name: clusters.dist.kio.kasten.io
  resourceVersion: "274216"
  uid: ea6d6de1-82ad-4524-b986-3ebdb8b578bb
spec:
  conversion:
    strategy: None
  group: dist.kio.kasten.io
  names:
    kind: Cluster
    listKind: ClusterList
    plural: clusters
    singular: cluster
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Cluster describes a connection to a cluster.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterSpec describes the details of the cluster.
            properties:
              apiserver:
                description: APIServer describes the API server endpoint.
                properties:
                  insecureTLS:
                    description: InsecureTLS disables TLS validation.
                    type: boolean
                  url:
                    description: URL is the address of the endpoint.
                    type: string
                required:
                - url
                type: object
              k10:
                description: K10 describes the K10 instance in the cluster.
                properties:
                  ingress:
                    description: Ingress describes the K10 ingress endpoint.
                    properties:
                      insecureTLS:
                        description: InsecureTLS disables TLS validation.
                        type: boolean
                      url:
                        description: URL is the address of the endpoint.
                        type: string
                    required:
                    - url
                    type: object
                  namespace:
                    description: Namespace is the namespace within which K10 is hosted.
                    type: string
                  releaseName:
                    description: ReleaseName is the Helm release name used to install
                      K10.
                    type: string
                type: object
              primary:
                description: Primary defines whether the cluster is a primary or secondary.
                type: boolean
              secret:
                description: |-
                  Secret references a secret containing sensitive cluster connection
                  details.

                  This is only used with secondary clusters.
                type: string
            type: object
          status:
            description: ClusterStatus describes the last observed state of the cluster.
            properties:
              capabilities:
                description: Capabilities lists the cluster's reported capabilities.
                items:
                  type: string
                type: array
              conditions:
                description: |-
                  Represents the observations of a cluster's current state.
                  Known .status.conditions.type are: "Connected"
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              id:
                description: ID is the cluster's last observed identifier.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: Cluster
    listKind: ClusterList
    plural: clusters
    singular: cluster
  conditions:
  - lastTransitionTime: "2026-05-23T07:21:10Z"
    message: no conflicts found
    reason: NoConflicts
    status: "True"
    type: NamesAccepted
  - lastTransitionTime: "2026-05-23T07:21:10Z"
    message: the initial names have been accepted
    reason: InitialNamesAccepted
    status: "True"
    type: Established
  storedVersions:
  - v1alpha1
