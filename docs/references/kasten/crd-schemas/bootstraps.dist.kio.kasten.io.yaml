apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: "2026-05-23T07:21:09Z"
  generation: 1
  name: bootstraps.dist.kio.kasten.io
  resourceVersion: "274213"
  uid: fe81cf67-a27d-41bc-a302-992b4f281a53
spec:
  conversion:
    strategy: None
  group: dist.kio.kasten.io
  names:
    kind: Bootstrap
    listKind: BootstrapList
    plural: bootstraps
    singular: bootstrap
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .metadata.annotations.dist\.kio\.kasten\.io\/expires-at
      name: ExpiresAt
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Bootstrap defines a set of cluster bootstrap operations.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BootstrapSpec defines the bootstrap operations to complete.
            properties:
              clusters:
                additionalProperties:
                  description: ClusterBootstrapSpec defines the bootstrap operation
                    for a single cluster.
                  properties:
                    apiserver:
                      description: |-
                        APIServer is the configuration for the cluster's APIServer endpoint.

                        Typically this can be omitted as this information is pulled from the
                        kubeconfig.
                      properties:
                        insecureTLS:
                          description: InsecureTLS disables TLS validation.
                          type: boolean
                        url:
                          description: URL is the address of the endpoint.
                          type: string
                      required:
                      - url
                      type: object
                    context:
                      description: |-
                        Context is the name of the kubeconfig context to use.

                        The secret specified by the bootstrap spec contains the kubeconfig.
                      type: string
                    generateName:
                      description: |-
                        GenerateName is the generateName prefix for the resulting cluster.

                        It used to generate a name for the cluster.
                      type: string
                    k10:
                      description: K10 is the configuration for the cluster's K10
                        endpoint.
                      properties:
                        ingress:
                          description: Ingress describes the K10 ingress endpoint.
                          properties:
                            insecureTLS:
                              description: InsecureTLS disables TLS validation.
                              type: boolean
                            url:
                              description: URL is the address of the endpoint.
                              type: string
                          required:
                          - url
                          type: object
                        namespace:
                          description: Namespace is the namespace within which K10
                            is hosted.
                          type: string
                        releaseName:
                          description: ReleaseName is the Helm release name under
                            which K10 is installed.
                          type: string
                      required:
                      - namespace
                      - releaseName
                      type: object
                    labels:
                      additionalProperties:
                        type: string
                      description: Labels is the set of labels to apply to the resulting
                        cluster.
                      type: object
                    name:
                      description: |-
                        Name is the name of the resulting cluster.

                        If the name and generateName fields are not specified,
                        then the higher-level key for the cluster bootstrap spec will be used.
                      type: string
                    primary:
                      description: Primary defines whether the cluster will be a primary
                        or secondary.
                      type: boolean
                    status:
                      description: Status contains the status properties (like Id)
                        of the cluster.
                      properties:
                        capabilities:
                          description: Capabilities lists the cluster's reported capabilities.
                          items:
                            type: string
                          type: array
                        id:
                          description: ID is the cluster's ID.
                          type: string
                      type: object
                  type: object
                description: Clusters is the set of cluster bootstrap operations to
                  be completed.
                type: object
              secret:
                description: |-
                  Secret references a secret that contains sensitive bootstrapping
                  information.

                  The necessary kubeconfig content is provided by the referenced secret.
                  This is only required for secondary bootstrap operations.
                type: string
            type: object
          status:
            description: BootstrapStatus defines the observed state of Bootstrap.
            properties:
              clusterStatuses:
                additionalProperties:
                  description: ClusterBootstrapStatus defines the bootstrap operation
                    for a single cluster.
                  properties:
                    conditions:
                      description: Represents the observations of the cluster's current
                        bootstrapping state.
                      items:
                        description: Condition contains details for one aspect of
                          the current state of this API Resource.
                        properties:
                          lastTransitionTime:
                            description: |-
                              lastTransitionTime is the last time the condition transitioned from one status to another.
                              This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                            format: date-time
                            type: string
                          message:
                            description: |-
                              message is a human readable message indicating details about the transition.
                              This may be an empty string.
                            maxLength: 32768
                            type: string
                          observedGeneration:
                            description: |-
                              observedGeneration represents the .metadata.generation that the condition was set based upon.
                              For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                              with respect to the current state of the instance.
                            format: int64
                            minimum: 0
                            type: integer
                          reason:
                            description: |-
                              reason contains a programmatic identifier indicating the reason for the condition's last transition.
                              Producers of specific condition types may define expected values and meanings for this field,
                              and whether the values are considered a guaranteed API.
                              The value should be a CamelCase string.
                              This field may not be empty.
                            maxLength: 1024
                            minLength: 1
                            pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                            type: string
                          status:
                            description: status of the condition, one of True, False,
                              Unknown.
                            enum:
                            - "True"
                            - "False"
                            - Unknown
                            type: string
                          type:
                            description: type of condition in CamelCase or in foo.example.com/CamelCase.
                            maxLength: 316
                            pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                            type: string
                        required:
                        - lastTransitionTime
                        - message
                        - reason
                        - status
                        - type
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - type
                      x-kubernetes-list-type: map
                    name:
                      description: |-
                        Name is the name of the resulting cluster.
                        If generateName was used, this will represent the resulting generated name.
                        If name was provided, it will be same as spec.clusters.name.
                      type: string
                  type: object
                description: Represents the current state for each cluster spec.
                type: object
              conditions:
                description: Represents the observations of a bootstrap's current
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: Bootstrap
    listKind: BootstrapList
    plural: bootstraps
    singular: bootstrap
  conditions:
  - lastTransitionTime: "2026-05-23T07:21:09Z"
    message: no conflicts found
    reason: NoConflicts
    status: "True"
    type: NamesAccepted
  - lastTransitionTime: "2026-05-23T07:21:09Z"
    message: the initial names have been accepted
    reason: InitialNamesAccepted
    status: "True"
    type: Established
  storedVersions:
  - v1alpha1
