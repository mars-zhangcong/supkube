# SupKube 术语表（Glossary · 单一来源）

> **用途**：本文件是 SupKube 全项目术语的**唯一权威定义来源**。PRD / ADR / 测试用例 / UI 文案 / API 字段命名，凡涉及下列术语，**一律以本表为准**，不得各写各的。
> **背景**：评审历史中反复出现的 finding（F1/F3 Posture 三套口径、E4 L1-L5 vs Snapshot/Export、P5 Posture vs Resilience Score、ADR-037 引入 fact/observation/inferred）本质都是**术语撞车**。本表存在的目的就是把"复发的 finding"变成"一次性修复"。
> **维护约定**：(1) 任何评审若发现新的术语歧义/撞车 → 必须回写本表（而不是只在评审报告里解释一次）；(2) 新增/修改术语走与 PRD 相同的轻量评审；(3) 改动记入文末《变更记录》。
> **关联**：[架构设计.md](架构设计.md)（ADR 权威）· [PRD.md](PRD.md) · [测试用例.md](测试用例.md) · [SECURITY.md](SECURITY.md)

---

## 0. 阅读指引：最容易撞车的 3 组术语（先看这里）

| 风险组 | 一句话辨析 |
|---|---|
| **Resilience Score vs 3-2-1-1-0 Posture** | 两个**独立**指标，**不是同义**：前者是**单应用**韧性分，后者是**集群级**层覆盖分。它们**共享数据采集层但口径独立**（评审 P5 收口结论）。详见 §3。 |
| **Layer 1-5（韧性模型）vs L1/L2（Policy 动作）** | "Layer 1~5"是 ADR-031 的**5 层数据韧性模型**；Policy 页里的"L1/L2"是**备份动作语义**（L1=只快照、L2=快照+导出）。**数字相同但所指不同**，写文档时必须带上下文。详见 §2。 |
| **Snapshot / Export / Copy** | Kasten 动作模型三个**不同动作**：Snapshot=本地卷快照、Export=导出到对象存储 BSL、Copy=BSL→第二 BSL 的副本复制。不可混用。详见 §2。 |

---

## 1. 产品与组件（Product & Components）

| 术语 | 定义 | 备注 / 出处 |
|---|---|---|
| **SupKube** | 构建在 Velero 之上的 Kubernetes 原生数据保护 UI + 智能层，对标 Kasten K10。 | README |
| **Velero** | 底层备份/恢复引擎（CRD 驱动）。SupKube **包装**它，不替代它。 | velero.io |
| **supkube-backend** | Go + Gin REST API，包装 Velero CRD。 | |
| **supkube-frontend** | Vue 3 + Element Plus UI。 | |
| **supkube-helm** | Helm Chart，K8s 部署单元。 | |
| **Advisor Engine** | "一个引擎，两个出口"的共享智能内核：内部 UI + 外部 MCP 共用，0 重复实现。 | ADR-033 |
| **MCP Server "Supkube Skills"** | 把 SupKube 能力以**意图级**（非原子级）暴露给客户 AI Agent 的开源服务器（Apache-2.0），走 Streamable HTTP。 | PRD-004 / ADR-034 |
| **OpenClaw（小龙虾）** | **客户侧**的 LLM Agent 框架/平台（类比 LangChain/Dify/Coze），由**客户自主部署运维**。**不是 SupKube 同公司产品，不是 SupKube 组件**。SupKube 仅通过 MCP Server 让它（及任意 MCP Client）调用能力。 | D-15 / PRD-003 |
| **Collector（采集器）** | ADR-037 中执行 CollectionContract 的只读采集组件，随后端 Pod 运行。三档连接形态共用同一个 Collector。 | ADR-037 |
| **Call Home / Auto-Support** | 半连接形态：经客户显式 opt-in + 脱敏后，把诊断快照单向回传售后系统自动开 Case。 | PRD-012 |

---

## 2. 数据韧性模型（5 层 / 动作 / 路径）— 撞车高发区

### 2.1 5 层数据韧性模型（ADR-031）

> "Layer N" 一律指本模型。**这是 SupKube 的核心叙事，对标 Kasten/Veeam 级定位。**

| 层 | 名称 | 含义 |
|---|---|---|
| **Layer 1** | 本地快照（Local Snapshot） | 集群内 CSI VolumeSnapshot，非 Velero 托管，最快恢复点。 |
| **Layer 2** | 备份（Backup / Snapshot + Export） | 快照 + 导出到对象存储 BSL（云或本地 MinIO）。 |
| **Layer 3** | 异地/第二介质 | 满足 3-2-1 的"异地"或"第二介质"份。 |
| **Layer 4** | 备份副本（Backup Copy） | BSL → **第二朵云** 的副本复制（3-2-1-1 的额外 "1"）。 |
| **Layer 5** | 虚拟实验室 / DR 演练（Virtual Lab / DR Drill） | 隔离环境验证可恢复性（3-2-1-1-0 的 "0 误差验证"）。 |

> **3-2-1-1-0** = 3 份数据 / 2 种介质 / 1 份异地 / 1 份不可变或离线 / 0 验证误差。

### 2.2 Policy 动作语义中的 "L1 / L2"（与 §2.1 不同！）

> Policy/Wizard 上下文里的 "L1/L2" 指**备份动作组合**，不是韧性模型的层。

| 记号 | 含义 | Velero spec |
|---|---|---|
| **L1（Snapshot only）** | 只做本地卷快照，不导出 | `snapshotVolumes:true, snapshotMoveData:false` |
| **L2（Snapshot + Export）** | 快照 + 导出到 BSL | `snapshotMoveData:true`（Kopia 上传） |

> ⚠ **写文档纪律**：单独写 "L1/L2" 容易和 Layer 1/2 混。涉及 Policy 动作时写全 **"Snapshot only (L1)" / "Snapshot+Export (L2)"**；涉及韧性模型时写全 **"Layer 1 / Layer 2"**。

### 2.3 Kasten 动作模型（Snapshot / Export / Copy）

| 动作 | 含义 |
|---|---|
| **Snapshot** | 本地卷快照（对应 Layer 1）。 |
| **Export** | 把快照数据导出到对象存储 BSL（对应 Layer 2 的导出半）。 |
| **Copy / Backup Copy** | 把一个 BSL 的备份复制到**第二个 BSL**（对应 Layer 4）。 |
| **Import** | 在目标集群把外部 BSL 的备份**登记**为可见还原点（不产生新数据）。 | 

### 2.4 数据路径（Data Path）

| 值 | 含义 |
|---|---|
| **csi-snapshot** | CSI 卷快照路径。 |
| **data-mover** | CSI 快照 + Kopia 上传到对象存储。 |
| **fs-backup** | 文件系统级备份（Kopia，不拍快照）。 |
| **metadata-only** | 仅备份 K8s 对象元数据，无卷数据。 |

---

## 3. 评分指标（Scores）— 撞车高发区

> **铁律（评审 P5 收口）**：下列是**两个独立指标，共享数据采集层但口径不同**。任何"层数 ×20 当安全百分比"的简化都是误导（F1）。

| 指标 | 范围 | 粒度 | 维度 | 来源 PRD | 计算方式 |
|---|---|---|---|---|---|
| **Resilience Score（应用韧性分）** | 0-100 | 单应用 / 单 ns | 5 业务/架构维度：Business Value / Architecture / Protection / Security / Operation | PRD-003 §3.3 / **PRD-011** | **Go 规则引擎确定性计算**（finding #2，可复现），LLM 只解释不打分 |
| **3-2-1-1-0 Posture（集群 Posture 分）** | 0-100 | 集群级 | 5 层覆盖度（Layer 1~5，各 20 分） | PRD-007 §4.8 | 层覆盖度聚合 |

**辨析口诀**：
- 看**单个应用**够不够安全 → **Resilience Score**。
- 看**整个集群** 3-2-1-1-0 哪几层 ✅/⚠/✗ → **Posture**。
- Posture 卡 hover 显示的是"该集群 N 个应用韧性分的**统计分布**"（avg/p50/p10），**不是**两分数相等。

### 3.1 风险等级 / 安全级别（Resilience Score → 安全级别，**收口为一套权威**）

> **权威收口（Mars 2026-06-03 拍板）**：本节是**per-应用 Resilience Score（§3 上表）的输出分档**，**不是** 3-2-1-1-0 Posture（集群级），也不是其它分。两套标签历史并存（4 档中文安全级别 vs 旧 5 档英文枚举 SAFE/LOW/MEDIUM/HIGH/CRITICAL）造成术语漂移，现统一为：
>
> - **对外/对客户展示一律用 4 档中文安全级别**（权威，分数带 90/75/60，源 PRD-011 §4.2 落地公式 3）。
> - **旧 5 档英文枚举仅作内部 enum**（代码字段值、API 内部枚举），**不得对外展示**；展示前必须按下方映射折叠到 4 档。

**权威 4 档安全级别（对客户展示，源 = PRD-011 §4.2）**：

| 分数带 | 安全级别（权威标签） | 含义 |
|---|---|---|
| 90–100 | **极高韧性级** | 对标 NIST 勒索防护，分钟级拉起。 |
| 75–89 | **合规风险低** | 满足 ISO 27001 基本审计，多厂商瘫痪场景有短板。 |
| 60–74 | **脆弱级** | 无不可变防护，易被勒索连同生产加密。 |
| < 60 | **高危级** | 伪备份，随时面临永久丢失。 |

> 另有 PRD-011 §4.2 两条**校准硬阈值**（从未备份 / 最近一次备份失败 → 强制下调到 30 + 落"高危级"），分档结果以校准后为准。

**4 档 ↔ 旧 5 枚举映射（折叠规则）**：

| 权威 4 档（对外展示） | 分数带 | 旧 5 枚举（内部 enum） |
|---|---|---|
| 极高韧性级 | 90–100 | `SAFE` |
| 合规风险低 | 75–89 | `LOW` |
| 脆弱级 | 60–74 | `MEDIUM` |
| 高危级 | < 60 | `HIGH` + `CRITICAL`（**合并**） |

> **映射理由**：4 档在 60 以下不再细分，而旧枚举把 60 以下劈成 HIGH / CRITICAL 两档——故 `HIGH` 与 `CRITICAL` 一并折叠进**高危级**。其余三档与旧 `SAFE/LOW/MEDIUM` 一一对应，仅阈值以权威 4 档（90/75/60）为准（旧枚举曾用过的 ≥70/≥40 阈值作废）。若代码里仍保留 5 枚举字段，**只在内部流转，渲染到 UI / 报告 / 对外文案前必须先折叠成 4 档**。

**各处该用哪套**：

| 引用方 | 用哪套 | 说明 |
|---|---|---|
| **PRD-011**（Resilience Score 评分 / UI 展示 / 报告） | **4 档安全级别**（权威） | 直接用极高韧性级 / 合规风险低 / 脆弱级 / 高危级。 |
| **PRD-012**（Call Home 触发"高风险"） | **4 档**（脆弱级 / 高危级） | 触发"高风险态势"应表述为"**安全级别 ≤ 脆弱级**"（即脆弱级或高危级），**不再写"Score ≤ HIGH"**。 |
| 代码内部 enum / API 内部字段 | 旧 5 枚举（如已存在）允许保留 | 仅内部，不对外；对外渲染前折叠到 4 档。 |

### 3.2 置信度（Confidence，finding #3）

所有 AI 推断的置信度只用 **`high / medium / low` 三档**，**禁止**输出"87%"这类伪精确百分比。信任靠引用证据（KB 条目数 + 检测事实数 + 规则引擎佐证），不靠数字。

---

## 4. 备份/恢复领域对象（Backup/Restore Domain）

| 术语 | 定义 |
|---|---|
| **BSL（BackupStorageLocation）** | Velero 备份存储位置（S3/MinIO/Azure/GCS）。分 **Cloud BSL** 与 **Local BSL**（本地 MinIO）。 |
| **VSL（VolumeSnapshotLocation）** | Velero 卷快照位置。 |
| **RP（Restore Point / 还原点）** | 一个可用于恢复的备份点。分 **Native RP**（本集群产生）与 **Imported RP**（从外部 BSL 导入登记）。 |
| **Backup（CR）** | Velero Backup 自定义资源。 |
| **Restore（CR）** | Velero Restore 自定义资源。 |
| **Schedule** | Velero 定时备份资源；一个双 Schedule（snapshot 半 + export 半）实现 L2。 |
| **Policy** | SupKube 对一组 Schedule 的高层封装（Kasten 风格）。分 **Snapshot Policy** 与 **Import Policy**（PRD-009 v2）。 |
| **Transform** | 还原时的资源改写规则（如 change-storage-class）。**一等公民**，两层模型。 |
| **TransformSet** | Transform 的引用容器（多个 Transform 组合）。 |
| **Object Lock** | 对象存储不可变锁（WORM），满足 3-2-1-1 的"不可变"份。 |
| **Frankenstein 检测** | Preflight 检测"拼接出的不一致还原配置"。 |
| **CSI 一键适配** | 自动建 SC + VSC + binding mode + 显示用到的 TransformSet（杀手级特性，task #104）。 |

---

## 5. 智能层数据模型（Canonical DSL，ADR-037）

> 源自 SupEye《数据获取器》四段模型。采集到的一切都封装为下列节点，**缺失数据必须显式建模，禁止用 0/空冒充"已确认无"**。

| 节点 | 结构 | 含义 |
|---|---|---|
| **fact（事实）** | `{value, status, source, evidence_refs}` | 客观采集到的事实。 |
| **observation（观察）** | 同上 | 对事实的直接观察/读数。 |
| **inferred（推断）** | `{conclusion, basis, confidence, evidence_refs}` | 推理产出（LLM 解释即此类），confidence 三档。 |
| **evidence（证据）** | 引用句柄 | 指回原始事实/KB 条目，支撑可追溯。 |

### 5.1 字段状态枚举（status）

| 状态 | 含义 |
|---|---|
| `confirmed` | 已确认 |
| `optional` | 可选项，未提供 |
| `missing` | 应有但采集不到 |
| `unable_to_confirm` | 无法确认（如探测不到 VolumeSnapshotClass） |
| `source_conflict` | 多源冲突 |

### 5.2 知识库双面（RAG）

| 平面 | 含义 |
|---|---|
| **A 面（出厂知识）** | 我们维护，knowledge-as-code，git PR、版本化、登记 KB-LEDGER。 |
| **B 面（客户本地覆盖）** | 客户导入的本地规范，**物理隔离**，来源可标，**绝不混入 A 面**。 |

---

## 6. 连接形态（Connectivity，ADR-037）

| 形态 | 出网 | 含义 |
|---|---|---|
| **SaaS 全连** | 双向 | 完全连接，最易调试。 |
| **Call Home 半连** | 单向出站 | 只出不入，opt-in + 脱敏，自动开 Case（PRD-012）。 |
| **AirGap 零出网** | 0 | 完全离线，本地 Ollama + 本地规则引擎 + 本地知识库。 |

> 三档**共用一个 Collector + 一套 DSL**，只换 egress 传输。默认 LLM = 本地 Ollama（0 出网）；SaaS Provider 需显式 opt-in 且经单一 `sanitize.go` 脱敏，生成 SanitizeReport 审计（SECURITY.md §6）。

---

## 7. 流程与治理（Process & Governance）

| 术语 | 定义 | 出处 |
|---|---|---|
| **ADR** | 架构决策记录。**编号唯一来源 = 架构设计.md 顶部 ADR-LEDGER**（防撞号）。 | 架构设计.md |
| **PRD 状态机** | 草稿→排队评审→评审中→{改正中｜驳回｜已评审}→研发中→待验收→Shipped→归档。 | PRD.md |
| **Rule A** | PRD 先于代码（例外：单文件 bug / UX 微调 / 文档 / dev 脚本）。 | ENGINEERING.md / D-18 |
| **Rule B** | 多任务并行开多 Agent。 | D-18 |
| **Rule C** | 并行 Agent 写共享契约必须有"单一来源"（防 X1 教训）。 | D-18 |
| **Verify-before-ship** | build pass ≠ deploy 完成；必须 `curl /status` 看 buildStamp + 走完整 user journey 才算 ship。 | D-17 |
| **非自治 / 推荐型** | 所有 AI / Call Home 只给**建议**，客户确认后自己执行；软件绝不自动改集群。 | ADR-033 §6 |
| **回归强制律** | 修过的 bug 必须有回归用例。 | 测试用例.md |

### 7.1 优先级模型（统一口径，消除 dashboard 矛盾）

> **本项目 P0-P3 = 严重度/紧迫度**，并映射到艾森豪威尔四象限。Dashboard Matrix 与 KPI 必须用同一口径。

| 级别 | 象限 | 含义 | 处理 |
|---|---|---|---|
| **P0** | 重要且紧急 | 阻塞客户使用 | 立刻 |
| **P1** | 重要不紧急 | 严重体验缺陷 | 三月内 |
| **P2** | 紧急不重要 / 架构债务 | 顺手做 | 排期 |
| **P3** | 不紧急不重要 | 触发再做 | backlog |

---

## 8. 缩写速查（Abbreviations）

| 缩写 | 全称 |
|---|---|
| RP | Restore Point（还原点） |
| BSL | BackupStorageLocation |
| VSL | VolumeSnapshotLocation |
| SC | StorageClass |
| VSC | VolumeSnapshotClass / VolumeSnapshotContent（按上下文） |
| CR | Custom Resource |
| DSL | Domain-Specific Language（此处指 Canonical DSL） |
| KB | Knowledge Base |
| RTO / RPO | Recovery Time / Point Objective（见 SLO-RTO-RPO.md） |
| HitL | Human-in-the-Loop |
| MCP | Model Context Protocol |

---

## 变更记录

| 日期 | 操作人 | 变更 |
|---|---|---|
| 2026-06-01 | Claude | 初版。建立全项目术语单一来源，重点收口 3 组高发撞车（Resilience Score vs Posture / Layer 1-5 vs L1-L2 / Snapshot-Export-Copy）+ Canonical DSL（ADR-037）+ 三档连接形态 + 优先级模型统一口径。 |
| 2026-06-03 | Claude | 收口"风险等级标签"双轨（§3.1）：以 Mars 4 档安全级别（极高韧性级/合规风险低/脆弱级/高危级，分数带 90/75/60，源 PRD-011 §4.2）为**对外展示权威**；旧 5 枚举 SAFE/LOW/MEDIUM/HIGH/CRITICAL 降为内部 enum，给出 4↔5 映射（HIGH+CRITICAL 合并入高危级，废弃旧 ≥70/≥40 阈值）+ 各引用方该用哪套。同步把 PRD-012 触发条件"Score ≤ HIGH"对齐为"安全级别 ≤ 脆弱级"。 |
