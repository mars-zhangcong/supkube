apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: "2026-05-23T07:21:11Z"
  generation: 1
  name: policies.config.kio.kasten.io
  resourceVersion: "274299"
  uid: aced932b-5c7c-4d65-bb71-385122e2014b
spec:
  conversion:
    strategy: None
  group: config.kio.kasten.io
  names:
    kind: Policy
    listKind: PolicyList
    plural: policies
    singular: policy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.validation
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Policy is the Schema for the policies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              PolicySpec defines the desired state of Policy
              Name, ID and CreationTime are in Policy.ObjectMeta
            properties:
              actions:
                items:
                  description: PolicyAction has an action to execute in a policy
                  properties:
                    action:
                      description: |-
                        Action is the type of action to be performed for a policy when a task executes
                        Equivalent swagger model: Action
                      type: string
                    backupClusterParameters:
                      description: BackupClusterParameters contains parameters for
                        the backup cluster action
                      properties:
                        filters:
                          description: |-
                            ActionFilters specifies filters that can be applied to resources that an action
                            operates on. All filters are ANDed.
                          properties:
                            excludeClusterResources:
                              description: |-
                                ExcludeCluster specifies which cluster-scope resource to exclude based
                                on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            excludeExtraResources:
                              description: ExcludeExtraResources specifies resources
                                to exclude from those included by IncludeExtraResources.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            excludeResources:
                              description: |-
                                ExcludeResources specifies which resources to exclude from the default resource set
                                based on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeClusterResources:
                              description: |-
                                IncludeCluster specifies which cluster-scope resources to include based
                                on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeExtraResources:
                              description: IncludeExtraResources specifies resources
                                that are not in the default resource set to include.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeResources:
                              description: |-
                                IncludeResources specifies which resources to include from the default resource set
                                based on a ResourceMatcher.
                                Kanister does not contain k8s:openapi-gen annotations, so we must exclude these
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                          type: object
                      type: object
                    backupParameters:
                      description: BackupParameters contains parameters for the backup
                        action
                      properties:
                        actionPodSpec:
                          description: ActionPodSpec reference to ActionPodSpec CR
                            which will be used for pods created during the operation.
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                        expiresAt:
                          format: date-time
                          type: string
                        filters:
                          description: |-
                            ActionFilters specifies filters that can be applied to resources that an action
                            operates on. All filters are ANDed.
                          properties:
                            excludeClusterResources:
                              description: |-
                                ExcludeCluster specifies which cluster-scope resource to exclude based
                                on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            excludeExtraResources:
                              description: ExcludeExtraResources specifies resources
                                to exclude from those included by IncludeExtraResources.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            excludeResources:
                              description: |-
                                ExcludeResources specifies which resources to exclude from the default resource set
                                based on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeClusterResources:
                              description: |-
                                IncludeCluster specifies which cluster-scope resources to include based
                                on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeExtraResources:
                              description: IncludeExtraResources specifies resources
                                that are not in the default resource set to include.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeResources:
                              description: |-
                                IncludeResources specifies which resources to include from the default resource set
                                based on a ResourceMatcher.
                                Kanister does not contain k8s:openapi-gen annotations, so we must exclude these
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                          type: object
                        hooks:
                          description: ActionHooks contains execution hooks to run
                            before and after actions.
                          properties:
                            onFailure:
                              description: ActionHook is a reference to an action
                                within a Blueprint.
                              properties:
                                actionName:
                                  type: string
                                blueprint:
                                  type: string
                                subject:
                                  description: Subject is the k8s resource on which
                                    hook blueprint needs to act on
                                  properties:
                                    name:
                                      type: string
                                    namespace:
                                      type: string
                                    type:
                                      type: string
                                  type: object
                              type: object
                            onSuccess:
                              description: ActionHook is a reference to an action
                                within a Blueprint.
                              properties:
                                actionName:
                                  type: string
                                blueprint:
                                  type: string
                                subject:
                                  description: Subject is the k8s resource on which
                                    hook blueprint needs to act on
                                  properties:
                                    name:
                                      type: string
                                    namespace:
                                      type: string
                                    type:
                                      type: string
                                  type: object
                              type: object
                            preHook:
                              description: ActionHook is a reference to an action
                                within a Blueprint.
                              properties:
                                actionName:
                                  type: string
                                blueprint:
                                  type: string
                                subject:
                                  description: Subject is the k8s resource on which
                                    hook blueprint needs to act on
                                  properties:
                                    name:
                                      type: string
                                    namespace:
                                      type: string
                                    type:
                                      type: string
                                  type: object
                              type: object
                          type: object
                        ignoreExceptions:
                          type: boolean
                        imageRepoProfile:
                          description: |-
                            Reference refers to a kubernetes custom resource object that is in the same
                            API group as the referring object, thus the API version is elided. The object
                            kind is also not included because it is determined by the referring object.
                            When the type of the referenced object can change, then use
                            K8sObjectReference instead
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                        profile:
                          description: |-
                            Reference refers to a kubernetes custom resource object that is in the same
                            API group as the referring object, thus the API version is elided. The object
                            kind is also not included because it is determined by the referring object.
                            When the type of the referenced object can change, then use
                            K8sObjectReference instead
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                      type: object
                    batchRestoreParameters:
                      description: BatchRestoreParameters defines parameters for batch
                        restore operation
                      properties:
                        actionPodSpec:
                          description: ActionPodSpec reference to ActionPodSpec CR
                            which will be used for pods created during the operation.
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                        artifactOverrideProfile:
                          description: |-
                            Reference refers to a kubernetes custom resource object that is in the same
                            API group as the referring object, thus the API version is elided. The object
                            kind is also not included because it is determined by the referring object.
                            When the type of the referenced object can change, then use
                            K8sObjectReference instead
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                        dataOnly:
                          type: boolean
                        filters:
                          description: |-
                            ActionFilters specifies filters that can be applied to resources that an action
                            operates on. All filters are ANDed.
                          properties:
                            excludeClusterResources:
                              description: |-
                                ExcludeCluster specifies which cluster-scope resource to exclude based
                                on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            excludeExtraResources:
                              description: ExcludeExtraResources specifies resources
                                to exclude from those included by IncludeExtraResources.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            excludeResources:
                              description: |-
                                ExcludeResources specifies which resources to exclude from the default resource set
                                based on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeClusterResources:
                              description: |-
                                IncludeCluster specifies which cluster-scope resources to include based
                                on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeExtraResources:
                              description: IncludeExtraResources specifies resources
                                that are not in the default resource set to include.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeResources:
                              description: |-
                                IncludeResources specifies which resources to include from the default resource set
                                based on a ResourceMatcher.
                                Kanister does not contain k8s:openapi-gen annotations, so we must exclude these
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                          type: object
                        hooks:
                          description: ActionHooks contains execution hooks to run
                            before and after actions.
                          properties:
                            onFailure:
                              description: ActionHook is a reference to an action
                                within a Blueprint.
                              properties:
                                actionName:
                                  type: string
                                blueprint:
                                  type: string
                                subject:
                                  description: Subject is the k8s resource on which
                                    hook blueprint needs to act on
                                  properties:
                                    name:
                                      type: string
                                    namespace:
                                      type: string
                                    type:
                                      type: string
                                  type: object
                              type: object
                            onSuccess:
                              description: ActionHook is a reference to an action
                                within a Blueprint.
                              properties:
                                actionName:
                                  type: string
                                blueprint:
                                  type: string
                                subject:
                                  description: Subject is the k8s resource on which
                                    hook blueprint needs to act on
                                  properties:
                                    name:
                                      type: string
                                    namespace:
                                      type: string
                                    type:
                                      type: string
                                  type: object
                              type: object
                            preHook:
                              description: ActionHook is a reference to an action
                                within a Blueprint.
                              properties:
                                actionName:
                                  type: string
                                blueprint:
                                  type: string
                                subject:
                                  description: Subject is the k8s resource on which
                                    hook blueprint needs to act on
                                  properties:
                                    name:
                                      type: string
                                    namespace:
                                      type: string
                                    type:
                                      type: string
                                  type: object
                              type: object
                          type: object
                        ignoreExceptions:
                          type: boolean
                        instantRecovery:
                          type: boolean
                        overwriteExisting:
                          type: boolean
                        pointInTime:
                          format: date-time
                          type: string
                        profile:
                          description: |-
                            Reference refers to a kubernetes custom resource object that is in the same
                            API group as the referring object, thus the API version is elided. The object
                            kind is also not included because it is determined by the referring object.
                            When the type of the referenced object can change, then use
                            K8sObjectReference instead
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                        restoreClusterResources:
                          type: boolean
                        restorePointsDiscoveryRange:
                          description: |-
                            DateRange is a pair of dates representing a closed interval.
                            DateRange with nil Start and End is an interval unbounded at both ends (-∞; +∞).
                          properties:
                            end:
                              description: |-
                                End is an endpoint to make the interval right-bounded.
                                Nil makes the interval right-unbounded.
                              format: date-time
                              type: string
                            start:
                              description: |-
                                Start is an endpoint to make the interval left-bounded.
                                Nil makes the interval left-unbounded.
                              format: date-time
                              type: string
                          type: object
                        skipWaitForWorkloadReady:
                          type: boolean
                        subjects:
                          items:
                            description: BatchRestoreSubject is a subject of batch
                              restore operation
                            properties:
                              namespace:
                                type: string
                              restorePointContentName:
                                type: string
                              restorePointName:
                                type: string
                            required:
                            - namespace
                            type: object
                          type: array
                        targetNamespace:
                          description: The namespace into which something should be
                            restored
                          type: string
                        targetNamespacePrefix:
                          type: string
                        targetNamespaceSuffix:
                          type: string
                        targetVsphereStorage:
                          type: string
                        transforms:
                          description: TransformsOrRefs describes all transformations
                            for a suite of resources
                          items:
                            description: TransformOrRef holds transformation for a
                              single resource type or a reference to some transform
                              set.
                            properties:
                              transformSetRef:
                                description: |-
                                  Reference refers to a kubernetes custom resource object that is in the same
                                  API group as the referring object, thus the API version is elided. The object
                                  kind is also not included because it is determined by the referring object.
                                  When the type of the referenced object can change, then use
                                  K8sObjectReference instead
                                properties:
                                  name:
                                    description: Name of the referenced object
                                    type: string
                                  namespace:
                                    description: |-
                                      Namespace where the object is located. If it is empty, it should be
                                      interpreted as the same namespace of the referencing object, and not
                                      `default` by default.
                                    type: string
                                required:
                                - name
                                type: object
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          type: array
                        virtualMachineParameters:
                          description: VirtualMachineParameters defines restore parameters
                            specific to virtual machines.
                          properties:
                            harvester:
                              description: Harvester represents restore parameters
                                specific to virtual machines on the Harvester platform.
                              properties:
                                overrideVolumeImages:
                                  additionalProperties:
                                    description: |-
                                      HarvesterImageOverride specifies a VM image override used during restoring a virtual machine
                                      in Harvester.
                                    properties:
                                      ignoreChecks:
                                        description: |-
                                          IgnoreChecks determines whether to skip VM image checksum validation during
                                          the restore process. If set to true, the image will be used without verifying its integrity.
                                          This is useful when intentionally using a different VM image than the one used in the backup.
                                        type: boolean
                                      name:
                                        description: Name of the VM image used for
                                          override.
                                        type: string
                                      namespace:
                                        description: Namespace where the VM image
                                          is located.
                                        type: string
                                    type: object
                                  description: |-
                                    OverrideVolumeImages maps VM image PVCs to be restored to existing VM image references
                                    (identified by name and namespace). After the restore, all references to VM images
                                    in the selected VM image PVCs will be replaced with the provided new VM image references.
                                  type: object
                              type: object
                            preserveVMMacAddresses:
                              description: |-
                                PreserveVMMacAddresses specifies whether to retain the original MAC addresses
                                on all network interfaces of virtual machines after restore.
                              type: boolean
                          type: object
                        volumeClones:
                          type: boolean
                      required:
                      - subjects
                      type: object
                    exportParameters:
                      description: ExportParameters Migration send policy
                      properties:
                        actionPodSpec:
                          description: ActionPodSpec reference to ActionPodSpec CR
                            which will be used for pods created during the operation.
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                        blockModeProfile:
                          description: |-
                            BlockModeProfile is a reference to a profile that supports block based backup.
                            It should only be used when the infrastructure also supports block based backup.
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                        expiresAt:
                          description: Date of expiration for manual exports
                          format: date-time
                          type: string
                        exportData:
                          description: ExportData references backup portability settings.
                          properties:
                            enabled:
                              description: |-
                                Default backup portability setting.  Applicable to all storage classes
                                unless overridden by settings in Overrides (below).
                              type: boolean
                            exporterStorageClassName:
                              description: |-
                                ExporterStorageClassName is an optional field used to specify the
                                default storage class used to export data for volumes. Applicable to
                                all volumes unless overridden by settings in Overrides (below).
                              type: string
                            overrides:
                              description: |-
                                Overrides is a slice of storage-specific backup portability settings.
                                Setting here overrides the default setting above.
                              items:
                                description: StorageOverride overrides export portability
                                  setting when exporting a volume.
                                properties:
                                  enabled:
                                    description: |-
                                      Enabled, if set to true, calls for the contents of the volume snapshot
                                      to be serialized into a Location. If so, then its contents may be
                                      imported into any storage system.
                                    type: boolean
                                  exporterStorageClassName:
                                    description: |-
                                      ExporterStorageClassName is an optional field used to specify the name
                                      of the storage class used to export data for volumes that were created
                                      with the storage class specified in `StorageClassName`
                                    type: string
                                  storageClassName:
                                    description: |-
                                      StorageClassName is used to match against a volume's storage class. Any
                                      volumes with that storage class will use this StorageOverride.
                                    type: string
                                required:
                                - enabled
                                - storageClassName
                                type: object
                              type: array
                          required:
                          - enabled
                          type: object
                        frequency:
                          description: Frequency indicates valid frequency values
                          type: string
                        hooks:
                          description: Hooks are references to kanister actions executed
                            before or after export.
                          properties:
                            onFailure:
                              description: ActionHook is a reference to an action
                                within a Blueprint.
                              properties:
                                actionName:
                                  type: string
                                blueprint:
                                  type: string
                                subject:
                                  description: Subject is the k8s resource on which
                                    hook blueprint needs to act on
                                  properties:
                                    name:
                                      type: string
                                    namespace:
                                      type: string
                                    type:
                                      type: string
                                  type: object
                              type: object
                            onSuccess:
                              description: ActionHook is a reference to an action
                                within a Blueprint.
                              properties:
                                actionName:
                                  type: string
                                blueprint:
                                  type: string
                                subject:
                                  description: Subject is the k8s resource on which
                                    hook blueprint needs to act on
                                  properties:
                                    name:
                                      type: string
                                    namespace:
                                      type: string
                                    type:
                                      type: string
                                  type: object
                              type: object
                            preHook:
                              description: ActionHook is a reference to an action
                                within a Blueprint.
                              properties:
                                actionName:
                                  type: string
                                blueprint:
                                  type: string
                                subject:
                                  description: Subject is the k8s resource on which
                                    hook blueprint needs to act on
                                  properties:
                                    name:
                                      type: string
                                    namespace:
                                      type: string
                                    type:
                                      type: string
                                  type: object
                              type: object
                          type: object
                        ignoreExceptions:
                          description: |-
                            Ignore exceptions during export runs

                            Deprecated: Not considered for ExportActions but it is used in Empty() check. Not removing to avoid the risk of a repeat of K10-4340. See comment in K10-12992.
                          type: boolean
                        migrationToken:
                          description: Reference to MigrationToken
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                        profile:
                          description: Profile reference
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                        receiveString:
                          description: |-
                            Opaque string that describes where the migration artifacts are located.
                            This is used in the import parameters on the receiving side.
                          type: string
                        volumeSnapshots:
                          description: |-
                            VolumeSnapshots reference provider-specific snapshot and export settings.
                            Present only when supported provider is in use.
                          properties:
                            awsEbs:
                              description: AWS EBS specific volume snapshot settings.
                              properties:
                                destinationAccount:
                                  description: Volume snapshot copy destination account.
                                  type: string
                                regions:
                                  description: List of regions to copy snapshots to.
                                  items:
                                    type: string
                                  type: array
                              type: object
                            azure:
                              description: Azure specific volume snapshot settings.
                              properties:
                                regions:
                                  description: List of regions to copy snapshots to.
                                  items:
                                    type: string
                                  type: array
                                resourceGroup:
                                  description: ResourceGroup of the snapshots destination
                                  type: string
                              type: object
                          type: object
                      type: object
                    importParameters:
                      description: |-
                        ImportParameters Migration send policy
                        Equivalent swagger model: PolicyParametersMigrationImport
                      properties:
                        actionPodSpec:
                          description: ActionPodSpec reference to ActionPodSpec CR
                            which will be used for pods created during the operation.
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                        migrationToken:
                          description: Reference to MigrationToken
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                        profile:
                          description: Profile reference
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                        receiveString:
                          description: Opaque string that describes where to locate
                            the artifacts to be imported.
                          type: string
                      type: object
                    migrateFCDParameters:
                      description: MigrateFCDParameters defines parameters for migrate
                        FCD operation
                      properties:
                        profile:
                          description: |-
                            Reference refers to a kubernetes custom resource object that is in the same
                            API group as the referring object, thus the API version is elided. The object
                            kind is also not included because it is determined by the referring object.
                            When the type of the referenced object can change, then use
                            K8sObjectReference instead
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                        session:
                          type: string
                        targetStorage:
                          type: string
                      type: object
                    reportParameters:
                      description: ReportParameters report parameters
                      properties:
                        statsIntervalDays:
                          format: int32
                          type: integer
                        statsIntervalHours:
                          format: int32
                          type: integer
                      type: object
                    restoreClusterParameters:
                      description: RestoreClusterParameters restore parameters
                      properties:
                        filters:
                          description: |-
                            ActionFilters specifies filters that can be applied to resources that an action
                            operates on. All filters are ANDed.
                          properties:
                            excludeClusterResources:
                              description: |-
                                ExcludeCluster specifies which cluster-scope resource to exclude based
                                on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            excludeExtraResources:
                              description: ExcludeExtraResources specifies resources
                                to exclude from those included by IncludeExtraResources.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            excludeResources:
                              description: |-
                                ExcludeResources specifies which resources to exclude from the default resource set
                                based on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeClusterResources:
                              description: |-
                                IncludeCluster specifies which cluster-scope resources to include based
                                on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeExtraResources:
                              description: IncludeExtraResources specifies resources
                                that are not in the default resource set to include.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeResources:
                              description: |-
                                IncludeResources specifies which resources to include from the default resource set
                                based on a ResourceMatcher.
                                Kanister does not contain k8s:openapi-gen annotations, so we must exclude these
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                          type: object
                        hooks:
                          description: ActionHooks contains execution hooks to run
                            before and after actions.
                          properties:
                            onFailure:
                              description: ActionHook is a reference to an action
                                within a Blueprint.
                              properties:
                                actionName:
                                  type: string
                                blueprint:
                                  type: string
                                subject:
                                  description: Subject is the k8s resource on which
                                    hook blueprint needs to act on
                                  properties:
                                    name:
                                      type: string
                                    namespace:
                                      type: string
                                    type:
                                      type: string
                                  type: object
                              type: object
                            onSuccess:
                              description: ActionHook is a reference to an action
                                within a Blueprint.
                              properties:
                                actionName:
                                  type: string
                                blueprint:
                                  type: string
                                subject:
                                  description: Subject is the k8s resource on which
                                    hook blueprint needs to act on
                                  properties:
                                    name:
                                      type: string
                                    namespace:
                                      type: string
                                    type:
                                      type: string
                                  type: object
                              type: object
                            preHook:
                              description: ActionHook is a reference to an action
                                within a Blueprint.
                              properties:
                                actionName:
                                  type: string
                                blueprint:
                                  type: string
                                subject:
                                  description: Subject is the k8s resource on which
                                    hook blueprint needs to act on
                                  properties:
                                    name:
                                      type: string
                                    namespace:
                                      type: string
                                    type:
                                      type: string
                                  type: object
                              type: object
                          type: object
                        transforms:
                          description: TransformsOrRefs describes all transformations
                            for a suite of resources
                          items:
                            description: TransformOrRef holds transformation for a
                              single resource type or a reference to some transform
                              set.
                            properties:
                              transformSetRef:
                                description: |-
                                  Reference refers to a kubernetes custom resource object that is in the same
                                  API group as the referring object, thus the API version is elided. The object
                                  kind is also not included because it is determined by the referring object.
                                  When the type of the referenced object can change, then use
                                  K8sObjectReference instead
                                properties:
                                  name:
                                    description: Name of the referenced object
                                    type: string
                                  namespace:
                                    description: |-
                                      Namespace where the object is located. If it is empty, it should be
                                      interpreted as the same namespace of the referencing object, and not
                                      `default` by default.
                                    type: string
                                required:
                                - name
                                type: object
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          type: array
                      type: object
                    restoreParameters:
                      description: |-
                        RestoreParameters restore parameters
                        swagger:model RestoreParameters
                      properties:
                        actionPodSpec:
                          description: ActionPodSpec reference to ActionPodSpec CR
                            which will be used for pods created during the operation.
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                        artifactOverrideProfile:
                          description: |-
                            Reference refers to a kubernetes custom resource object that is in the same
                            API group as the referring object, thus the API version is elided. The object
                            kind is also not included because it is determined by the referring object.
                            When the type of the referenced object can change, then use
                            K8sObjectReference instead
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                        dataOnly:
                          type: boolean
                        filters:
                          description: |-
                            ActionFilters specifies filters that can be applied to resources that an action
                            operates on. All filters are ANDed.
                          properties:
                            excludeClusterResources:
                              description: |-
                                ExcludeCluster specifies which cluster-scope resource to exclude based
                                on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            excludeExtraResources:
                              description: ExcludeExtraResources specifies resources
                                to exclude from those included by IncludeExtraResources.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            excludeResources:
                              description: |-
                                ExcludeResources specifies which resources to exclude from the default resource set
                                based on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeClusterResources:
                              description: |-
                                IncludeCluster specifies which cluster-scope resources to include based
                                on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeExtraResources:
                              description: IncludeExtraResources specifies resources
                                that are not in the default resource set to include.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeResources:
                              description: |-
                                IncludeResources specifies which resources to include from the default resource set
                                based on a ResourceMatcher.
                                Kanister does not contain k8s:openapi-gen annotations, so we must exclude these
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                          type: object
                        hooks:
                          description: ActionHooks contains execution hooks to run
                            before and after actions.
                          properties:
                            onFailure:
                              description: ActionHook is a reference to an action
                                within a Blueprint.
                              properties:
                                actionName:
                                  type: string
                                blueprint:
                                  type: string
                                subject:
                                  description: Subject is the k8s resource on which
                                    hook blueprint needs to act on
                                  properties:
                                    name:
                                      type: string
                                    namespace:
                                      type: string
                                    type:
                                      type: string
                                  type: object
                              type: object
                            onSuccess:
                              description: ActionHook is a reference to an action
                                within a Blueprint.
                              properties:
                                actionName:
                                  type: string
                                blueprint:
                                  type: string
                                subject:
                                  description: Subject is the k8s resource on which
                                    hook blueprint needs to act on
                                  properties:
                                    name:
                                      type: string
                                    namespace:
                                      type: string
                                    type:
                                      type: string
                                  type: object
                              type: object
                            preHook:
                              description: ActionHook is a reference to an action
                                within a Blueprint.
                              properties:
                                actionName:
                                  type: string
                                blueprint:
                                  type: string
                                subject:
                                  description: Subject is the k8s resource on which
                                    hook blueprint needs to act on
                                  properties:
                                    name:
                                      type: string
                                    namespace:
                                      type: string
                                    type:
                                      type: string
                                  type: object
                              type: object
                          type: object
                        ignoreExceptions:
                          type: boolean
                        instantRecovery:
                          type: boolean
                        overwriteExisting:
                          type: boolean
                        pointInTime:
                          format: date-time
                          type: string
                        profile:
                          description: |-
                            Reference refers to a kubernetes custom resource object that is in the same
                            API group as the referring object, thus the API version is elided. The object
                            kind is also not included because it is determined by the referring object.
                            When the type of the referenced object can change, then use
                            K8sObjectReference instead
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                        restoreClusterResources:
                          type: boolean
                        skipWaitForWorkloadReady:
                          type: boolean
                        targetNamespace:
                          description: The namespace into which something should be
                            restored
                          type: string
                        targetVsphereStorage:
                          type: string
                        transforms:
                          description: TransformsOrRefs describes all transformations
                            for a suite of resources
                          items:
                            description: TransformOrRef holds transformation for a
                              single resource type or a reference to some transform
                              set.
                            properties:
                              transformSetRef:
                                description: |-
                                  Reference refers to a kubernetes custom resource object that is in the same
                                  API group as the referring object, thus the API version is elided. The object
                                  kind is also not included because it is determined by the referring object.
                                  When the type of the referenced object can change, then use
                                  K8sObjectReference instead
                                properties:
                                  name:
                                    description: Name of the referenced object
                                    type: string
                                  namespace:
                                    description: |-
                                      Namespace where the object is located. If it is empty, it should be
                                      interpreted as the same namespace of the referencing object, and not
                                      `default` by default.
                                    type: string
                                required:
                                - name
                                type: object
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          type: array
                        virtualMachineParameters:
                          description: VirtualMachineParameters defines restore parameters
                            specific to virtual machines.
                          properties:
                            harvester:
                              description: Harvester represents restore parameters
                                specific to virtual machines on the Harvester platform.
                              properties:
                                overrideVolumeImages:
                                  additionalProperties:
                                    description: |-
                                      HarvesterImageOverride specifies a VM image override used during restoring a virtual machine
                                      in Harvester.
                                    properties:
                                      ignoreChecks:
                                        description: |-
                                          IgnoreChecks determines whether to skip VM image checksum validation during
                                          the restore process. If set to true, the image will be used without verifying its integrity.
                                          This is useful when intentionally using a different VM image than the one used in the backup.
                                        type: boolean
                                      name:
                                        description: Name of the VM image used for
                                          override.
                                        type: string
                                      namespace:
                                        description: Namespace where the VM image
                                          is located.
                                        type: string
                                    type: object
                                  description: |-
                                    OverrideVolumeImages maps VM image PVCs to be restored to existing VM image references
                                    (identified by name and namespace). After the restore, all references to VM images
                                    in the selected VM image PVCs will be replaced with the provided new VM image references.
                                  type: object
                              type: object
                            preserveVMMacAddresses:
                              description: |-
                                PreserveVMMacAddresses specifies whether to retain the original MAC addresses
                                on all network interfaces of virtual machines after restore.
                              type: boolean
                          type: object
                        volumeClones:
                          type: boolean
                      type: object
                    retention:
                      description: Retention has the retention schedule for a policy
                      properties:
                        daily:
                          format: int64
                          type: integer
                        hourly:
                          format: int64
                          type: integer
                        monthly:
                          format: int64
                          type: integer
                        table:
                          description: |-
                            TimeTable is a collection of entries for a (retention) schedule
                            Equivalent swagger model: TimeTable
                          items:
                            description: |-
                              TimeTableEntry Entry / row in a time table containing a retention count and a
                              crontab specification for when the action should occur
                              Equivalent swagger model: TimeTableEntry
                            properties:
                              count:
                                description: Number of artifacts to retain for this
                                  Entry
                                format: int64
                                type: integer
                              crontab:
                                description: |-
                                  crontab entry (line) specifying when the action should occur.
                                  See `man crontab` and https://en.wikipedia.org/wiki/Cron#CRON_expression
                                type: string
                            type: object
                          type: array
                        weekly:
                          format: int64
                          type: integer
                        yearly:
                          format: int64
                          type: integer
                      type: object
                    retireParameters:
                      description: RetireParameters retire parameters
                      properties:
                        actionPodSpec:
                          description: ActionPodSpec reference to ActionPodSpec CR
                            which will be used for pods created during the operation.
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                        retireIndex:
                          description: RetireIndex counts up with each retirement
                            by a policy; zero indicates uncounted
                          type: integer
                      type: object
                    stageParameters:
                      description: |-
                        StageParameters are the parameters for a stage action
                        swagger:model StageParameters
                      properties:
                        actionPodSpec:
                          description: ActionPodSpec reference to ActionPodSpec CR
                            which will be used for pods created during the operation.
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                        filters:
                          description: |-
                            ActionFilters specifies filters that can be applied to resources that an action
                            operates on. All filters are ANDed.
                          properties:
                            excludeClusterResources:
                              description: |-
                                ExcludeCluster specifies which cluster-scope resource to exclude based
                                on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            excludeExtraResources:
                              description: ExcludeExtraResources specifies resources
                                to exclude from those included by IncludeExtraResources.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            excludeResources:
                              description: |-
                                ExcludeResources specifies which resources to exclude from the default resource set
                                based on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeClusterResources:
                              description: |-
                                IncludeCluster specifies which cluster-scope resources to include based
                                on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeExtraResources:
                              description: IncludeExtraResources specifies resources
                                that are not in the default resource set to include.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeResources:
                              description: |-
                                IncludeResources specifies which resources to include from the default resource set
                                based on a ResourceMatcher.
                                Kanister does not contain k8s:openapi-gen annotations, so we must exclude these
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                          type: object
                        transformations:
                          description: |-
                            Transformations are the transformations to apply to the staged data
                            before it is staged.
                            This is a list of transformation references or inline transformations.
                            If this is empty, no transformations are applied.
                            For stage actions, transformations are currently only useful for PVCs
                            since only PVCs are staged. For other resources, transformations
                            can be applied during the restore action.
                          items:
                            description: TransformOrRef holds transformation for a
                              single resource type or a reference to some transform
                              set.
                            properties:
                              transformSetRef:
                                description: |-
                                  Reference refers to a kubernetes custom resource object that is in the same
                                  API group as the referring object, thus the API version is elided. The object
                                  kind is also not included because it is determined by the referring object.
                                  When the type of the referenced object can change, then use
                                  K8sObjectReference instead
                                properties:
                                  name:
                                    description: Name of the referenced object
                                    type: string
                                  namespace:
                                    description: |-
                                      Namespace where the object is located. If it is empty, it should be
                                      interpreted as the same namespace of the referencing object, and not
                                      `default` by default.
                                    type: string
                                required:
                                - name
                                type: object
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          type: array
                      type: object
                    upgradeParameters:
                      properties:
                        profile:
                          description: Profile reference
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                      type: object
                    validateParameters:
                      description: |-
                        ValidateParameters are the parameters for a validate action
                        swagger:model ValidateParameters
                      properties:
                        actionPodSpec:
                          description: ActionPodSpec reference to ActionPodSpec CR
                            which will be used for pods created during the operation.
                          properties:
                            name:
                              description: Name of the referenced object
                              type: string
                            namespace:
                              description: |-
                                Namespace where the object is located. If it is empty, it should be
                                interpreted as the same namespace of the referencing object, and not
                                `default` by default.
                              type: string
                          required:
                          - name
                          type: object
                        filters:
                          description: |-
                            ActionFilters specifies filters that can be applied to resources that an action
                            operates on. All filters are ANDed.
                          properties:
                            excludeClusterResources:
                              description: |-
                                ExcludeCluster specifies which cluster-scope resource to exclude based
                                on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            excludeExtraResources:
                              description: ExcludeExtraResources specifies resources
                                to exclude from those included by IncludeExtraResources.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            excludeResources:
                              description: |-
                                ExcludeResources specifies which resources to exclude from the default resource set
                                based on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeClusterResources:
                              description: |-
                                IncludeCluster specifies which cluster-scope resources to include based
                                on a ResourceMatcher.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeExtraResources:
                              description: IncludeExtraResources specifies resources
                                that are not in the default resource set to include.
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                            includeResources:
                              description: |-
                                IncludeResources specifies which resources to include from the default resource set
                                based on a ResourceMatcher.
                                Kanister does not contain k8s:openapi-gen annotations, so we must exclude these
                              items:
                                description: ResourceRequirement allows specifying
                                  a resource requirement by type and/or name
                                properties:
                                  group:
                                    type: string
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  resource:
                                    type: string
                                  version:
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              type: array
                          type: object
                        volumeDataCheckOptions:
                          description: |-
                            VolumeDataCheckOptions is a structure that holds the options applicable for when the
                            dry run mode is volume data check.
                          properties:
                            failFast:
                              description: |-
                                FailFast will cause any issues found while checking volume data to immediately cease the
                                check and return the report. If unset, and by default, the volume data check will
                                continue after finding errors and accumulate multiple error outputs at a time.
                              type: boolean
                            verifyFilesPercent:
                              description: |-
                                VerifyFilesPercent gives the percent of files to be randomly downloaded to verify their content.
                                Valid values are strings representing floats in the range [0.0 .. 100.0].
                              type: string
                          type: object
                      type: object
                  type: object
                type: array
              backupWindow:
                description: OperationalWindow has a time frame within which policy
                  should be executed
                properties:
                  end:
                    description: TimeOfDay has time of the day
                    properties:
                      hour:
                        format: int32
                        type: integer
                      minute:
                        format: int32
                        type: integer
                    required:
                    - hour
                    - minute
                    type: object
                  start:
                    description: TimeOfDay has time of the day
                    properties:
                      hour:
                        format: int32
                        type: integer
                      minute:
                        format: int32
                        type: integer
                    required:
                    - hour
                    - minute
                    type: object
                required:
                - end
                - start
                type: object
              comment:
                description: User-defined description for this policy
                type: string
              createdBy:
                type: string
              enableStaggering:
                type: boolean
              frequency:
                description: Frequency indicates valid frequency values
                type: string
              kdrSnapshotConfiguration:
                description: KDRSnapshotConfiguration wraps the user intention to
                  enable export of catalog pvc snapshot
                properties:
                  exportCatalogSnapshot:
                    description: enable users to opt-in to exporting the catalog PVC
                      data
                    type: boolean
                  takeLocalCatalogSnapshot:
                    description: enable users to opt-out of creating a local catalog
                      PVC snapshot
                    type: boolean
                type: object
              lastModifyHash:
                format: int32
                type: integer
              modifiedBy:
                type: string
              paused:
                type: boolean
              presetRef:
                description: |-
                  Reference refers to a kubernetes custom resource object that is in the same
                  API group as the referring object, thus the API version is elided. The object
                  kind is also not included because it is determined by the referring object.
                  When the type of the referenced object can change, then use
                  K8sObjectReference instead
                properties:
                  name:
                    description: Name of the referenced object
                    type: string
                  namespace:
                    description: |-
                      Namespace where the object is located. If it is empty, it should be
                      interpreted as the same namespace of the referencing object, and not
                      `default` by default.
                    type: string
                required:
                - name
                type: object
              retention:
                description: Retention has the retention schedule for a policy
                properties:
                  daily:
                    format: int64
                    type: integer
                  hourly:
                    format: int64
                    type: integer
                  monthly:
                    format: int64
                    type: integer
                  table:
                    description: |-
                      TimeTable is a collection of entries for a (retention) schedule
                      Equivalent swagger model: TimeTable
                    items:
                      description: |-
                        TimeTableEntry Entry / row in a time table containing a retention count and a
                        crontab specification for when the action should occur
                        Equivalent swagger model: TimeTableEntry
                      properties:
                        count:
                          description: Number of artifacts to retain for this Entry
                          format: int64
                          type: integer
                        crontab:
                          description: |-
                            crontab entry (line) specifying when the action should occur.
                            See `man crontab` and https://en.wikipedia.org/wiki/Cron#CRON_expression
                          type: string
                      type: object
                    type: array
                  weekly:
                    format: int64
                    type: integer
                  yearly:
                    format: int64
                    type: integer
                type: object
              selector:
                description: |-
                  A label selector is a label query over a set of resources. The result of matchLabels and
                  matchExpressions are ANDed. An empty label selector matches all objects. A null
                  label selector matches no objects.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              subFrequency:
                description: SubFrequency has modifiers for a Frequency associated
                  with a Policy or PolicyAction
                properties:
                  days:
                    items:
                      format: int32
                      type: integer
                    type: array
                  hours:
                    items:
                      format: int32
                      type: integer
                    type: array
                  minutes:
                    items:
                      format: int32
                      type: integer
                    type: array
                  months:
                    items:
                      format: int32
                      type: integer
                    type: array
                  weekdays:
                    items:
                      format: int32
                      type: integer
                    type: array
                type: object
            type: object
          status:
            description: PolicyStatus defines the observed state of Policy
            properties:
              error:
                description: List of errors with the policy (for example, due to validation
                  failures)
                items:
                  type: string
                type: array
              hash:
                description: Hash of Spec
                format: int32
                type: integer
              specModifiedTime:
                description: Timestamp when spec last changed
                format: date-time
                type: string
              validation:
                description: Validation status
                type: string
            type: object
        type: object
        x-kubernetes-validations:
        - message: name must be 63 characters or fewer to be usable as a Kubernetes
            label value
          optionalOldSelf: true
          rule: oldSelf.hasValue() || size(self.metadata.name) <= 63
    served: true
    storage: true
    subresources: {}
status:
  acceptedNames:
    kind: Policy
    listKind: PolicyList
    plural: policies
    singular: policy
  conditions:
  - lastTransitionTime: "2026-05-23T07:21:12Z"
    message: no conflicts found
    reason: NoConflicts
    status: "True"
    type: NamesAccepted
  - lastTransitionTime: "2026-05-23T07:21:13Z"
    message: the initial names have been accepted
    reason: InitialNamesAccepted
    status: "True"
    type: Established
  storedVersions:
  - v1alpha1
