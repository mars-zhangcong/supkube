# SupKube 架构设计

> **文档定位**
> - `USER_MANUAL.md` = 客户 / 运维视角（怎么用、怎么配、怎么排障）
> - `架构设计.md` = 开发者视角（为什么这么造、哪个组件管什么、决策为什么是这个不是那个）
>
> **版本**：v0.8.5-alpha（持续更新；每次发版后追加变更记录到第 11 章）
> **目标读者**：参与 SupKube 开发 / 评估 / 二次定制的工程师

---

## 目录

1. [设计哲学](#1-设计哲学)
2. [整体架构](#2-整体架构)
3. [后端架构](#3-后端架构)
4. [前端架构](#4-前端架构)
5. [数据模型与术语映射](#5-数据模型与术语映射)
6. [关键流程](#6-关键流程)
7. [部署与配置](#7-部署与配置)
8. [安全模型](#8-安全模型)
9. [ADR — 架构决策记录](#9-adr--架构决策记录)
10. [编码规范与约定](#10-编码规范与约定)
11. [未来演进路径](#11-未来演进路径)

---

## 1. 设计哲学

### 1.1 我们是什么 / 不是什么

**SupKube 是 Kubernetes 数据保护的产品层**，在 Velero 之上提供生产可用的 Web UI、智能化运维（Pre-flight / Advisor）、和多 IdP 集成。

| 是 | 不是 |
|---|---|
| Velero 的 UI + 智能编排 | 一个新的备份引擎 |
| Kasten K10 设计模式的开源替代 | Kasten 的克隆 |
| GitOps 友好的纯 Helm 应用 | 自带数据库的 SaaS |
| 多云 BSL/IdP/CSI 透明适配 | 绑定某家云厂商 |

### 1.2 三个核心原则

**原则一：站在巨人肩上，不重造轮子**

Velero 在 K8s 数据保护领域是 CNCF 毕业项目、事实标准。重做一遍备份引擎是 18 个月的工作量，没有商业价值。SupKube 的所有差异化都在**产品层 + 智能层**：

| 层 | 谁负责 |
|---|---|
| 应用层 UI / UX / i18n | **SupKube** |
| 智能编排（Advisor / Pre-flight / Transform Set） | **SupKube** |
| 数据面：实际备份 / 恢复 / 调度 | Velero |
| 卷快照 | CSI Snapshotter / Restic / Kopia |
| 对象存储 | S3 / MinIO / GCS / Azure Blob |
| 认证编排 | Dex（OIDC 适配层） |
| 集群编排 | Kubernetes |

**原则二：用户视角 ≠ 实现视角**

K8s 用户思考的是"我要恢复到某个时间点"，不是"我要创建一个 Backup CR"。SupKube **永远用产品语言面向用户**，CRD 名只出现在管理员排障时（kubectl 输出）。例：

| 用户视角 | 实现视角 |
|---|---|
| Restore Point | `Backup` CRD |
| Storage Profile | `BackupStorageLocation` CRD |
| Snapshot Profile | `VolumeSnapshotLocation` CRD |
| Policy | `Schedule` CRD + 保留策略 |
| Action | 统一抽象层，映射到 Backup/Restore CR |
| Transform Set | `ConfigMap`（特殊 label）|

**原则三：配置即代码，无管理后台**

所有系统级配置（IdP、RBAC、BSL 凭据、TLS）都走 Helm values + Secret。**SupKube 没有 admin 设置页面**。

理由：
- 客户已经在做 GitOps，再造一个 admin UI 与他们的流程对抗
- 安全审计走 Git PR，比 UI 审计便宜
- 多集群部署只需 helm install，不需要逐集群手工配置

### 1.3 与上游项目的关系

```
                  SupKube
                  ┌─────────────────────────┐
                  │  UI + 智能编排           │  ← 我们造
                  └────────────┬────────────┘
                               │ K8s API + 自定义业务逻辑
                  ┌────────────▼────────────┐
                  │  Velero (CNCF)          │  ← 数据面
                  └────────────┬────────────┘
                               │
        ┌──────────┬───────────┼───────────┬──────────┐
        ▼          ▼           ▼           ▼          ▼
    ┌──────┐  ┌─────────┐  ┌─────────┐  ┌──────┐ ┌──────┐
    │ CSI  │  │  BSL    │  │  Dex    │  │Kanis-│ │Kuber-│
    │Snap  │  │(S3/GCS/ │  │(OIDC    │  │ ter  │ │netes │
    │      │  │ Azure)  │  │adapter) │  │(v0.9)│ │  API │
    └──────┘  └─────────┘  └─────────┘  └──────┘ └──────┘
```

**升级 Velero 时我们也跟着升级**。SupKube 锁定 `velero v1.13.0` 作为最低版本；新 Velero 特性（如 v1.15 的 datamover）我们按需追加适配，不会因为 Velero 改 API 而强制重构 SupKube 业务层（因为我们用的是 Velero 的 typed CRD client，编译期就能发现 break）。

---

## 2. 整体架构

### 2.1 组件拓扑

```
                            ┌──────────────────────────┐
                            │     用户浏览器           │
                            └────────────┬─────────────┘
                                         │ HTTPS (生产)
                                         │ HTTP  (本地)
                                         ▼
┌────────────────────────────────────────────────────────────────────┐
│  Kubernetes Cluster: namespace=supkube                              │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  supkube-frontend (Deployment, nginx + Vue SPA)               │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │  /              → Vue 3 SPA (index.html + assets/*)     │  │  │
│  │  │  /api/*         → proxy_pass http://supkube-backend     │  │  │
│  │  │  /dex/*         → proxy_pass http://supkube-dex (auth)  │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  └────────────────┬────────────────────┬───────────────────────┘  │
│                   │                    │                          │
│          ┌────────▼─────────┐  ┌──────▼─────────┐                │
│          │ supkube-backend  │  │  supkube-dex   │                │
│          │ (Go binary)      │  │  (OIDC server) │                │
│          │                  │  │                │                │
│          │ - REST API       │  │ - JWT issuer   │                │
│          │ - Velero client  │  │ - Connectors:  │                │
│          │ - Dynamic client │  │   • password   │                │
│          │ - OIDC verifier  │  │   • Keycloak   │                │
│          │ - BSL clients    │  │   • Okta       │                │
│          │   (v0.8.6+)      │  │   • Azure AD   │                │
│          │                  │  │   • GitHub     │                │
│          └────────┬─────────┘  └────────┬───────┘                │
│                   │                     │                         │
└───────────────────┼─────────────────────┼─────────────────────────┘
                    │ K8s API             │ HTTPS to upstream IdP
                    ▼                     ▼
            ┌───────────────┐    ┌────────────────┐
            │ Kubernetes    │    │  External IdP  │
            │  API Server   │    │ (customer-side)│
            └───────┬───────┘    └────────────────┘
                    │
                    ▼
            ┌───────────────────────────┐
            │ namespace=velero          │
            │ ┌──────────┐ ┌──────────┐ │
            │ │ Velero   │ │  Velero  │ │
            │ │ Server   │ │  CRDs    │ │
            │ │ (CRDs    │ │ Backup   │ │
            │ │  manager)│ │ Restore  │ │
            │ │          │ │ Schedule │ │
            │ │          │ │ BSL/VSL  │ │
            │ │          │ │ DRBR DR  │ │
            │ └────┬─────┘ └──────────┘ │
            └──────┼────────────────────┘
                   │
        ┌──────────┴──────────┐
        ▼                     ▼
   ┌──────────┐         ┌─────────────┐
   │  CSI     │         │  BSL        │
   │  Snap-   │         │  (S3/MinIO/ │
   │  shotter │         │   GCS/Azure)│
   └──────────┘         └─────────────┘
```

### 2.2 数据流（典型备份操作）

```
1. 用户点击「创建 Restore Point」
   └─▶ SPA: POST /api/v1/backups
       └─▶ Backend: 校验 Bearer JWT + RBAC
           └─▶ Backend: 构造 velerov1.Backup CR + cl.Create()
               └─▶ Velero Controller: 接管 → 触发实际备份流程
                   ├─▶ CSI Snapshotter: 创建 VolumeSnapshot
                   └─▶ BSL: 上传 tarball + metadata
2. SPA 5s 轮询 GET /api/v1/actions
   └─▶ Backend: 聚合 Backup CR 列表 + 派生 Action.phases
       └─▶ SPA: 渲染卡片 + ✓ 阶段清单
```

### 2.3 部署形态

**单 Helm Chart**：`supkube-helm/supkube/` 包含所有组件（frontend / backend / dex / nginx ConfigMap / RBAC / Service / 可选 Dex）。

- `helm install supkube ./supkube-helm/supkube -n supkube --create-namespace`
- 一条命令完整部署
- Dex 可选（`auth.dex.enabled=false` 时跳过部署，客户接外部 OIDC issuer）
- 不依赖 Helm Operator / ArgoCD / Flux

**为什么单 Chart**：参见 [ADR-004](#adr-004-helm-单包不做管理后台)。

---

## 3. 后端架构

### 3.1 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 语言 | Go 1.25 | K8s 生态全栈 Go，client-go 是一等公民 |
| Web 框架 | Gin | 轻量、中间件生态丰富、性能足够 |
| K8s 客户端 | `client-go` + `controller-runtime` | 前者用于 typed core API；后者用于 Velero CRD（带 scheme 注册） |
| 动态 K8s 客户端 | `dynamic.Interface` | 用于枚举任意 GVR（artifacts、components）|
| OIDC 验证 | `github.com/coreos/go-oidc/v3` | 事实标准，Kasten/ArgoCD/Velero 都用 |
| YAML 解析 | `sigs.k8s.io/yaml` | K8s 风格 YAML，处理 metav1 类型正确 |

### 3.2 包结构

```
supkube-backend/
├── cmd/server/server.go       # gin engine + 路由注册 + 中间件链
├── main.go                    # 入口，调用 server.Run()
├── internal/
│   ├── k8s/                   # client 构造（runtime + typed + dynamic）
│   ├── auth/                  # OIDC verifier + middleware + handlers
│   │   ├── auth.go           # Config, Middleware, Init
│   │   └── handlers.go       # /providers /callback /me /logout
│   ├── api/v1/               # REST handlers (按资源类型分文件)
│   │   ├── handlers.go       # 基础 (Backup/Restore/Namespace CRUD)
│   │   ├── actions.go        # Activity / Action 统一抽象
│   │   ├── applications.go   # 应用列表 + Backup Advisor
│   │   ├── appdetails.go     # 应用详情
│   │   ├── dashboard.go      # Dashboard 聚合统计
│   │   ├── schedules.go      # Policy (Schedule) CRUD
│   │   ├── storage.go        # BSL/VSL CRUD + GetBackupResources
│   │   ├── transform_sets.go # Transform Set CRUD + ApplyConflictFixes
│   │   ├── transform_sets_seed.go # 内置模板种子 + 迁移
│   │   ├── restores_preflight.go # Pre-flight 检测器
│   │   └── restore_results.go    # DownloadRequest 流程拉详细错误
│   └── bsl/                  # (v0.8.6+) BSL SDK 抽象
│       ├── client.go         # 接口 + ClientFor 工厂
│       ├── aws.go            # AWS S3 + MinIO 兼容
│       ├── gcp.go            # GCS
│       └── azure.go          # Azure Blob
```

**包组织原则**：
- 按 **业务域** 而不是按层（不要 `controllers/`、`services/`、`repos/`）
- 每个文件 < 600 行，超了就按子主题拆分
- `internal/api/v1/` 是稳定 API 表面；将来 v2 通过 `internal/api/v2/` 平行存在

### 3.3 REST API 规范

#### 路径约定

```
/api/v1/<resource>             # 集合
/api/v1/<resource>/:name       # 单个
/api/v1/<resource>/:name/:verb # 子操作（动名词）
```

**资源名永远复数**：`/backups`、`/restores`、`/policies`、`/transform-sets`、`/actions`。

**动词作为路径段而不是 HTTP method**：`POST /transform-sets/apply-conflict-fixes` 而非 `POST /apply?type=conflict-fix`。

#### HTTP method 语义

| Method | 用途 |
|---|---|
| `GET` | 列表 + 单个 + 子查询。永远幂等 |
| `POST` | 创建 + "执行某个动作"（如 `/preflight`、`/run-once`、`/verify`）|
| `PUT` | 完整替换（Transform Set）|
| `PATCH` | 部分更新（Schedule pause/resume）|
| `DELETE` | 删除 |

#### 响应约定

成功：
```json
{
  "items": [...],
  "total": N
}
```
单个：直接返回对象。

错误：
```json
{ "error": "human-readable message" }
```

**HTTP 状态码**：
- `200` 读成功
- `201` 创建成功
- `400` 客户端请求格式错误
- `401` 未认证
- `403` 已认证但权限不足
- `404` 资源不存在
- `409` 冲突（资源已存在 / 状态不允许操作）
- `500` 后端内部错误

#### 缓存策略

服务端永远 `Cache-Control: no-store`。客户端每个 GET 自动加 `?_=<timestamp>` 缓存破坏参数（前端 axios 拦截器）。

### 3.4 认证中间件链

```
请求 → CORS → AuthMiddleware → Gin handler
                   │
                   ├─ /api/v1/status         → bypass (健康检查)
                   ├─ /api/v1/auth/*         → bypass (登录流自身)
                   └─ 其它                   → 校验 Bearer JWT
                                                └─ 注入 user.* 到 gin.Context
```

中间件实现见 `internal/auth/auth.go` 的 `Middleware()`。

### 3.5 缓存策略

| 缓存项 | 实现 | TTL |
|---|---|---|
| OIDC provider + JWKS | go-oidc 自带 | go-oidc 默认刷新 |
| BSL ListObjects（v0.8.6+）| 内存 map[bslName] → result | 60s |
| Action 列表 | 不缓存（直接 K8s API）| — |
| 应用列表 | 不缓存（直接 K8s API）| — |
| Transform Set 列表 | 不缓存 | — |

**不缓存的原因**：K8s API server 自带 watch + cache，client-go 命中本地缓存的概率很高。我们再加一层会引入一致性问题（删除后列表还显示旧的）。

### 3.6 错误处理

```go
// 模式 1: 用户可纠正 → 400 + 友好消息
c.JSON(http.StatusBadRequest, gin.H{"error": "name is required"})

// 模式 2: 后端依赖故障 → 500 + 原始错误（运维需要）
c.JSON(http.StatusInternalServerError, gin.H{"error": err.Error()})

// 模式 3: 部分成功 → 200 + 详细 status
c.JSON(http.StatusOK, gin.H{"items": items, "warnings": [...]})
```

**Best-effort 模式**：核心数据先返回，可选 detail 失败时不阻塞主流程：

```go
// 例：GetRestoreResults
payload := gin.H{...必填字段...}
if hasErrors {
    detailed, err := fetchRestoreDetailedResults(ctx, name)
    if err != nil {
        payload["detailedError"] = err.Error() // 不阻塞
    } else {
        payload["detailed"] = detailed
    }
}
```

---

## 4. 前端架构

### 4.1 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 框架 | Vue 3 + Composition API | 比 Options API 更适合大型应用，类型友好 |
| UI 库 | Element Plus | K8s 控制台生态首选，组件最全 |
| 构建 | Vite | dev 速度快、HMR 体验好、ES module 原生 |
| 状态管理 | Composables（不用 Pinia）| 我们的状态足够简单 |
| 图表 | vue-echarts + ECharts 5 | 与 useTheme 集成实现暗色模式自动跟随 |
| i18n | vue-i18n 9 | Composition API 友好 |
| HTTP | axios | 拦截器机制成熟（Bearer / 401 / 缓存破坏）|
| 路由 | vue-router 4 | beforeEach 守卫 + meta 字段做 public 路由 |

### 4.2 模块组织

```
supkube-frontend/src/
├── App.vue                    # 根组件：sidebar + header + router-view
├── main.js                    # 入口：挂载、注册 i18n、ElementPlus
├── router/index.js            # 路由表 + 认证守卫
├── i18n.js                    # vue-i18n 配置 + locale 切换持久化
├── api/velero.js              # 所有 axios 调用，业务函数式封装
├── composables/
│   ├── useAuth.js             # 认证状态 + login/logout/handleCallback
│   └── useTheme.js            # 暗色模式 + 反应式 ECharts 调色板
├── locales/
│   ├── en.js                  # 英文 i18n
│   └── zh-CN.js               # 简中 i18n
├── components/                # 跨页面复用组件
│   ├── ActionPhases.vue       # 通用阶段清单 (Activity + Detail Drawer 复用)
│   ├── ActionDurationChart.vue
│   ├── ActionDetailDrawer.vue
│   ├── RestoreDrawer.vue      # Kasten 风格的恢复抽屉
│   ├── BackupSuccessTrend.vue
│   ├── ProtectionCoverage.vue
│   └── StorageUsage.vue
├── views/                     # 路由对应的页面
│   ├── Login.vue              # 单文件覆盖 /login + /auth/callback
│   ├── Dashboard.vue
│   ├── Applications.vue
│   ├── Activity.vue           # 统一动作流（替代 Restores）
│   ├── Backups.vue            # Restore Points 列表
│   ├── Policies.vue
│   ├── TransformSets.vue
│   ├── StorageLocations.vue
│   ├── VolumeSnapshotLocations.vue
│   ├── BackupAdvisor.vue
│   ├── BackupDetail.vue
│   └── Settings.vue
└── utils/
    └── phase.js               # phase → tag type 等 helper
```

### 4.3 状态管理：为什么不用 Pinia

我们考虑过 Pinia，但发现：

- **大多数状态是本地的**：每个 view 自己 fetch 自己用，不跨页面共享
- **少数全局状态**：`useAuth`（user/token）、`useTheme`（dark mode）—— 用 composables + reactive ref 完全够用
- **避免过度工程**：引入 Pinia 意味着 store 文件 + 设备工具 + DevTools 集成，对一个 10 页面的应用是负担

**判断准则**：当某个状态 ≥3 个不相关页面同时读写时再考虑 Pinia。目前不到这个阈值。

### 4.4 路由守卫

```javascript
// router/index.js
router.beforeEach(async (to) => {
  if (to.meta?.public) return true        // /login, /auth/callback 直接放行
  const auth = useAuth()
  if (!authBootstrapped) {
    await auth.bootstrap()                // 调一次 /auth/me 看 token 是否有效
    authBootstrapped = true
  }
  if (auth.isLoggedIn.value) return true
  return { path: '/login', query: { redirect: to.fullPath } }
})
```

**meta.public** 是路由的 escape hatch — 登录页和回调页自己不能被守卫拦截。

### 4.5 i18n 与暗色模式

**i18n key 命名规则**：
- `<page>.<element>.<state?>` — `restoreDrawer.preflightTitle`、`activity.detail.errorsHeader`
- `common.*` 跨页面通用（`save`、`cancel`、`name`、`status`）
- `phase.*`、`compliance.*` 是状态枚举的翻译

**暗色模式**：通过给 `<html>` 加 `.dark` class 触发。`useTheme` composable 用 MutationObserver 监听这个 class 的变化，并反应式更新 `isDark` ref。ECharts 实例订阅这个 ref，主题切换时图表配色自动更新（不需要重新挂载）。

### 4.6 图表系统

```javascript
// composables/useTheme.js
const isDark = ref(document.documentElement.classList.contains('dark'))
new MutationObserver(() => {
  isDark.value = document.documentElement.classList.contains('dark')
}).observe(document.documentElement, { attributes: true, attributeFilter: ['class'] })

const chartColors = computed(() => isDark.value ? darkPalette : lightPalette)
```

每个图表组件用 `computed` 构造 ECharts `option`，其中颜色字段引用 `chartColors.value`。Vue 自动重渲染时 vue-echarts 检测到 option 变化、平滑过渡到新主题。

**为什么不用 ECharts 自带 theme**：自带 theme 是初始化时一次性注册，切换主题要重新初始化整个图表实例，会闪烁 + 丢失动画。

---

## 5. 数据模型与术语映射

### 5.1 用户视角 vs 实现视角

```
┌─────────────────────┬───────────────────────────────┬──────────────────┐
│ 用户看到 (UI / 手册) │ 底层 (Velero CRD / K8s 资源)   │  实现位置          │
├─────────────────────┼───────────────────────────────┼──────────────────┤
│ Restore Point       │ velero.io/v1 Backup           │ handlers.go      │
│ Storage Profile     │ velero.io/v1                  │ storage.go       │
│                     │   BackupStorageLocation       │                  │
│ Snapshot Profile    │ velero.io/v1                  │ storage.go       │
│                     │   VolumeSnapshotLocation      │                  │
│ Policy              │ velero.io/v1 Schedule         │ schedules.go     │
│ Action              │ Backup CR + Restore CR        │ actions.go       │
│                     │ (聚合 + Phase 派生)            │  (统一抽象层)     │
│ Transform Set       │ core/v1 ConfigMap with        │ transform_sets.go│
│                     │   label supkube.io/kind=...   │                  │
│ Pre-flight Conflict │ 内存对象（不持久化）           │ restores_preflight│
│ Components 计数     │ count(restoreArtifactGVRs)    │ applications.go  │
│ Restore Points 计数 │ count(Backup with this ns)    │ applications.go  │
└─────────────────────┴───────────────────────────────┴──────────────────┘
```

**永远不要在 UI 上暴露 CRD 名字**，除非是故障排查页（YAML viewer）。

### 5.2 Action 统一抽象

Action 是 SupKube 引入的最大产品抽象，[ADR-002](#adr-002-action-统一抽象) 详细论证。简言之：

```go
// internal/api/v1/actions.go
type Action struct {
    ID              string
    Type            string  // "Backup" | "Restore" | "Export"（v0.9）
    Status          string  // "running" | "completed" | "failed" | "partial"
    Phases          []Phase // ✓ ⏳ ✗ 三态清单（Kasten 风格）
    StartTime       *time.Time
    EndTime         *time.Time
    DurationSeconds int
    // 关联引用（按 Type 不同）
    Policy          *Ref
    ProtectedObject *Ref
    RestorePoint    *Ref
    TargetNamespace string
    Artifacts       *ArtifactStats
    Errors, Warnings int
    RawKind, RawName string
    Origin          string  // "manual" | "policy" | "imported"
}
```

每个 Velero Backup/Restore CR 通过 `backupToAction()` / `restoreToAction()` 派生成 Action。**Phase 列表是关键** — 把 Velero 的扁平 `.status.phase` 字符串映射成 Kasten 风格的多阶段 ✓ 清单，是产品差异化的核心。

### 5.3 Conflict + TransformRule（Pre-flight + Apply Fix 数据模型）

```go
// internal/api/v1/restores_preflight.go
type Conflict struct {
    Kind         string  // "NodePortCollision" | "PVBinding" | ... (10 类)
    Severity     string  // "blocker" | "warning"
    Artifact     ArtifactRef
    Field        string  // "/spec/clusterIP" 等 JSON Pointer
    CurrentValue interface{}
    ConflictsWith string  // 人类可读的冲突来源
    Message       string
    SuggestedTransform *TransformRule
}

type TransformRule struct {
    // 三种 patch 类型互斥 — 见 ADR-006
    Operation, Path string
    Value           interface{}
    MergePatch      string  // RFC 7396
    StrategicPatch  string  // K8s strategic merge
}
```

**Pre-flight 的关键设计**：每个 Conflict **自带 SuggestedTransform**，让"检测"和"修复"用同一份数据流。Apply Fix 不需要重新计算什么 — 把 SuggestedTransform 序列化成 Velero ResourceModifier ConfigMap 即可。

---

## 6. 关键流程

### 6.1 备份流程（用户视角）

```
1. 用户进入 Restore Points 页 → 点 [Create Restore Point]
2. 填表：name, ns, mode (FS vs CSI), TTL, storage location
3. 提交 → SPA: POST /api/v1/backups (Bearer JWT)
   └─ 后端: 校验 → 构造 velerov1.Backup CR → cl.Create()
4. Velero 接管：
   ├─ Validation 阶段 (status.phase = "New" → "InProgress")
   ├─ Snapshotting Application Components (PVCs 走 CSI 或 Restic)
   ├─ Uploading to BSL (status.phase = "Uploading")
   └─ Finalizing → "Completed" / "PartiallyFailed" / "Failed"
5. SPA: 5s 轮询 GET /api/v1/actions
   └─ 后端: 遍历 Velero Backup CR → 派生 Action.phases
       └─ SPA: 渲染 ✓ 阶段清单 + 进度
6. 备份完成后，自动滚动到 Activity 卡片，状态变绿
```

### 6.2 还原流程（Pre-flight + Apply Fix + Transform Set 全链）

```
1. 用户：Restore Points 列表 → 任意行 ⋮ → [Restore]
2. 抽屉打开：
   ├─ Application Name 区块（命名空间选择 + 新建 ns 按钮）
   │  └─ 选 = 原 ns 时：弹出黄色覆盖警告 + 强制 checkbox
   ├─ Optional Settings（Restore name, existingResourcePolicy）
   └─ 加载中：
      ├─ GET /api/v1/backups/:name/artifacts （Spec 清单 + YAML）
      └─ POST /api/v1/restores/preflight     （冲突检测）
3. Pre-flight 区块出现，列出 N 个冲突：
   ├─ 每条带 Severity 徽章 (blocker / warning)
   ├─ Suggested fix 代码预览（mergePatch / strategicPatch / JSON Patch）
   └─ [Apply Fix] 按钮
4. 用户点 Apply Fix：
   └─ SPA: POST /api/v1/transform-sets/apply-conflict-fixes
      （把当前所有已修复的 conflicts 整批发过去）
      └─ 后端: 合并所有 rules 进单个 ConfigMap "preflight-fix-<restore-name>"
          └─ SPA: 标记该 conflict 已修复 (绿色 ✓)，selectedTransformSet 自动选中
5. 所有 blocker 都修复后：[恢复] 按钮自动变可点
6. 用户点 [恢复]：
   ├─ 如果是覆盖原 ns：后端先 kubectl delete ns + 等待消失 (max 60s)
   └─ 后端: 创建 velerov1.Restore CR
            with spec.resourceModifierRef → 上面生成的 Transform Set
7. Velero 接管：
   ├─ Validation
   ├─ Restoring Application Components (按 backup tarball + applying patches)
   └─ Finalizing → 状态汇总
8. SPA: 跳转到 Activity 页面，Restore 卡片实时显示 phase 进度
9. 若 PartiallyFailed：
   └─ 用户点详情卡片 → ActionDetailDrawer
       └─ 后端: GET /api/v1/restores/:name/results
           └─ 通过 DownloadRequest 拉真实错误（per-namespace 分组）
```

### 6.3 OIDC 登录流程

```
浏览器                  SupKube SPA      Backend (gin)    Dex            Upstream IdP
   │                       │                │              │                 │
   │ 1. GET /              │                │              │                 │
   ├──────────────────────►│                │              │                 │
   │                       │ 2. 路由守卫    │              │                 │
   │                       │    检查 token  │              │                 │
   │                       │    无 → /login │              │                 │
   │                       │                │              │                 │
   │ 3. /login 页面挂载    │                │              │                 │
   │                       │ GET /providers │              │                 │
   │                       ├───────────────►│              │                 │
   │                       │◄───────────────┤              │                 │
   │                       │ [{loginUrl,    │              │                 │
   │                       │   connector_id}]              │                 │
   │ 4. 用户点按钮         │                │              │                 │
   │ ◄─────────────────────┤                │              │                 │
   │  redirect to          │                │              │                 │
   │  /dex/auth?...        │                │              │                 │
   │ ──────────────────────────────────────►│              │                 │
   │                                        │ 5. 跳上游 IdP 或本地密码表单     │
   │                                        ├──────────────►│                │
   │                                        │              │                │
   │ 6. 用户认证           │                │              │                │
   │ ──────────────────────────────────────────────────────►                │
   │ ◄──────────────────────────────────────────────────────                │
   │                                        │              │                │
   │ 7. /auth/callback?code=...&state=...  │              │                │
   │ ──────────────────────►│                │              │                 │
   │                       │ Login.vue      │              │                 │
   │                       │ POST /callback │              │                 │
   │                       ├───────────────►│              │                 │
   │                       │                │ 8. exchange  │                 │
   │                       │                │    code      │                 │
   │                       │                ├─────────────►│                 │
   │                       │                │◄─────────────┤                 │
   │                       │                │  id_token    │                 │
   │                       │                │              │                 │
   │                       │                │ 9. verify JWT signature        │
   │                       │                │    (JWKS internal URL)         │
   │                       │                │              │                 │
   │                       │◄───────────────┤              │                 │
   │                       │ {idToken, user}│              │                 │
   │                       │ → localStorage │              │                 │
   │                       │                │              │                 │
   │ 10. router.replace('/')                │              │                 │
   │ ◄─────────────────────┤                │              │                 │
   │                       │                │              │                 │
   │ 11. GET /api/...      │                │              │                 │
   │   Authorization:      │                │              │                 │
   │   Bearer <idToken>    │                │              │                 │
   ├──────────────────────►│───────────────►│              │                 │
   │                       │                │ middleware 校验JWT → user inject│
   │                       │◄───────────────┤              │                 │
```

**双 URL 关键点**：
- 步骤 4 / 5：浏览器跳 `localhost:30888/dex/auth`（公开 URL）
- 步骤 8：后端跳 `supkube-dex:5556/dex/token`（集群内 URL）
- 步骤 9：后端验签从 `supkube-dex:5556/dex/keys` 取 JWKS

详见 [ADR-005](#adr-005-双-url-oidc-设计)。

### 6.4 删除 Restore Point（级联删除）

```
SPA → DELETE /api/v1/backups/:name
  └─ Backend: 不调用 cl.Delete(backup) ← 这样只删 CR，BSL 数据残留
      └─ 而是创建 velerov1.DeleteBackupRequest CR
          └─ Velero backup-deletion-controller 接管：
              ├─ 删除 BSL 上的 tarball + metadata
              ├─ 删除关联 CSI VolumeSnapshot + Content
              ├─ 删除 PodVolumeBackups（FS 模式）
              └─ 最后删除 Backup CR 自身
```

[ADR-012](#adr-012-级联删除走-deletebackuprequest)。

---

## 7. 部署与配置

### 7.1 Helm 单包架构

```
supkube-helm/supkube/
├── Chart.yaml                  # version (chart) + appVersion (image tag)
├── values.yaml                 # 客户配置入口
└── templates/
    ├── _helpers.tpl
    ├── backend-deployment.yaml
    ├── frontend-deployment.yaml
    ├── frontend-nginx-configmap.yaml    # v0.8.5: 不 baked into image
    ├── dex-deployment.yaml              # v0.8.5: 仅 auth.dex.enabled=true 时渲染
    ├── dex-configmap.yaml
    ├── service.yaml
    ├── configmap.yaml                   # 业务配置（veleroNamespace 等）
    └── rbac.yaml                        # ServiceAccount + ClusterRole + Binding
```

**版本约定**：
- `Chart.yaml.version` = Helm chart 版本（手动）
- `Chart.yaml.appVersion` = 应用版本（与镜像 tag 一致）
- 两者独立递增 — chart-only 改动（如调整 RBAC）只 bump chart version

### 7.2 ConfigMap-mounted nginx

v0.8.5 前 nginx 配置是 baked into Docker image（`Dockerfile.frontend` 里 RUN printf）。v0.8.5 起拆出到独立 ConfigMap 并 mount 到 Deployment：

```yaml
# frontend-deployment.yaml
volumeMounts:
  - name: nginx-conf
    mountPath: /etc/nginx/conf.d/default.conf
    subPath: default.conf
    readOnly: true
```

**为什么改**：v0.8.5 加 `/dex/*` 反向代理需要改 nginx 配置 — 如果还在 image 里就需要重新构建 frontend image。改成 ConfigMap 后只需 `helm upgrade`，无需 rebuild。

### 7.3 双 URL 设计

[ADR-005](#adr-005-双-url-oidc-设计)。后端注入：

```yaml
- name: OIDC_ISSUER_URL                  # 浏览器看到的 (JWT iss claim)
  value: "http://localhost:30888/dex"
- name: OIDC_DISCOVERY_URL               # 集群内可达
  value: "http://supkube-dex:5556/dex"
```

后端用 `oidc.InsecureIssuerURLContext` 告诉 go-oidc"信任 JWT 的 iss 即使和 discovery URL 不同"。

### 7.4 启动时迁移机制

v0.8.4 引入 [ADR-014](#adr-014-启动时迁移机制) — 后端启动时检查 K8s 资源是否符合最新 schema，自动修正：

```go
// transform_sets_seed.go
func SeedBuiltinTransformSets() {
    go func() {
        time.Sleep(3 * time.Second)
        migrateBrokenTransformSets()   // v0.8.2 → v0.8.3: 删多 key 的 ConfigMap
        seedBuiltinTransformSetsOnce() // 强制刷新内置模板到最新 schema
    }()
}
```

**约定**：每次发版引入了**用户不可见但内部需要清理**的迁移时，都要在 seed 文件加一个 `migrateXxx()` 函数，幂等、容错、最多吐 stdout log。

---

## 8. 安全模型

### 8.1 认证（OIDC via Dex）

详见 USER_MANUAL §11 + [ADR-001](#adr-001-dex-作为-oidc-编排层)。要点：

- 后端只验证一种 token（Dex-issued JWT）
- N 个上游 IdP 通过 Dex connector 适配，**不动后端代码**
- 加新 IdP = 加 `auth.dex.connectors` 数组的一个条目

### 8.2 授权（RBAC 3 层，v0.8.5 step 3）

固定 3 个角色，OIDC groups claim → role 映射通过 Helm values：

```yaml
auth:
  rbac:
    bindings:
      - group: platform-admins
        role: admin
      - group: app-team-postgres
        role: editor
        namespaces: [postgres-prod]
```

| 角色 | 权限 |
|---|---|
| `admin` | 全集群 CRUD（包括 BSL、RBAC、Audit）|
| `editor` | 指定 ns 的 Backup/Restore/Policy CRUD |
| `viewer` | 全集群只读 |

后端 gin middleware 在 auth middleware 之后加 role check middleware，按 endpoint 注解需要的最低角色。

### 8.3 审计日志（v0.8.5 step 4）

- 所有写操作（POST/PUT/PATCH/DELETE + 登录失败）→ K8s Event + stdout 双写
- Schema: `{user, action, resource, namespace, sourceIP, result, timestamp}`
- 不审计读操作（量太大、价值低）
- UI: Settings → Audit Log 页

### 8.4 网络策略

**默认不开 NetworkPolicy**（很多 K8s 没装 CNI 插件）。如果客户需要，提供示例 manifest（`docs/networkpolicy-examples/`，v0.9）。

---

## 9. ADR — 架构决策记录

### ADR-001 Dex 作为 OIDC 编排层

**状态**：Accepted（v0.8.5）
**背景**：客户用 Keycloak / Okta / Azure AD / GitHub / LDAP 等多种 IdP。

**决策**：内嵌 Dex 作为 OIDC 适配层。SupKube 后端只验证 Dex 颁发的 JWT；Dex 内部通过 connectors 适配各上游 IdP。

**为什么不直接实现多 IdP 支持**：
- LDAP / OIDC / OAuth2 协议差异巨大，自己写 = 维护 N 套
- Kasten / ArgoCD 都用 Dex，验证过
- 加新 IdP 只需配 Dex，不动后端

**代价**：多一个 pod (~50MB 内存)；客户运维要懂 Dex。

### ADR-002 Action 统一抽象

**状态**：Accepted（v0.8.0）
**背景**：v0.7.x 时期我们按 CRD 类型分页面（Restore Points / Restores / Policies），用户被迫学 Velero CRD 才能找"刚才做了什么"。

**决策**：引入 Action 抽象层。所有"用户做了一件事"（Backup / Restore / Export）统一映射到 Action，对外暴露 `/api/v1/actions`，前端 Activity 页消费。

**为什么不用 Velero 已有 CRD**：
- Velero 把 Backup / Restore 分开，但用户的心智模型是"我做了什么"，不是"我有几种 CR"
- Kasten 的"Activity"列表是同样思路，工业验证过

**取舍**：Restores 独立页废弃（向后兼容 redirect /restores → /activity?type=Restore）。

### ADR-003 Transform Set 用 ConfigMap，不造 CRD

**状态**：Accepted（v0.8.2）
**背景**：需要存储一组 JSONPath patch 规则，让 Velero 在 restore 时应用。

**决策**：直接复用 Velero 原生的 `Restore.spec.resourceModifierRef`，存为 `velero` ns 下带 `supkube.io/kind=transform-set` 标签的 ConfigMap。

**为什么不造新 CRD**：
- Velero 已经懂 ResourceModifier 格式
- 多一个 CRD = 多一份 CRD 安装步骤 + RBAC + 字段维护
- 客户用 kubectl 也能直接操作

**注意 ⚠️**：v0.8.2 第一次实现时把 description / rules.yaml / rules.json 三个 key 全放 `data` 字段，导致 Velero 报 `illegal resource modifiers`（要求 `len(Data)==1`）。v0.8.3 修复：description 和 rules.json 移到 annotations，data 只留 `rules.yaml`。**永远不要在 ResourceModifier ConfigMap 的 data 里塞多 key**。

### ADR-004 Helm 单包，不做管理后台

**状态**：Accepted（v0.5）
**背景**：管理后台诱惑大 — 客户可视化配置 BSL / IdP / RBAC 听起来很爽。

**决策**：所有系统配置只通过 Helm values + Secret。SupKube 不暴露任何写入 BSL 凭据 / IdP secret / RBAC 规则的 UI。

**为什么**：
- 客户 95% 已经在 GitOps，再造 admin UI 是反向操作
- 安全审计走 Git PR，比 UI 操作日志可靠 100 倍
- 多集群部署不需要逐集群点 UI

**例外**：Transform Sets 是业务数据（被 Restore 引用），有 UI 编辑。

### ADR-005 双 URL OIDC 设计

**状态**：Accepted（v0.8.5）
**背景**：OIDC issuer URL 在 JWT 里作为 `iss` claim — 是用户浏览器跳转用的公开 URL（`localhost:30888/dex`）。但后端从 pod 内部访问这个 URL 不通（localhost 是 pod 自己）。

**决策**：拆成 4 个 URL 用途：
| URL | 谁用 | 指向 |
|---|---|---|
| AuthURL (`/auth`) | 浏览器跳转 | 公开 URL |
| TokenURL (`/token`) | 后端 code-for-token | 集群内 |
| JWKS URL (`/keys`) | 后端 JWT 签名验证 | 集群内 |
| Issuer URL | JWT iss claim | 公开 URL（不变）|

**关键 API**：`oidc.InsecureIssuerURLContext(ctx, publicIssuerURL)` 告诉 go-oidc "discovery 从内部 URL 拉，但 iss 验证用公开 URL"。

[v0.8.5 step 1 中我们花了半天调试这个](https://github.com/mars-zhangcong/supkube) — 不写下来下次还会踩。

### ADR-006 三种 Velero patch 类型分场景使用

**状态**：Accepted（v0.8.4）
**背景**：Velero ResourceModifier 支持 3 种 patch — JSON Patch / Merge Patch / Strategic Merge Patch。每种的失败行为不同。

**决策**：按冲突类型选最稳妥的 patch：

| 冲突 | Patch 类型 | 理由 |
|---|---|---|
| Service.spec.ports[N].nodePort | Strategic | K8s 知道按 `port` 字段 merge ports 数组 |
| Service.spec.clusterIP | Merge | Velero backup 可能已剥离该字段 — merge patch 缺失字段 silent no-op |
| PVC.spec.volumeName | Merge | Velero 100% 在 backup 时剥离 volumeName — JSON Patch remove 必失败 |
| Ingress.spec.rules[N].host | JSON Patch | Replace 操作，路径必然存在 |

**关键洞察**：JSON Patch RFC 6902 规定 `remove` 不存在的字段必报错。Velero backup 时会预剥离很多字段，所以"显然的" `remove` 经常失败。**默认用 Merge Patch (set null) 更安全**。

### ADR-007 cleanupBeforeRestore 解决 existingResourcePolicy: none 陷阱

**状态**：Accepted（v0.7.10）
**背景**：Velero 默认 `existingResourcePolicy: none` — 目标资源已存在则跳过。导致"就地恢复"几乎总是 no-op，用户报 phase=Completed 但数据没变。

**决策**：UI 提供"就地恢复"选项时，强制弹出警告 + 强制 checkbox 确认。后端拿到 `cleanupBeforeRestore=true` 时**先 kubectl delete 目标 ns 并轮询直到完全消失**（最多 60s），然后才创建 Velero Restore CR。

**保护清单**：refuse to delete `velero / supkube / kube-system / kube-public / kube-node-lease / default`。

### ADR-008 Restores 独立页废弃

**状态**：Accepted（v0.8.0）

随 ADR-002 一起。`/restores` URL 自动 redirect 到 `/activity?type=Restore`。前端代码 `views/Restores.vue` 暂时保留，v0.9 删除。

### ADR-009 用户视角术语与 CRD 名解耦

**状态**：Accepted（v0.5）

详见 §5.1。**任何新功能在写 UI 文案 / i18n key 前必须先决定"用户怎么称呼这个东西"**，CRD 名只允许出现在故障排查页面（YAML viewer）。

### ADR-010 Restore Drawer 用侧边抽屉而非全屏向导

**状态**：Accepted（v0.7.10）
**背景**：复杂的恢复表单（命名空间选择、Pre-flight、Spec 清单、existingResourcePolicy）需要给用户一个集中的输入界面。

**决策**：右侧 640px 宽抽屉（el-drawer，参考 Kasten），不是 modal 也不是新页面。

**为什么**：
- 抽屉保留了背景上下文（Restore Points 列表），用户能看到他在哪一行操作
- 模态会盖住一切；新页面让用户失去导航上下文
- Kasten 验证过这个模式适合此类工作流

### ADR-011 cluster fingerprinting for Local/Imported detection

**状态**：Accepted（v0.7.2）
**背景**：BSL 可以被多个集群共享，需要区分一个 Backup 是本集群创建的还是另一个集群同步过来的。

**决策**：用 Velero v1.10+ 自动添加的 `velero.io/source-cluster-k8s-gitversion` 等注解组合作为集群指纹。前端启动时取所有 Backup 里最频繁的指纹作为"本集群基线"，其它都标 Imported。

**简化假设**：假设这个集群的 Backup 数量 > 任何单个外部集群的同步数量（通常成立）。

### ADR-012 级联删除走 DeleteBackupRequest

**状态**：Accepted（v0.7.9）
**背景**：直接 `cl.Delete(backup)` 只删 K8s CR，不删 BSL 上的 tarball / CSI snapshot / PodVolumeBackup。Velero BackupSyncController 每 60s 扫 BSL，看到 BSL 上还有数据但 CR 没了，会自动重建 CR。

**决策**：UI 删除走 Velero `DeleteBackupRequest` CRD。Velero backup-deletion-controller 处理它：先删 BSL 数据 → 再删关联 VolumeSnapshot → 最后删 Backup CR 自身。

**用户教育**：UI 弹出删除确认对话框，明确列出"会被级联删除的内容"。

### ADR-013 详细错误通过 DownloadRequest 拉取

**状态**：Accepted（v0.7.11）
**背景**：Velero `Restore.status.errors` 只是数字。真正的错误消息存在 BSL 上的 `<name>-results.gz`。

**决策**：后端创建 `velerov1.DownloadRequest`，Velero 控制器填充 `.status.downloadURL`（短期预签名 URL），后端 HTTP GET + gunzip + JSON parse → 返回结构化的 `{errors: {namespaces: {"ns1": [...]}}, warnings: ...}`。

**Best-effort**：失败时还是返回 summary（错误个数），只是没有详细消息。UI 提示 "Could not fetch detail"。

### ADR-014 启动时迁移机制

**状态**：Accepted（v0.8.4）
**背景**：v0.8.2 引入了 Transform Set ConfigMap，结构有 bug（多 key 违反 Velero 校验）。v0.8.3 修复了结构，但部署后老的 ConfigMap 还在集群里。

**决策**：后端启动时跑 `migrateBrokenTransformSets()` — 扫所有带 SupKube label 的 ConfigMap，检测结构是否符合最新 schema，不符合就删。`seedBuiltinTransformSets()` 紧接着把内置模板用新 schema 重新创建。

**约定**：每次发版涉及"用户不可见但内部需要的清理"，在 seed 文件加一个 `migrateXxx()`。幂等 + 容错 + stdout log。

### ADR-015 polling cadence

**状态**：Accepted（v0.8.0）

```javascript
function tunePolling() {
  if (pollTimer) clearInterval(pollTimer)
  const anyRunning = actions.value.some(a => a.status === 'running')
  pollTimer = setInterval(fetchActions, anyRunning ? 5000 : 30000)
}
```

**理由**：
- 有 running action → 5s 轮询（用户在看实时进度）
- 全静态 → 30s 轮询（节省 API 调用）
- 不用 WebSocket / SSE：复杂度 ↑，K8s nginx ingress 兼容性问题，5s 足够

### ADR-016 ConfigMap-mounted nginx（不 baked into image）

**状态**：Accepted（v0.8.5）

详见 §7.2。**约定**：所有"运行时需要可配置但 SPA 本身不需要重 build"的东西都走 ConfigMap mount，不进 image。

### ADR-019 审计日志：K8s Events + stdout 双写

**状态**：Accepted（v0.8.5 step 4）
**背景**：审计是合规硬要求（SOC2 / GDPR / 客户内审）。需要"谁在什么时间对什么资源做了什么"的不可变记录。

**问题**：审计存哪里？候选方案：

| 方案 | 优点 | 缺点 |
|---|---|---|
| 自带 SQL/NoSQL DB | 查询灵活、长期保留 | 引入新依赖、运维一台 DB、备份 / 高可用 / 升级都要管 |
| ConfigMap append-only | K8s 原生、零额外依赖 | 1MB 上限、append race condition |
| 专用 PVC + JSONL 文件 | 简单、本地持久化 | backend 多副本时争用、灾难恢复要外部备份 |
| **K8s Events + stdout 双写** | **K8s 原生、kubectl 可见、stdout 给 SIEM** | **K8s Events 默认 1h TTL（短期）** |
| 接 Prometheus / 外部 SIEM 直接 | 工业级 | 客户必须已装 Prometheus / Loki |

**决策**：**K8s Events + stdout 双写**。

- **K8s Events**（events/v1，写到 supkube ns）：UI 短期查询。带 `supkube.io/audit=true` label 便于过滤。
- **stdout**（结构化 JSON 行，固定 `[audit]` 前缀）：客户的日志聚合管道（Loki / EFK / Splunk / Fluent Bit / Datadog ...）自动收集，作为**长期保留路径**。

**为什么是 K8s Events 而不是自带 DB**：
- 不增加运维负担（多了 DB 谁来备份、谁来扩容、谁来升级？）
- kubectl 用户也能 `kubectl -n supkube get events -l supkube.io/audit=true`
- TTL 由 kube-apiserver 控制，K8s 原生设施

**为什么是 stdout 而不是直接对接 SIEM SDK**：
- SIEM 种类太多（Splunk / Datadog / Sumo / Elastic / Loki / Wasabi / 自建 ELK ...）
- 客户已经在做 stdout → SIEM 的日志管道（K8s 标准实践）
- 我们只负责"生成可被采集的格式"，**不负责"采集 + 转发"**

**取舍**：
- ✅ 零额外依赖
- ✅ 业内标准 pattern（K8s 自己的 audit log 也是写到文件 + 外部采集）
- ❌ UI 仅显示近期（默认 1h）—— 但我们把这个限制写进 USER_MANUAL，客户清楚预期
- ❌ K8s Events 的 schema 限制了字段灵活性 —— 通过 annotations 解决，但 kubectl get events 默认不显示 annotation；UI 解决这个

**实现亮点**（写在 `internal/auth/audit.go`）：
- middleware 在 RBAC 之后跑，能看见每个请求是允许（2xx）还是被拒绝（4xx）
- 读取请求 body 来提取 namespace 字段（best-effort：试 includedNamespaces → targetNamespace → body.namespace → body.name）
- Event 的 InvolvedObject 用 SupKube 内部的资源命名（Backup / Restore / Schedule），不污染 Velero 的 InvolvedObject 命名空间
- sanitizeLabel() 把任意 email 转成 K8s label 合规字符串（63 字符限制 / 字母数字+`.-_`）

### ADR-018 RBAC 3 角色模型 + 集中权限表

**状态**：Accepted（v0.8.5 step 3）
**背景**：v0.8.5 step 1+2 解决了"用户是谁"（OIDC），但**所有人都是 admin**。多人多团队场景需要权限分层。

**决策**：

1. **固定 3 角色**：`admin > editor > viewer`。**不**做"自定义 role + permission set"那一套（K8s 的 RBAC 已经够复杂了，再造一层只会让客户痛苦）。
2. **OIDC groups claim → 角色映射**通过 Helm `auth.rbac.bindings` 配置。**不**做 UI 管理（参见 ADR-004）。
3. **后端集中权限表**（`internal/auth/rbac.go::permissionTable`）：每个 `(method, path)` 对应一个最低 role。中间件统一查表。
4. **命名空间细粒度**仅 editor 角色需要：写操作时 handler 内部用 `RequireNamespaceAccess()` 校验。admin / viewer 跳过 ns 检查。
5. **fail-closed**：endpoint 不在权限表 = 拒绝（403 + "endpoint not in RBAC table"）。强制开发者新加路由时也加权限。

**为什么集中权限表 vs 每个 handler 注解**：
- ❌ 注解模式：30 个 handler 各自写 `auth.RequireRole(...)`，审计员要在 30 处确认权限
- ✅ 集中表：一处看完所有权限，CI 可以 lint "新路由必须在表里"
- ✅ 业务 handler 干净（只关心业务逻辑，权限是中间件管的）

**为什么 ns 细粒度做在 handler 内部 vs 中间件**：
- editor 的 ns 范围检查需要**知道操作影响哪些 ns**
- 不同 endpoint 的 ns 出现位置不同：URL path (`/backups/:name` 需要 lookup CR) / body (`POST /backups` 看 IncludedNamespaces) / mapping (`POST /restores` 看源 + 目标)
- 想做成"中间件 + 每路由配置 ns 提取函数"过度抽象，6 个 mutating handler 加 6 行 `RequireNamespaceAccess()` 调用更直接

**取舍**：
- ✅ 简单：客户读一遍 §12 就能配
- ✅ K8s-native：与 OIDC groups claim 对齐
- ❌ 不支持 "Operator A 能管 ns-X 的 Backup 但不能管 Restore"（资源 × 动词级别权限）—— 一般中型企业够用，超大客户会要求自定义 role，v0.10 再考虑

### ADR-017 OIDC connector secrets 走 K8s Secret + 环境变量替换

**状态**：Accepted（v0.8.5 step 2.5）
**背景**：客户的 values.yaml 通常在 Git。OAuth client secret 明文进 Git 是合规事故。

**决策**：Dex 配置支持 `$VAR_NAME` 环境变量替换（Dex 在 config 加载时调用 `os.ExpandEnv`）。Helm chart 暴露两种注入方式：

| 字段 | 用途 |
|---|---|
| `auth.dex.envFromSecrets[]` | 整个 K8s Secret 的所有 key 注入为 env var |
| `auth.dex.env[]` | 单独 env var → 特定 Secret key 引用 |

Connector 配置写 `clientSecret: "$GITHUB_CLIENT_SECRET"`，Dex 在加载 config.yaml 时把 `$GITHUB_CLIENT_SECRET` 替换为实际值。

**代价**：
- Secret 轮换需要 `kubectl rollout restart deploy/supkube-dex`（env var 是容器启动时一次性写入）
- v0.9 计划集成 reloader.stakater.com 注解做自动 reload

**为什么不直接 mount Secret 到 Dex 文件系统**：
- Dex 不支持从文件读 client_secret，所有 connector 配置必须在 config.yaml 里
- env 替换是 Dex 原生支持的 ergonomic 路径
- mount Secret 到文件还得自己写 entrypoint 脚本拼 config，复杂度反而高

### ADR-020 非 OIDC 认证 fallback：Bearer Token + Basic Auth

**状态**：Accepted（v0.8.5 step 6）
**背景**：v0.8.5 step 1-4 把 OIDC + Dex 做完了，覆盖**人类浏览器登录**。但还有两类客户被挡在门外：

1. **机器调用**：CI/CD 流水线、Terraform/Pulumi、运维脚本。给它们走 OAuth2 code exchange 是反模式 — 没有浏览器、没有交互。
2. **气隙 / 反向代理**：客户内网装了 mod_auth_kerb / SAML2Apache，前端代理已经把人认了；让 SupKube 重新走 OIDC 是双重劳动，且气隙环境压根没法接外部 IdP。

**决策**：在 OIDC 之外**并行支持**两个 fallback，共用同一个 `/api/v1/*` 表面，三种 header 自动分发：

| Header 形态 | 路径 | 用途 |
|---|---|---|
| `Bearer <opaque>` | sha256 lookup → 内置 token 表 | CI/CD / 脚本 |
| `Bearer <jwt>` | OIDC verifier（JWKS）| 人类浏览器登录 |
| `Basic <b64>` | bcrypt 比对 htpasswd | 反代/气隙环境 |

分发逻辑（`auth.Middleware()`）：

```
Bearer 且 不像 JWT (≠ 3 段 dot-separated) → token 表
Bearer 且   像 JWT                          → OIDC verify
Basic                                        → htpasswd bcrypt
```

**Token 表设计**：
- 配置位置：`auth.staticTokens.tokens[]`（Helm values）
- **存 hash 不存明文**：plaintext 由管理员本地生成（`openssl rand -hex 32`），sha256 才进集群。`helm get values` 泄漏 ≠ token 泄漏。
- 比较走 `crypto/subtle.ConstantTimeCompare` 防时序探测
- 每个 token 自带 `role` + `namespaces`，**绕过** `RBAC bindings` 表 — token 是基础设施配置，不能被普通管理员通过 RBAC UI 动
- Token 不入审计日志原文，只记 `name` 字段

**htpasswd 设计**：
- 配置位置：管理员手动建一个 Secret（含 `htpasswd` key），`auth.basic.secretName` 指向它
- backend mount 到 `/etc/supkube/htpasswd`，启动时解析一次
- **只接受 bcrypt 行**（`$2a$` / `$2b$` / `$2y$`），MD5/SHA1 启动时打 warn 跳过 — 不让"看起来配了密码但其实没保护"
- 用户名走**同一张** `auth.rbac.bindings` 表（按 `user:` 字段匹配）—— 这样撤销/重新授权不用碰 htpasswd 本身

**为什么 token 不走 bindings 表而 htpasswd 走**：
- Token = 机器身份，**生命周期与权限绑定**（吊销 = 删 token）。permissions inline 让 Helm 一处就能审计全部 machine identity。
- htpasswd = 人类身份，**人不变但权限会调整**（小李从 viewer 升 editor）。走 bindings 表就和 OIDC 路径一致，不需要双重维护。

**为什么不支持 K8s ServiceAccount token / TokenReview**：
- 评估过：好处是利用 K8s 既有的 SA + RoleBinding。坏处是 SupKube 的 role 模型（admin/editor/viewer）和 K8s RBAC（verb × resource）不同形，硬映射会出现"SA 是 cluster-admin 但 SupKube 里是 viewer"的认知失调。
- 另：TokenReview 走 API server，每个请求一次 round-trip，p99 加 ~5ms。本地 hash lookup 是 ns 级延迟。

**取舍**：
- ✅ 三种方式互相独立，关掉其中两个不影响第三个
- ✅ 不引入状态存储（无 DB、无 Redis），可重启随时来
- ✅ Token revocation 走 GitOps（改 values → helm upgrade）—— 审计可追溯
- ❌ 无运行时签发 token 的 API：那需要持久化撤销列表 + 审计签名，溢出 v0.8 范围。v0.10 在 RBAC UI 一并考虑。
- ❌ htpasswd 修改要重启 backend pod：v0.8.6 计划加 SIGHUP / fsnotify 热加载

**用户文档**：USER_MANUAL §14 — 含 token 生成 / htpasswd 生成 / 常见踩坑表。

### ADR-021 备份组成元数据：dataPath + 卷字节 + tarball 字节（v0.8.6）

**状态**：Accepted（v0.8.6）
**背景**：用户提出三个被搁置已久的问题 ——「这个备份是全备还是增量？多大？压缩去重比多少？」。Velero 的 Backup CR 一个都不答；要给出靠谱答案得跨 4 个数据源：

1. Backup CR 本身（spec + status）
2. VolumeSnapshotContent CR（CSI 快照大小）
3. PodVolumeBackup CR（文件系统备份字节）
4. BSL 对象存储（tarball 大小）

**决策**：在 backend 加一层 **enrichment**，把上述四源汇成 `supkube` sidecar 挂到 Velero Backup 响应上。后端做聚合，前端只渲染。

#### 数据形态

```jsonc
{
  "metadata": { ... },  // velerov1.Backup 原样
  "spec":     { ... },
  "status":   { ... },
  "supkube": {           // ← 新增 sidecar，跟 Velero schema 完全独立
    "dataPath":     "csi-snapshot",   // 4 值枚举
    "volumeCount":  2,
    "volumeBytes":  6442450944,        // 卷大小总和（语义依路径）
    "tarballBytes": 18432,             // 资源清单字节
    "tarballError": ""                 // BSL 不可达时的人类可读原因
  }
}
```

**为什么挂 sidecar 而不是改 Velero 字段**：

- ❌ 直接往 `metadata.annotations` 写自己的字段 → 是写操作，每次 List 调用都改集群状态，巨大权限放大
- ❌ 复用 Velero status 字段 → 会被 Velero controller 覆盖
- ✅ 响应层 enrich → 纯只读、零集群副作用、未来 v0.9 加 `compressionRatio` / `dedupRatio` 等字段直接往 sidecar 里塞，不破坏 Velero schema 兼容

#### 性能：单次响应的外部调用次数

ListBackups 渲染一页（100 行）只发：
- 1 次 `kubectl list VolumeSnapshotContent`
- 1 次 `kubectl list PodVolumeBackup`
- 1 次 S3 `ListObjectsV2`（prefix=backups/, 60s 缓存内零调用）

不是 100 行 × 4 数据源 = 400 次。靠两个机制：

- **批量 join in memory**：`collectVolumeMetadataBatch()` 一把 List 所有 VSC/PVB，按 `velero.io/backup-name` label 在内存里建 map
- **BSL 列表 60s 缓存**：第一次访问触发 ListObjectsV2 拿全 bucket 的 tarball 大小，后续 60s 内的 ListBackups / GetBackup 命中缓存

GetBackup（N=1）放弃 batch 优化直接读，省一次 list 的资源量是值得的。

#### 数据路径分类器：信 status 不信 spec

```go
spec.snapshotMoveData      = true                 → data-mover
spec.defaultVolumesToFsBackup = true              → filesystem
spec.snapshotVolumes = true 且 status.csiCount>0  → csi-snapshot
其它                                              → metadata-only
```

**关键设计**：CSI 分支要求 `status.CSIVolumeSnapshotsCompleted > 0`。声明了 CSI 但实际一个快照都没建出来（PVC 在不支持 snapshot 的 StorageClass）的备份判为 metadata-only —— spec 是用户**想要**什么，status 是用户**得到**了什么。给出 metadata-only 比 csi-snapshot 更诚实。

#### CSI 大小的 caveat

`VolumeSnapshotContent.status.restoreSize` = PVC 声明容量。100GiB PVC 实际占用 2 GiB 也是显示 100 GiB。这是 K8s CSI API 的限制 —— 没有任何标准字段表达"actual data used"。我们：

- 在大小数字下方加一行红色 caveat 提示用户
- 推荐想要精确去重后字节就开 Data Mover 或 FS backup

**为什么不调用 CSI driver 的 capacity 接口**：CSI VolumeSnapshot 接口没定义 "GetActualUsage"，得 SSH 进 driver 容器跑驱动专属脚本。不通用。

#### BSL S3 客户端

- v0.8.6 只支持 `provider: aws`（S3 / MinIO / 腾讯 COS / 阿里 OSS 等都是 S3 协议）
- 凭据从 Velero 引用的 K8s Secret 拿，解析 `[default]` ini 格式
- endpoint override + path-style 由 BSL `spec.config["s3Url"]` / `["s3ForcePathStyle"]` 控制
- v0.9 添加 GCS / Azure provider（拉 google-cloud-storage + azblob SDK）

#### 留给 v0.9 的洞

- 压缩比 / 去重比 → 需要 Kopia 集成（`kopia repository status` 拉仓库物理占用 + `kopia snapshot list` 拉单次逻辑大小，相除得出）
- PV 真实占用（非声明容量）→ 需要 Kanister-style pre-backup hook 抓 `df` 输出
- 物理增量（"这次备份比上次多写了多少"）→ 跟压缩去重比同根，需 Kopia 集成

**用户文档**：USER_MANUAL §15 — dataPath 四值含义 / 大小语义 / 暂未提供的指标清单。

### ADR-022 跨集群 DR：Data Mover 暴露 + Azure provider 落地（v0.8.7）

**状态**：Accepted（v0.8.7）
**背景**：用户提了一个真实场景 ——"本地 docker-desktop 的 test-app 备份到 Azure Blob，还原到 AKS officialwebsite"。这暴露 v0.8.6 之前的三个产品缺口：

1. 用户没法在 UI 选 Data Mover 模式（只能手动 kubectl edit）
2. BSL 表单只支持 AWS-flavored S3，Azure 必须手写 YAML
3. v0.8.6 的 tarball-size 跳过 Azure（误以为 BSL 不可用）

**决策**：v0.8.7 把这条 DR pipeline 做到 UI **全可视化**。三个交付：

#### 1. Policy 表单：2-pill 卷模式 → 4-pill 数据路径

原 UI（v0.7 引入）只有 Filesystem / CSI 二选一，对应 `defaultVolumesToFsBackup` / `snapshotVolumes`。问题：Data Mover 是层叠在 CSI 上的第三种模式，原 UI 表达不了；Metadata Only 也藏在"两个都没选"的隐式状态里。

新 UI 把 Velero 的 3-flag 三元组（snapshotVolumes / defaultVolumesToFsBackup / snapshotMoveData）反向映射为 4 个**互斥**选项：

```
csi-snapshot   → (true,  false, false)   单集群 CoW，最快
data-mover     → (true,  false, true)    CSI + Kopia 上传，跨集群推荐
filesystem     → (false, true,  false)   Kopia 走文件系统，CSI 不可用时退路
metadata-only  → (false, false, false)   只备 YAML
```

**为什么不简单加第三个 pill ("Data Mover")**：那会让"开了 CSI + Data Mover"在 UI 上变成两个并列勾选，但物理上 Data Mover **依赖** CSI（先建快照才上传）。叠加表达 vs 互斥表达 —— 前者让用户产生"我可以只开 Data Mover 不开 CSI"的错觉，触发 Velero 静默忽略。互斥 4 选 1 直接消灭这个错误状态。

**ns 能力检查覆盖面扩大**：原本只 CSI 路径触发 PVC StorageClass 检查；现在 Data Mover 也走 CSI 底层，所以两个路径都触发。Filesystem / Metadata 跳过检查。

**向后兼容读**：`hydrateFormFromSchedule()` 先读新注解 `supkube.io/data-path`，没有就降级读老的 `supkube.io/volume-mode`，再没有就根据 spec 三元组反推。老策略导入到新 UI 不会"看起来变了"。

#### 2. BSL 表单：Provider dropdown 起作用

原 dropdown 已经有 `azure` 选项但**只是装饰** —— 提交还是按 AWS 字段发请求。v0.8.7：

- Provider 切到 Azure 时**整套字段切换**：bucket → Container；region/endpoint/accessKey/secretKey 隐藏；显出 storageAccount / storageAccountKey / resourceGroup / subscriptionId
- Backend `CreateStorageLocationWithSecret` 按 `req.Provider` switch：
  - `aws` → 生成 INI-format credential（`[default]\naws_access_key_id=...`）+ 设 `config.s3Url` / `s3ForcePathStyle`
  - `azure` → 生成 Velero Azure 插件期望的 `AZURE_STORAGE_ACCOUNT_ACCESS_KEY=...\nAZURE_SUBSCRIPTION_ID=...\nAZURE_CLOUD_NAME=AzurePublicCloud\n` + 设 `config.resourceGroup` / `storageAccount` / `subscriptionId`
- 共用同一个 CRUD 表面：BSL CR 仍是 `BackupStorageLocation`，Secret 仍叫 `supkube-bsl-<name>-credentials`，label 都带 `supkube.io/managed-by=supkube`，删 BSL 时统一清理

**为什么 v0.8.7 只支持 storage-account-key 不上 AAD service-principal**：AAD 需要 5 个字段（tenant/sub/RG/clientId/clientSecret）+ 凭据轮换语义，UX 复杂度跳一档；而 storage-account-key 是 Azure 文档里推荐给"运行 Velero 的一次性场景"的路径。v0.9 再加 AAD。

**Validation 前移**：Azure 必填字段（storageAccount / storageAccountKey / resourceGroup）在 backend 入口直接 400，而不是让 BSL CR 创建出来然后 Velero 在 reconcile 时报错。错误就近，用户不需要看 events。

#### 3. Azure tarball-size：azblob SDK 走第二路径

v0.8.6 的 `backup_meta_bsl.go` 只有 AWS 路径。v0.8.7 拆分：

```
hydrateBSLSizes(bslName)
  ├─ provider=aws   → hydrateBSLSizesAWS  (原 v0.8.6 代码)
  └─ provider=azure → hydrateBSLSizesAzure (新增, azblob SDK)
```

Azure 路径用 `github.com/Azure/azure-sdk-for-go/sdk/storage/azblob`：

1. 从 BSL credential Secret 拿 storage account key（自定义 KV 解析，因为 Velero 的 Azure 凭据**不**是 INI 格式而是裸 key=value 行）
2. `azblob.NewSharedKeyCredential` + `azservice.NewClientWithSharedKeyCredential` 建 client
3. `containerClient.NewListBlobsFlatPager(prefix="backups/")` 拉清单
4. 同 AWS 路径走 60s per-BSL 缓存

共享的部分：
- `tarballSizes` cache shape
- `bslBucketAndPrefix` helper（Azure 没有 prefix 的语义，但接口对齐）
- 错误消息 trim 逻辑（裁第一行 + 120 字符）

不共享的部分：
- 完全不同的 SDK pager 形状（S3 是 `Paginator.NextPage`，azblob 是 `Pager.More() / NextPage()`）
- 完全不同的 blob 描述（S3 `obj.Size` 是 int64，azblob `Properties.ContentLength` 是 `*int64`）

#### 关键边界

- **Velero 自己负责跨集群 backup 同步**：SupKube 不干预 BackupSyncController；两边集群配同一个 BSL，Velero 60s 同步一次。SupKube v0.7 已加的"Source: Local / Imported" chip 自动正确显示
- **SupKube 不管 Velero 安装**：用户需要在两边集群独立 `velero install`，包含 Azure 插件和 node-agent。文档（USER_MANUAL §16）给完整 helm 命令
- **node-agent 不在 SupKube Helm chart 里**：跟 Velero 装一起；SupKube 在 Policy 表单里 only 检测和提示（"Data Mover 需要 node-agent"）

#### v0.8.7 不做的事

- **Hub-Spoke 多集群 UI**：单个 SupKube 实例还是只管一个集群。跨集群操作要切 UI 实例。这是 v0.10 的范围
- **运行时 BSL 校验**：用户点"Create"后 BSL phase 是不是 Available，由 Velero 自己异步判（已有 phase 字段，UI 显示就行）
- **CSI VolumeSnapshot Azure 集成**：要做这个得装 azure-disk CSI driver + 注册 VolumeSnapshotClass。超出 SupKube 范围

**用户文档**：USER_MANUAL §16 —— 含完整 helm 命令、UI 配置截屏式说明、排障表。

### ADR-023 第三方集成的版本管理 + 镜像分发策略

**状态**：Accepted（v0.8.7.2）
**背景**：SupKube 不只是一个 Go binary —— 它依赖 Velero（含 N 个 ObjectStore 插件）、Dex、CSI snapshot-controller、未来还有 Prometheus / Grafana / Kanister / cert-manager。这些组件分别有自己的版本节奏、自己的 helm chart、自己的容器镜像。客户问"我装 SupKube 0.8.7 需要哪些镜像"时，原本只能口头答 —— 错过一个就翻车（见 §6.2 的 node-agent 案例）。

**决策**：建立 **整合三件套**：

1. **`INTEGRATION.md`**（根目录） —— 单一可读文档，给交付工程师 + SRE + 合规
2. **`integrations/images.yaml`**（机器可读 YAML） —— 单一事实来源，列出每个版本所有镜像
3. **`hack/*.sh`** —— 自动化脚本，从 YAML 派生出：mirror（推私有 registry）/ airgap（打离线包）/ verify（检查集群里到位情况）

> 三个文件的约定：升级任何组件，**必须**改 YAML（脚本会自动跟上） + 改 INTEGRATION.md（人读 source of truth）。CI 校验两者一致。

#### Tier 分级

每个组件标三个属性：

| 属性 | 含义 |
|---|---|
| `category` | `core` / `optional` / `planned`（已交付 / 可选不必装 / 路线图上）|
| `tier`     | `required` / `optional` / `test-only`（生产必装 / 看场景 / 测试用，**生产严禁**） |
| `supkubeVersion` | 该镜像版本属于 SupKube 哪个 release 的兼容矩阵 |

#### 镜像分发：3 种模式

| 模式 | 客户特征 | 工具 |
|---|---|---|
| 联网直拉 | 公有云、可达 docker.io / quay.io | 无 — Helm 默认 |
| 半离线 mirror | 客户有 Harbor / Nexus / Artifactory，要求所有镜像 IP 自主可控 | `hack/mirror-images.sh <prefix>` |
| 气隙离线 | 客户网络物理隔离，需 sneakernet | `hack/airgap-bundle.sh <tar>` |

mirror / airgap 都读 `images.yaml`，自动包含 `category != planned` 的镜像（避免把还没用的镜像也搬运）。

#### 工具选型：crane > skopeo > docker

`mirror-images.sh` 按下面顺序探测 + 用：

1. **crane**（首选）：纯 Go 二进制，无 daemon，blob 直传不落地，多 arch manifest 自动处理
2. **skopeo**：广泛部署，能处理 OCI / Docker 双格式
3. **docker pull + tag + push**：兜底，但需要本地 daemon

不强求客户装某一个 —— 哪个有用哪个。CI 上我们用 crane。

#### 为什么 SupKube 不自动装 Velero

考虑过把 Velero 作为 SupKube Helm chart 的 dependency（subchart）。最终拒绝：

- ❌ 客户 60%+ 的集群**已经装了 Velero**（K10 / Trilio / 上游 chart），双装会争 BSL CR 抢主，灾难
- ❌ Velero 的 helm chart 升级节奏和 SupKube 不同步，硬绑死会拖累 SupKube 节奏
- ❌ Velero plugin 是 init container 注入式，**不是** chart values，subchart 装不出我们的多 provider 组合
- ✅ 拆开后客户拥有 Velero 的安装路径选择（CLI / helm / yaml / Operator），SupKube 只验证它在
- ✅ `verify-cluster.sh` 在客户首次部署前跑一次，把缺组件的问题前移到部署阶段

**唯一例外**：Dex 是和 SupKube Helm chart 一起装的，因为 Dex 实际是 SupKube 的 OIDC 配置工具，不应该让客户单独管。

#### 验证脚本 `verify-cluster.sh` 的角色

不只是"运行时健康检查"，更是**部署 gate**。CI 流水线在客户环境部署完 SupKube 后跑一遍，返回非零就阻止后续业务上线。它检查：

- SupKube backend/frontend 副本就绪
- Velero deployment 存在 + 必要 plugin 装载了
- node-agent DaemonSet 全 Ready（如客户要 Data Mover）
- snapshot-controller 在跑（多种 label 探测，避免假阴性）
- 所有 BSL Phase=Available

每个 fail 直接给排查路径，不是"something went wrong"。

#### 取舍

- ✅ 客户能一眼看清"我装了什么、装了什么版本"
- ✅ 升级路径明确：改 YAML → 跑 mirror → 改 Helm values → verify-cluster → 上线
- ✅ 气隙友好：一个 tarball + 一个 SHA + README 就能交付
- ❌ Velero 不绑定 = 客户需要自己装一次 —— 用 `verify-cluster.sh` 在部署前就告诉客户缺什么，缓解
- ❌ 镜像 digest 还没全锁 —— v0.8.7.2 只用 tag；生产 release 计划全锁 digest（防上游覆盖 tag）

**追溯**：本 ADR 起因于一次具体故障 —— 客户 cluster 装好 Velero 但漏装 Azure 插件，BSL 报 `unable to locate ObjectStore plugin named velero.io/azure`，调试 30 分钟才定位（v0.8.7 部署阶段）。新流程下，`verify-cluster.sh` 会在 BSL 创建前就报 "Azure plugin NOT loaded"。

---

#### v0.8.7.2 修订：BSL identity 字段实际上是 editable

v0.8.7.1 我们把 Bucket/Container + Storage Account 设成 Edit 中 disabled，理由是"它们是 BSL 身份的一部分，credentials 跟它绑定"。**这个判断错了**：

- AWS：access key 是 **IAM 身份**的，不绑定具体 bucket。同一对 key 能访问 IAM 策略允许的任何 bucket。改 bucket 名字 = 改配置，credentials 不变
- Azure：storage account key 是**账户级**的。同一账户下任意 container 都用同一个 key 访问。改 container 名字 = 改配置，key 不变

真正绑定 credentials 的只有 **Azure Storage Account**（key 是账户级 → 换账户需要换 key）。其它都误判。

参考 Kasten K10 的 Location Profile Edit 页：所有字段（Storage Account / Access Key / Container）全可编辑，顶部一行 "Please re-enter your account credentials"。v0.8.7.2 直接照搬这个 UX。

**校验**移到 Save 时：
- bucket 改了 → 不需要重输 credentials（同 IAM/同账户）
- storage account 改了 + 没填新 key → 后端 400 拒绝（避免 silent break）
- 前端显示橙色 banner 实时提示用户

**教训**：UX 上"不让用户碰高危字段"看似安全，实际是降低产品能力 —— 用户的真实场景是"我在 Azure 门户建了新容器，想把现有 BSL 切过去"，原 v0.8.7.1 的 lockdown 把这条路堵死了，逼用户走"删 BSL 重建"，但 BSL 删除会触发 backup tarball 关联清理逻辑（DeleteBackupRequest），可能误删历史数据。**identity field 可编辑 + 验证就近**是更安全的设计。

#### v0.8.7.3 修订：BSL 元数据聚合补 DataUpload 路径

v0.8.6 的 `collectVolumeMetadata` 只统计 `VolumeSnapshotContent.restoreSize`（CSI 路径）和 `PodVolumeBackup.totalBytes`（Filesystem 路径），**漏掉 `DataUpload.totalBytes`（Data Mover 路径）**。v0.8.7 的 Data Mover UI 上线后，跑出的备份在 UI 里 volumeBytes 永远显示 0 —— 哪怕 Azure Blob 里实际有 32 MB Kopia 数据。

漏的原因不是写代码时疏忽，而是**测试环境没装 node-agent**，整个 Data Mover 路径压根没跑通过，所以 v0.8.6 单元测试里只有 CSI + PVB 两种 fixture。第一次 e2e Data Mover 测试做完才暴露。

修复涉及三处：
1. `internal/k8s/client.go`：注册 `velero.io/v2alpha1` scheme（DataUpload/DataDownload 在这个 group，不是 v1）
2. `internal/api/v1/backup_meta.go`：单点 + 批量两个聚合函数都加第三段 DataUploadList 求和
3. `supkube-helm/.../rbac.yaml`：ServiceAccount 加 `datauploads`/`datadownloads`/`backuprepositories`/`podvolumebackups` 的 list/get/watch（v0.8.6 漏权限，list 失败被 `if err == nil` 静默吞）

**教训**：测试覆盖不只是代码覆盖率，**集成路径**也要全覆盖。v0.9 计划在 CI 流水线加 minikube + 装 node-agent 的 e2e 测试，确保所有三条 path 都跑过。

第三点的 RBAC silent swallow 是更普遍的问题 —— SupKube 后端有 5+ 处 `if err == nil` 模式，client.List 失败时不抛错只返回空。v0.9 计划统一改成把 err 透传到响应 sidecar（`volumeMetaError`，类比 v0.8.6 的 `tarballError`）。

#### v0.8.7.5 修订：Data Path 与 Action 强耦合 + L1/L2 双列布局

**问题**：v0.8.7 第一版的 Action toggle（L1 Snapshot vs L2 Snapshot+Export）和 Data Path 单选（CSI/Mover/FS/Meta）是**两个独立 widget**。用户可以选 "L2 + CSI Snapshot"，UI 显示 "Snapshot + Export"，但 `collapseToVelero` 生成的 Velero spec 是 `snapshotVolumes:true, snapshotMoveData:false` —— **没有 Export**。沉默的不一致，用户拿到的备份没数据上对象存储，从前端没法察觉。

**原因**：L1/L2 和 Data Path 在产品语义上是耦合的（L1=不导出，L2=导出），但 Velero 的 spec 是 `(snapshotVolumes, defaultVolumesToFsBackup, snapshotMoveData)` 三元组，每个独立。UI 没把这个耦合外显，让用户能选出不一致的组合。

**v0.8.7.5 修法**：

1. **双列布局**：Data Path 改成左右两列，左列 "Snapshot only (L1)"，右列 "Snapshot + Export (L2)"。两个 dataPath 值各归一列：

   ```
   Left column (L1):           Right column (L2):
     📸 CSI Snapshot              🚚 Data Mover
     📋 Metadata Only             📁 Filesystem
   ```

2. **Action 决定哪列可点**：`createForm.export.enabled === false` 时（L1）右列整体灰化 + 按钮 disabled；反之亦然。视觉上左/右框各加一条 4px 蓝色 ring 表示"当前激活列"。

3. **`setDataPath(value)` 守门**：所有按钮 click 不再直接 `v-model="dataPath"`，而是经过 setDataPath 函数。函数检查 value 属于哪一列、当前 L1/L2 是哪个，不匹配直接 return —— 即使有人用 DevTools 改 v-model 也无效。

4. **`migrateDataPathOnActionChange` 自动迁移**：watch `createForm.export.enabled` 变化，按"最近邻"规则改 dataPath：

   ```
   L1 → L2:   csi-snapshot  → data-mover    (都用 CSI 底层)
              metadata-only → data-mover    (回退默认)
   L2 → L1:   data-mover    → csi-snapshot  (剥掉 Kopia 上传)
              filesystem    → metadata-only (FS 没拍快照，迁回 metadata)
   ```

   迁移规则的设计原则：**保留用户意图的最大公约数**。L1 → L2 时如果用户原本选了 CSI，他想用 CSI 这个事实保留下来；L2 → L1 时如果他原本用 FS，FS 走 Kopia 不拍快照 → L1 没合理对应，迁到 metadata-only。

**取舍**：
- ✅ UI 状态和 Velero spec 双向一致，不可能选出非法组合
- ✅ 视觉上 L1/L2 vs Data Path 的关系一目了然
- ✅ 自动迁移避免"我改了 Action 但忘了同步改 Data Path"的二次配置错误
- ❌ 4-pill grid 变成 2x2 双列后占用垂直空间略多（+ 60px）— 用 grid-template-columns 控制了宽度，没影响 drawer

**Velero spec 真值表**（修后保证）：

| Action | Data Path | snapshotVolumes | defaultVolumesToFsBackup | snapshotMoveData |
|---|---|---|---|---|
| L1 | csi-snapshot | true | false | false |
| L1 | metadata-only | false | false | false |
| L2 | data-mover | true | false | **true** |
| L2 | filesystem | false | **true** | false |

不在表里的组合（如 L1+data-mover、L2+csi-snapshot）UI 无法触发。

**追溯**：本次修订起因于用户对话中的一个直觉问题 ——「我选了 L2，为什么 Data Path 还是单选？」。其实是 UI 让 L1/L2 和 Data Path 看起来独立可选，触发了用户的产品逻辑直觉但实际后果是沉默不一致。**用户的"奇怪"问题往往是产品 bug 的最早信号** —— 这次没用一次 GitHub issue + 复现步骤就抓住了。

#### v0.8.7.6 修订：Custom 频率改 On Demand + 真按暂停语义

**问题**：v0.7 Policy 表单的 Backup Frequency 第六个 pill 叫 "Custom"，意图是"自定义 cron"。但代码里 `FREQUENCY_TO_CRON.custom = '0 * * * *'`（hourly），点 Custom 实际把 cron 改成了每小时跑。配套的 `el-input` cron 字段虽然存在，但很多用户根本没注意到它，以为 Custom = "我自己定哪天跑"，结果 cron 静默落到 hourly。

**用户场景实测**：用户在 Edit 表单点 "Custom"，**预期**是"这个策略只在我手动触发时跑"，**实际后果**是 schedule 变成 `0 * * * *`，策略**每小时**自动跑一次。直到对方在另一次对话发现备份越来越多才察觉。

**根因**：UI 标签和实际语义错位 + 多义概念混在一个 pill 里：
1. "Custom cron"（让用户写自定义 cron 表达式）
2. "On Demand"（不自动跑，只手动触发）

这是两个独立产品概念，v0.7 用一个 pill 同时混了两者，再加上默认 cron 落到 hourly，导致**沉默地变更行为**。

**v0.8.7.6 修法**：

1. **Custom → On Demand**：i18n 字符串改名；新 key `policies.frequencyOnDemand`（en: "On Demand"，zh: "仅按需"）
2. **加 `paused` 字段到表单状态**：`createForm.paused` boolean，默认 false
3. **`selectFrequency('ondemand')` 设 `paused = true`**：其他 preset 同时设 `paused = false`（允许反向切换）
4. **Velero spec.paused 真正起作用**：`collapseToVelero` 把 paused 透传到后端；`CreateSchedule` 把它写入 `velerov1.ScheduleSpec.Paused`；`PatchSchedule` v0.8.5 step 5 已支持
5. **去掉那个被埋没的 cron 输入框**：用户场景不需要、又是 bug 源头，删；自定义 cron 表达式留给 v0.9 的 "Advanced Frequency Options" 单独做
6. **黄色 notice 解释状态**：选 On Demand 时下方显示"策略已暂停 — 不会按时刻自动触发"

**Hydration 反向也匹配**：

```
schedule.spec.paused == true                 → frequency = 'ondemand'
schedule.spec.paused == false + cron 匹配预设 → 对应 preset
schedule.spec.paused == false + cron 不匹配  → fallback 到 'daily'
                                              （v0.9 会改成自定义 cron 编辑）
```

这样在 Edit 一个已经暂停的策略时，UI 正确显示 "On Demand"；从 On Demand 切回 Daily 时 paused 重新设为 false，schedule 重新激活。

**Velero 语义验证**：`spec.paused=true` 时 Velero Schedule controller 在 reconcile 期完全跳过 cron firing，无论 cron 是什么。所以即使 cron 字段保留为 daily / hourly，也不会有 backup 被创建。**用户手动点 Run Once 仍然走 RunScheduleOnce 路径**，从 paused 状态下也能触发一次性备份（这是 Velero v1.15+ 的明确支持行为，不是变通）。

**取舍**：
- ✅ 名字符合用户直觉（Kasten K10 用的就是 "On Demand"）
- ✅ 语义可预测（"我点了 On Demand 它就不再自动跑"）
- ✅ 失误成本归零 —— 用户点错也不会沉默改成每小时跑
- ❌ 去掉了"用户写自定义 cron 表达式"的入口 —— 9 成用户从来没用过，剩下 1 成等 v0.9 的 Advanced toggle

**教训**：

1. **默认值是 UX 决策不是工程兜底**。`FREQUENCY_TO_CRON.custom = '0 * * * *'` 这种"先随便填一个免得空"的代码，掩盖了产品概念不清的本质。空值 / 显式 null / 强制用户选择都比"偷偷塞个看似无害的默认"安全
2. **同一个 pill 不要承载两个语义**。Custom = "我写 cron" 和 "我手动管" 是两个完全不同的产品需求，混在一起必然有用户用错
3. **复现路径越短越要立即修**。这个 bug 1 次点击就能触发 + 数据后果是"越来越多次备份"，复合起来在生产里是真实事故。优先级应该高于功能开发

**追溯**：用户「Custom 没起任何作用」+「改成 On Demand 吧」两句话直接定位到根因。生产事故没发生只是因为他在测试环境点的 —— Edit 表单的 Custom pill 在 v0.7 上线后保留了 6 个月没被指出，今天才算修完。

### ADR-024 SupKube 作为 Velero UX gap-filler 的设计哲学（v0.8.8）

**状态**：Accepted（v0.8.8）
**背景**：v0.5 到 v0.8.7 的 24 个 ADR 都是单一功能决策（怎么做 Restore drawer、怎么集成 Dex、怎么做 RBAC…）。但用户在 v0.8.7.6 之后提的几个问题暴露了一类**跨功能的产品定位问题**，单独的 ADR 解释不了。所以本 ADR 把 SupKube 这个产品**在 Velero 生态里的定位**讲清楚，给未来的功能决策提供"是不是该做"的判断框架。

#### SupKube 不是 Velero 的 UI 套壳

如果只把 Velero 的 CR 用图形界面展示出来，SupKube 没有存在价值 —— 客户用 `kubectl` + `velero` CLI 就够了。SupKube 的产品价值来自**填补 Velero 在 UX、生命周期管理、跨集群协作上的具体空白**。这些空白的特征：

- Velero 工程团队**有意识**地选择不在自己的层做（Velero 是引擎，不是用户产品）
- 客户**没意识到**这些空白，直到上线碰到才发现
- 修法是**SupKube 自己实现补偿层**，不是改 Velero 上游（Velero 升级我们也不被破坏）

下面列出截至 v0.8.8 已经识别 + 补偿的 5 类空白，未来每加一个功能前可对照这张表判断"是不是 gap-filler"：

#### Gap 1: orphan resource 清理（ADR-024 主体）

Velero 设计上**有意**让 Data Mover 的中间 VSC 用 `deletionPolicy: Retain` 防止上传期被误删；**但 Velero 没设计补偿清理**。客户视角："我删了 RP，为什么底层还有？"。

**SupKube 补**：v0.8.8 的 orphan-gc 包 + Settings → Cluster Hygiene panel。三种触发：定时（默认 6h）、删除后（debounced 60s）、手动按钮。每次都写 K8s Event 进 Activity 页保持透明。

**判断式**：当客户做了一个**逻辑动作**（删 RP），但底层多步**物理动作**只完成了一部分，SupKube 就该补。

#### Gap 2: "备份链" 概念翻译（USER_MANUAL §18）

Velero/Kopia 没有传统意义上的备份链 —— 每个 RP 都是独立可恢复的快照，物理存储是 forever-incremental + content-addressable dedup。但 60% 的客户来自 Veeam / NetBackup 世界，会问"你们走全备还是增量"。

**SupKube 补**：UI 上**不暴露**"full/incr"概念（让产品保持现代），但在 USER_MANUAL §18 里**详细解释**对应关系给合规审计用。

**判断式**：当**用户的概念地图**和**底层技术的概念地图**不对齐时，SupKube 要在文档/UI 层做翻译，**不**为了讨好用户去伪造一个不该有的概念。

#### Gap 3: 双 RP 模型 (Snapshot + Export)

Velero v1.18 的 Data Mover backup 拍完 CSI snapshot 就**立刻删了**它（snapshot 只是 Kopia 上传的中间产物）。一次 backup = 一个 RP。但 Kasten K10 这种成熟产品的 UX 是"一次 policy 执行 = 一个集群本地 Snapshot RP + 一个对象存储 Export RP"。两个 RP 用途不同（快恢复 vs 跨集群）、retention 不同（24h vs 30d）。

**SupKube 补**：v0.8.9（即将做）让一个 L2 Policy 在后端建**两个** Velero Schedule（snapshot + export），UI 上聚合显示为一个 Policy。一次 fire 产生两个 Backup CR。详见 ADR-025。

**判断式**：当 Velero 的最小单元（一个 Backup CR）**承载不了**用户的产品级概念（"一次保护动作"），SupKube 就要在自己的层定义更高级的聚合单元。

#### Gap 4: 沉默的默认值是 UX 决策

v0.8.7.6 之前 Policy 表单的 "Custom" pill 默认 cron 是 `0 * * * *`（每小时）—— 一个看似无害的"先填一个免得 cron 为空"代码默认值。结果用户点 Custom 后什么都不输，**沉默地把策略改成每小时跑**，没有 UI 报错、没有事故告警。

**SupKube 教训**（已写入 ADR-022 v0.8.7.6 修订段）：**任何"先随便填一个免得 null"都是 UX 决策不是工程兜底**。三种正确处理：

1. **强制用户选**（pick a value, no default）
2. **显式 null + UI 说明**（"this policy is paused"）
3. **保留旧值**（hydrate from existing state，不要预填 random）

**判断式**：每次写 `if val == "" { val = "<some safe default>" }` 都要问："如果用户没注意这个默认，他会得到他想要的吗？"答案是"不"就别填。

#### Gap 5: 沉默错误吞噬

后端代码里 `if err == nil { ... }` 模式 + 不 propagate 错误到响应。具体案例：

- v0.8.6 的 ListBackups 的 collectVolumeMetadataBatch 里 `cl.List(... DataUpload ...)` 失败（RBAC 没配齐），但代码是 `if err == nil`，错误被吞，UI 显示 `volumeBytes: 0`。**用户以为备份是空的**，实际有 32 MB 在 Azure Blob 里。

**SupKube 补**（v0.9 计划）：把所有 `if err == nil` 改成显式 error propagation，错误塞到响应的 sidecar 字段（类比 v0.8.6 的 `tarballError`、计划中的 `volumeMetaError`）。前端 tooltip 显示具体原因。

**判断式**：**沉默的 0/空值比报错更危险** —— 错误用户能修，沉默的 0 用户根本不知道错了。所有"best effort"路径都要把错原因带出来。

#### 框架：什么算 SupKube 该做的功能

对照新功能想法，问 5 个问题：

1. Velero 本身做不做？做了 = 别重复。**没做 + 客户期望 = gap，候选**
2. 客户用 kubectl 能做吗？能 = 文档化即可。**不能或太麻烦 = 候选**
3. 我们做了能保护用户免遭沉默错误吗？能 = ✅
4. 我们做了和 Velero 上游对齐 6 个月后还兼容吗？是 = ✅
5. UI 表达后用户的"概念地图"会更清晰还是更乱？更清晰 = ✅

**5/5 通过 = 该做**。3/5 = 进 backlog 评估。≤ 2 = 拒绝（让客户用 kubectl）。

#### 已根据这个框架做出的决策（追溯）

| 功能 | gap 类型 | 已做 / 拒绝 |
|---|---|---|
| Pre-flight 冲突检测（v0.7.10）| Gap 3（聚合用户级概念）| 已做 |
| Audit log（v0.8.5 step 4）| Gap 1（lifecycle 补）| 已做 |
| 数据路径 chip（v0.8.6）| Gap 4（沉默默认值）| 已做 |
| Data Mover UI（v0.8.7）| Gap 4 + Gap 3 | 已做 |
| Custom → On Demand（v0.8.7.6）| Gap 4 | 已做 |
| Orphan GC（v0.8.8）| Gap 1 | 本 ADR |
| 双 RP 模型（v0.8.9）| Gap 3 | 即将做 |
| Kopia 压缩比可视化（v0.9）| Gap 2（翻译） | 已规划 |
| 直接 SQL 接 Velero DB | — | **拒绝**（违反 #4，Velero 没 DB）|
| 自动从 BSL 反向推导 schedule | — | **拒绝**（违反 #5，让用户更乱）|

**未来加新功能时，把这张表填一行，再做 / 不做**。

#### 元教训：用户的"为什么"是产品 bug 的最早信号

v0.8.7.x 的连续 5 次 patch（.2, .3, .4, .5, .6）+ v0.8.8 的 orphan GC 都不是计划好的 sprint —— 都是用户用产品时问"为什么这里 X 又这样了？"或"这个我应该能 Y 啊？"触发的。**这种用户口语化的疑问比 GitHub issue 模板的"复现步骤"还快指向根因**。

写在 ADR 里是为了让 v1.0 之后接手的工程师明白：**SupKube 的产品质量很大程度上靠这种"高带宽的用户对话"。不要切到"用户提 issue → 工程师 review → 排期 → 修"的低带宽流程**，那条路会把这一切窒息。

### ADR-025 双 Schedule 模型：一次 L2 Policy → 两个 RP（v0.8.9）

**状态**：Accepted（v0.8.9）
**背景**：Kasten K10 的核心 UX 是 "一次 L2 Policy 触发产生两个独立 RP" —— Snapshot RP（集群本地，短 retention，秒级回滚）和 Exported RP（BSL，长 retention，跨集群 DR）。Velero v1.18 的 Data Mover 是单一动作（拍快照 → 上传 → 删快照），完成后集群里**没有**可用的本地 snapshot，所以 SupKube v0.8.8 之前一次 L2 备份只产生一个 RP，等于把"双 RP 模型"的产品价值丢掉了。

**决策**：在 SupKube 自己这一层补"双 Schedule 模型"：

```
用户视角:                       后端实际创建:
  L2 Policy "test-app-backup"    Schedule "test-app-backup"
                                   ├─ label: policy-name=test-app-backup
                                   ├─ label: policy-role=snapshot
                                   ├─ snapshotMoveData: false
                                   └─ ttl: 24h
                                 Schedule "test-app-backup-export"
                                   ├─ label: policy-name=test-app-backup
                                   ├─ label: policy-role=export
                                   ├─ snapshotMoveData: true
                                   └─ ttl: 720h
```

#### 数据模型

每个 SupKube Policy 用一个**逻辑名字**（用户取的）。底层映射到 1-2 个 Velero Schedule CR：

| 模式 | K8s Schedule 数 | 用途 |
|---|---|---|
| **legacy** | 1，无 label | v0.8.9 之前创建的 Policy；行为不变 |
| **single** (L1) | 1，带 `policy-role=snapshot` | 新创建的 L1 Policy，只想要本地快照 |
| **dual** (L2) | 2，配对的 snapshot + export | 新 L2 Policy，每次触发产 2 个 Backup |

#### Schedule 命名约定

- Snapshot 半：`<policy-name>`（裸名字 — 保持 legacy URL 兼容）
- Export 半：`<policy-name>-export`（合成后缀）

这样 `/api/v1/schedules/test-app-backup` 路由对 legacy / single / dual 三种模式都自然命中（dual 时由 `findPolicy` 通过 label 反查出 export 半）。

#### 状态机：CreateSchedule / Patch / Delete / RunOnce

所有 4 个 CRUD 操作都通过 `findPolicy()` 把名字解析成 `PolicyAggregate`（包含 1 或 2 个 Schedule），然后对结果集执行：

- **Create**：根据 `dual` 标志创建 1 或 2 个 Schedule。Partial failure（snapshot 半成功但 export 半失败）返回 200 + 部分成功描述，**不回滚** — 一个 partial L2 至少是一个能用的 L1，比"两个都没创建"对用户更有用。
- **Patch**：把请求里的字段同时 apply 到两个半（除了 `snapshotMoveData` 这种角色专属字段自动按角色设定）。
- **Delete**：先删 export 半，再删 snapshot 半 — 失败中断时剩下的至少是个能用的 L1。
- **RunOnce**：两个 Backup 用**同一个时间戳后缀**，让 UI 能按时间窗 + policy-name 把它们配对显示。

#### Backup 端的传播：Velero 自动复制 label

Velero v1.15+ 的 Schedule controller 在 fire 时把 Schedule 的 labels 复制到生成的 Backup CR。所以 `policy-name` + `policy-role` 自动出现在每个 Backup 上，不需要 SupKube 额外加 patch。RunOnce 路径手动 copy 这些 label（manual run 不走 Velero controller）。

#### 前端整合

- **Policies 列表**：每行 = 一个 PolicyAggregate。L2 行有"L2"chip，点开 Edit 显示双 retention 字段（snapshot retention + export retention）。
- **Restore Points 列表**：v0.8.9 的 TYPE chip 直接读 `supkube.dataPath` 区分 📸 Snapshot vs 🚚 Exported —— 已经在 v0.8.6 dataPath 分类里有了，不需新数据。
- **Edit drawer**：`hydrateFormFromSchedule` 接受 PolicyAggregate 或 raw Schedule，向后兼容旧响应。

#### 向后兼容矩阵

| 旧数据 | 新 API 行为 |
|---|---|
| Legacy 单 Schedule（无 label）| `findPolicy` 通过精确名字回退命中，PolicyAggregate.mode = "single" |
| v0.8.8 后创建的 L1 Policy | 带 `policy-name + role=snapshot` label；同上 |
| v0.8.9 后创建的 L2 Policy | 两个 Schedule，aggregate.mode = "dual" |

旧 Schedule 不会被自动升级 — 用户**手动** Edit + Save 时如果还是 L2 模式会**重建**为新模型（删旧的，建配对）。出于谨慎我们 v0.8.9 暂不做自动升级，让用户主动操作。

#### 部分失败的设计哲学

CreateSchedule 的 partial-success（snapshot 半成功 + export 半失败 = 返回 200 + warning）违反了 "atomic operation" 直觉，但符合 §11.3（沉默错误反模式）的对偶：**对用户来说，半 L2 比零 L2 有用，而 partial 状态可见 + 可恢复**。用户从 UI 看到"创建成功但 export 半失败：<具体原因>"，可以选择删了重建 / 修复底层问题再加 export 半 / 接受这是个 L1。**不允许"全有或全无"的暴政式语义**。

#### 取舍

- ✅ Kasten 一致的产品形态：一个 L2 Policy fire = 两个 RP
- ✅ Snapshot 与 Export 各有独立 retention（DB 短期回滚 + DR 长期归档）
- ✅ Run Once 一次产 2 个 Backup，无需手动触发两次
- ❌ Velero 这一层多了一个 Schedule CR，集群里 schedule 数 ×2（n=10 policy → 20 Schedule）
- ❌ Velero schedule-controller 偶尔出现"两半 cron 错开 1-2 秒"的边界情况；UI 按时间窗 grouping 容忍这个误差

**用户文档**：USER_MANUAL §20 — Policy 双 RP 模型 + Restore Points TYPE chip 含义 + 与 legacy 模式的迁移指引。

---

### ADR-026 Plan-B 配对触发控制器 + Plan-C 升级预留（v0.8.10）

**背景**：ADR-025 落地后客户线发现：

- Snapshot 半的 Backup 在 cron 触发 + 数秒 Velero 调度后开始运行
- Export 半的 Backup 走自己的 cron 行 + 自己的 backup queue —— **它会再拍一次 CSI 快照**
- 两次快照之间的**真实时间差**有时高达 7 分钟（实测，看 22:04 vs 21:57 那两条配对 RP）
- 客户从 Snapshot 半 vs Export 半恢复出来的数据**不一致** —— 违反"同一次策略运行 = 同一时刻数据"的产品承诺

**Velero v1.18 上游硬约束**：

```
Backup with snapshotMoveData=true:
  1. CSI driver 拍 VolumeSnapshot + VolumeSnapshotContent
  2. node-agent 通过临时 PVC clone 读快照 → Kopia 上传到 BSL
  3. DataUpload CR 进入 Completed
  4. >>> Velero 主动删除 VolumeSnapshot + VolumeSnapshotContent <<<
  5. 本地快照已不存在
```

第 4 步是写死的。v1.18 没有 `preserveSnapshotsAfterUpload` 字段（社区有提案：vmware-tanzu/velero#7338），所以"一次快照同时产 Snapshot RP + Exported RP"今天技术上做不到。

**Plan B**（v0.8.10 落地）：把两半的触发**串行化**

- snapshot 半 Schedule 维持 Velero 调度（按 cron 触发）
- export 半 Schedule **永久 `paused=true`**，移交触发权给 SupKube
- 新组件 `internal/policypair/` —— polling 10s 扫一次 `Backup with policy-role=snapshot`
  - 见到 `Completed` 且未配对 → 用 export 半 Schedule 的 spec.template 立刻 Create 一个新 Backup
  - 给两半都打 annotation：
    - `supkube.io/dual-rp-paired=<peer-backup-name>` —— UI 配对引用
    - `supkube.io/policy-run-instant=<snapshot-half-creation-ts>` —— 共享逻辑时间戳
- 失败半（PartiallyFailed/Failed）→ **不**触发 export 半 + emit K8s Event 解释（避免脏数据进 BSL）
- L1 snapshot-only policy（无 export 半 Schedule）→ controller 标 `no-peer` 后跳过

**Plan B 数据延迟**：从 ~7 min 压到 ~30s（snapshot complete → controller 10s 内扫到 → Create Backup CR → Velero queue 5s 内开始 → fresh CSI snapshot at T+~15s）。仍然不是 0 差距，但是**诚实地告诉用户这个差距**，而不是 ADR-025 那样靠 cron 巧合"看起来同步"。

**Plan C 升级预留**（v0.8.10 上线时就埋好的三层）

| 层 | v0.8.10 代码 | Plan C 升级时怎么用 |
|---|---|---|
| **Data contract** | controller 在每条 export 半 Backup 上打 `velero.io/csi-volumesnapshot-content-retain-policy=retain` | Velero 上游加 `preserveSnapshotsAfterUpload` 后，data mover 删 VSC 但底层快照保留 → SupKube 重建静态 VSC 形成 "Snapshot RP"；这条 annotation 让此路径自然 work |
| **UI 接口** | Action 增字段 `pairedAction` + `policyRunInstant` + `role`；前端 Activity Drawer 已渲染 paired-with banner | Plan C 阶段两半共享同一个 Backup CR，UI 仍用 `pairedAction` 做引用（指向 virtual Snapshot RP），文案改 "Same data, two storage destinations" |
| **后端模型** | `policypair` 包独立、可禁用（`policyPair.enabled=false` in settings CM） | Plan C 上线时改 controller 模式为 "single-Backup + clone VSC"，老 `policypair` 包降级为遗留 fallback 路径 |

**为什么不直接做 Plan C**：

- Plan C 要求 SupKube 接管 PVC 恢复（绕开 Velero 的 volume restore），工程量 ~2-3 周
- 数据正确性的风险比 Plan B 高 —— 我们自己写 storage-class 兼容、cross-ns 重写、existingResourcePolicy
- Plan B 把客户痛点（7 分钟 vs 30 秒）解决到 "够用"，给 v0.8.11/0.9 留时间稳妥做 Plan C

**遗留兼容**：

- v0.8.9 之前创建的 Schedule（labels=null）不被 controller 识别 → 维持各自 cron 触发，与 v0.8.9 同样表现。要享受 Plan B 必须**新建** v0.8.10 dual policy 或将来加 "Migrate to v0.8.10" 按钮
- 启动迁移：所有带 `policy-role=export` label 的 Schedule 强制 `paused=true`（idempotent），防止 v0.8.9 与 controller 双触发

**测试用例**：TC-POL-007、TC-POL-008（测试用例.md）。冒烟集合中必跑。

---

### ADR-028 Velero v1.18 删 VSC 行为 + 本地快照战略选项（v0.8.11 调研，未决策）

> **状态**：仅记录调查结论 + 三个候选方向。**产品方向待 Mars 决策**。任何代码变更都另起 ADR。

**调研触发**：客户报告 Snapshot RP 列 Size = "—"。展开后发现整个 Snapshot RP 概念可能在 Velero v1.18 下失效。

**调查证据（三层独立验证）**：

1. **Velero v1.18 强制 VSC `deletionPolicy=Delete`**
   - 创建 Backup 时带 `velero.io/csi-volumesnapshot-content-retain-policy=retain` annotation
   - 观察生成的 VolumeSnapshotContent：`spec.deletionPolicy=Delete`
   - 结论：**annotation 被忽略**

2. **Velero v1.18 在 backup Completed 时主动删 VSC**（无论 `snapshotMoveData` 值）
   - 实测：`t+10s` 时 VSC count=2，`t+12s` backup 进入 Completed → VSC count=0
   - 即便 `snapshotMoveData=false`（原本应"保留本地快照"），VSC 仍被删

3. **存储后端快照随 VSC 删除而消失**（生产驱动）
   - csi-hostpath 测试驱动**例外**：`.snap` 文件在 `/csi-data-dir/` 累积成孤儿（测试驱动不实现真删 RPC）
   - 生产驱动（AWS EBS / Azure Disk / GCP PD）：DeleteSnapshot RPC 真删

**对各路径的影响（精确）**：

| SupKube 路径 | `snapshotMoveData` | 卷数据走向 | 是否受 v1.18 bug 影响 | 客户能恢复卷数据吗 |
|---|---|---|---|---|
| L1 Snapshot-only Policy | false | 期望本地保留 | ❌ 受影响 | **不能** |
| L2 Snapshot+Export — snapshot 半 | false | 同 L1 | ❌ 受影响 | **不能** |
| L2 Snapshot+Export — export 半 | true | Kopia → BSL | ✅ **不受影响** | **能** |
| Application 一键 Snapshot 按钮 | false | 同 L1 | ❌ 受影响 | **不能** |
| POST /backups 用户选 moveData=true | true | Kopia → BSL | ✅ 不受影响 | **能** |
| Imported RP（跨集群同步） | 源集群形态 | 已在 BSL | ✅ 不受影响 | **能** |

**Velero 上游态度**：

- 这不是 bug，是 v1.18 **故意**的设计转向 —— "BSL 是唯一持久层；本地快照只是 transient working buffer"
- 上游 issue [#7338](https://github.com/vmware-tanzu/velero/issues/7338) 提议 `preserveSnapshotsAfterUpload`，**尚未合并到任何已发布版本**

**本地快照价值矩阵**（哪些场景本地快照是不可替代的）：

| 场景 | 本地快照价值 | 备注 |
|---|---|---|
| 5 分钟内回滚错误部署 | ✅ 显著快于 BSL 下载 | 主要 use case |
| DR 演练 / 测试 restore | ✅ 不消耗出口流量 | 频繁动作 |
| 同集群蓝绿 / 跨 ns 克隆 | ✅ 完全 cluster-local | 较少 |
| 跨集群恢复 / DR | ❌ 本地快照在源集群 | 必须 BSL |
| 集群整体损坏 / 节点全挂 | ❌ 本地快照随存储后端没了 | 必须 BSL |
| 勒索软件加密 PV | ❌ 攻击者有 K8s 权限就能删 | 必须 immutable BSL |
| 长期归档（合规留存） | ❌ 本地存储成本高 | 必须 BSL |

**三个战略候选**：

| 选项 | 内容 | 工程量 | 产品定位含义 |
|---|---|---|---|
| **A** | 接受 Velero 世界观：禁 L1 Snapshot-only，Application 一键改 Data Mover，UI 不再有"本地快照"概念 | ~1 天 | "轻量级 Velero UI"路线 |
| **B** | SupKube 自造本地快照层：绕过 Velero 直接管 VS/VSC，custom restore controller | ~2-3 周 | 接近 Kasten 路线，但失去"轻量"卖点 |
| **C** | 文案诚实 + 等上游：UI 加警告"待 Velero 修复"，跟踪 #7338 | 0.5 天 | 折中，但客户体验长期处于"灰色"状态 |

**待 Mars 决策**：在 SupKube 的产品定位（轻量 Velero UI vs Kasten 替代）上选 A/B/C。

**测试用例**：TC-REG-013 待写 —— 自动化断言 "snapshot-only backup 后 VSC 在 30s 内消失"。这条用例**在上游修复后会自动失败**，是个绝佳的"上游修了"探测器。

> **2026-05-24 更新**：Mars 拍板选 **D — 用本地 BSL 取代 Snapshot 概念**（这是 ADR-028 三选项之外的新方案）。详细论证见 **ADR-029**。

---

### ADR-029 本地 BSL 取代 Snapshot —— 双 BSL 模型（v0.8.12）

> **状态**：已决策（Mars 2026-05-24）。v0.8.12 LBS1-4 落地。
> **取代关系**：覆盖 ADR-025（双 Schedule 模型）/ ADR-026（Plan-B 控制器）/ ADR-028（候选 A/B/C 全部弃用，本 ADR 是 ADR-028 的 "D" 选项）。

**核心决策**：把"Snapshot Half"（依赖 VSC）替换为"Local Backup"（落到集群内 MinIO BSL）。Velero 删 VSC 与产品价值脱钩，从架构层面规避 v1.18 行为。

**两个数据流并存的新模型**：

```
                ┌─────────────────────────┐
                │  User PVC (source)      │
                └────────────┬────────────┘
                             │ CSI Snapshot (transient, Velero 自己管删除)
                             ▼
                ┌─────────────────────────┐
                │  Kopia upload via       │
                │  node-agent             │
                └────────┬─────────┬──────┘
                         │         │
            ┌────────────┘         └──────────────┐
            ▼                                       ▼
   ┌────────────────────┐                ┌────────────────────┐
   │ Local BSL          │                │ Cloud BSL          │
   │ (MinIO in-cluster) │                │ (Azure / S3 / OSS) │
   │ Object Lock ON     │                │ Object Lock ON     │
   │ 局域网读，秒级     │                │ 异地，跨集群可读   │
   └────────────────────┘                └────────────────────┘
            │                                       │
            ▼                                       ▼
   Local Backup CR                          Cloud Backup CR
   = Velero Backup w/                       = Velero Backup w/
     spec.storageLocation=local-bsl           spec.storageLocation=cloud-bsl
```

**两份都是真备份**（不依赖 VSC 持久存在），与现有 Velero 行为完全兼容。

**新概念映射（取代旧概念）**：

| 旧概念（v0.8.11.2 及之前） | 新概念（v0.8.12+） | 数据真实性 |
|---|---|---|
| Snapshot Only Policy (L1) | **Local Backup Policy** | 旧 L1 是空头（VSC 删） → 新 L1 是真备份 ✅ |
| Snapshot + Export Policy (L2) | **Local + Cloud Backup Policy** | 旧 L2 的 Snapshot 半是空头 → 新 L2 两半都真 ✅ |
| Snapshot Half | **Local Half** | 数据从 VSC 改到本地 BSL |
| Export Half | **Cloud Half** | 不变（仍是 Cloud BSL） |
| Snapshot RP | **Local RP** | UI chip 蓝 → 绿 |
| Exported RP | **Cloud RP** | UI chip 紫 |
| Imported RP | **Imported RP** | 不变（仍来自跨集群 BSL sync） |

**架构层面的 6 条决策**（MVP）：

| # | 决策项 | 选定 |
|---|---|---|
| 1 | 本地 BSL 后端 | **MinIO**（Velero 官方 demo 后端 + 完整 S3 Object Lock） |
| 2 | 部署模式 | **可选启用**（Settings UI 一键 helm upgrade） |
| 3 | 存储后端 | **PVC**（企业级 HA 要求；hostPath 弃用） |
| 4 | CSI VSC 加速 | **保留**（Velero 内部仍先拍 VSC 再 Kopia 上传，完成后该删就删——不再依赖其持久存在） |
| 5 | 上传策略 | **串行**（先本地 BSL，再云端 BSL；本地写完即返回"备份成功"，云端后台异步） |
| 6 | 多集群恢复 | **走云端 BSL**（本地 BSL 仅源集群可见——这与"local 加速 / cloud 灾备"语义对齐） |

**为什么不直接用 ADR-028 的 A/B/C**？

- **A（接受 Velero 世界观禁 L1）**：放弃"快速本地恢复"卖点，让客户每次走公网拉 → RTO 退化 50–200×。
- **B（自造本地快照层）**：实现成本 2-3 周；与 Velero 平行实现增加维护负担；丧失"轻量"卖点。
- **C（文案诚实等上游）**：客户体验长期处于"灰色"状态，不可接受。
- **D（本 ADR）**：复用 Velero 已有 Kopia + BSL 全套机制，**零新内核**，只是多配一个 BSL；保留所有 Velero 兼容性；客户在局域网读本地 MinIO，RTO 不退化。

**v0.8.10 已有 policypair 控制器**（ADR-026）的变化：

- **代码逻辑保留**（不动 controller 框架）
- **annotation 语义换名**：`supkube.io/policy-role=snapshot|export` → `supkube.io/policy-role=local|cloud`
- **触发链不变**：local 完成后 controller 立即触发 cloud（与旧的 snapshot→export 完全对称）
- **UI 文案全替**：所有 "Snapshot Half" / "Export Half" → "Local Half" / "Cloud Half"

**3-2-1-1-0 在 SupKube 中的实现**：

| 规则 | 落地 |
|---|---|
| **3 份副本** | 源 PVC + 本地 MinIO BSL + 云端 BSL |
| **2 种介质** | 本地块存储 PVC（SSD）+ 云对象存储 |
| **1 份异地** | 云端 BSL（不同 region / 不同 cloud 更佳） |
| **1 份不可变** | Object Lock 默认 Governance 模式（v0.8.12 LBS3）；客户可切 Compliance |
| **0 错误** | Velero 完成后 checksum + 周期性 Kopia repo check（v0.8.15 数据校验） |

**对 v1.18 VSC 行为的最终处理**：

- 不再"修"它：VSC 删了无所谓，本地 BSL 是真备份
- v0.8.12 上线时 `velero.io/csi-volumesnapshot-content-retain-policy=retain` annotation **直接去掉**（不再有意义）
- ADR-028 的 TC-REG-013 失效（不再需要"上游修了探测器"）

**对老备份的迁移策略（兼容）**：

- v0.8.11.2 之前的 Snapshot-only Backup CR：UI chip 显示 "Snapshot (Legacy)"，灰色 + tooltip "Created before v0.8.12, not durable"
- 不强迫迁移，TTL 到期自然淘汰
- v0.9 加 "Migrate to Local Backup" 按钮（一次性把老 Schedule 重建为新模型）

**v0.8.12 之后的 Sprint 链**：

- **v0.8.13** Helm 打包优化（Velero 必装 + Dex/MinIO 可选）
- **v0.8.14** 日志查看器 MVP（替代 #28 那种调研难题）
- **v0.8.15** 备份数据校验
- **v0.8.16** Swagger REST API
- **v0.9.0** 多集群管理 + 跨集群恢复 Wizard（Kasten-style，本地 BSL → 云端 BSL → 异地集群恢复 = **MVP 闭环**）

**MVP 闭环**：v0.8.12 + v0.9.0 共 12 天工程量 → "本地备份 + 异地恢复" 完整路径，可以卖。

---

### ADR-030 还原时安全扫描架构：YARA + ClamAV 双引擎（v0.9.6）

**状态**：Proposed（2026-05-28，客户触发）
**对标参考**：[kasten-flr-ui](https://github.com/jdtate101/kasten-flr-ui)（Kasten 单引擎 ClamAV，社区 PoC）
**商业定位**：Premium 套餐杀手锏 — 金融/政府/医疗合规标书硬指标

#### 问题背景

备份 = 攻击者的次要目标。勒索软件常见手法：
1. 加密生产数据 → 客户从备份还原 → **加密代码已经在备份里** → 还原即"重新感染" → 备份失去意义
2. 沉默潜伏（"slow-burn"）：数月前埋下 webshell / cobalt strike beacon → 备份滚走干净 RP → 还原时只剩带后门的 RP

**对应防御**：还原**之前**扫描备份内容。这件事 Velero 不做（数据面只搬字节）；Kasten K10 通过 [kasten-flr-ui](https://github.com/jdtate101/kasten-flr-ui) 社区项目接 ClamAV；Veeam / Rubrik / Cohesity 已经把 YARA 当标配。SupKube 跳过单引擎方案，直接做**双引擎并联**。

#### 决策

**1. 引擎选型 — 双引擎并联**

| 引擎 | 角色 | 强项 | 短板 |
|---|---|---|---|
| **ClamAV** | 通用病毒库 | 5000+ 万签名，每日厂商更新，零规则编写 | 落后于新型 APT / 0day；签名库膨胀 |
| **yara-x**（VirusTotal 官方 Rust 重写 libyara，2024 GA） | 规则引擎 | 规则即代码，可表达 IOC / 字节模式 / 行为，**纯 Rust 静态编译无 cgo**，CI 友好 | 规则需要人维护 |

**并联而非二选一**：ClamAV 兜底已知威胁，YARA 抓**针对性攻击**（勒索软件 family、客户特有 IOC、APT 组织 TTP）。

**为什么选 yara-x 而不是经典 libyara**：
- libyara 是 C 库 → Go 集成走 cgo → 编译复杂 + 跨平台噩梦 + 镜像膨胀
- yara-x = 纯 Rust，静态编译为 standalone binary → Job 镜像内一个文件就够
- 规则语法 100% 兼容 libyara → Yara-Rules 公共仓库 / 商业规则订阅都能直接用
- Apache-2.0 → 商业友好

**2. 扫描时机 — 三种模式都做**

| 模式 | 触发 | 用途 |
|---|---|---|
| **on-demand 还原前扫描** | Restore Wizard 勾选 "Run security scan first" | 还原时**强制净化**（最高安全级别 ns） |
| **scheduled 定时巡检** | cron + backupSelector | 周期扫存量备份（捕捉 slow-burn） |
| **post-backup 备份后扫描** | BackupPolicy 选 "Auto-scan after backup" | 备份完立即扫，缩短发现窗口 |

**3. CRD 设计**

```yaml
# 一次扫描的执行实例
apiVersion: security.supkube.io/v1
kind: RestoreSecurityScan
metadata:
  name: scan-prod-db-20260528
spec:
  backupName: prod-db-20260527
  engines: [clamav, yara]              # 双引擎并联
  yaraRules:
    - configMapRef: yara-rules-builtin-ransomware    # 内置
    - configMapRef: yara-rules-customer-iocs         # 客户上传
  action: quarantine                   # quarantine / block / warn
  scope:
    paths: ["/", "!/var/log"]          # YARA-style 路径过滤
status:
  phase: Completed                     # Pending / Scanning / Completed / Failed
  totalFiles: 1240153
  malicious: 3
  hits:
    - path: /app/data/uploads/x.bin
      engine: yara
      rule: Ransomware_Conti_Payload
      severity: critical
    - path: /tmp/.x
      engine: clamav
      signature: Win.Trojan.Generic-9999
      severity: high
  quarantined: true
  reportURL: s3://supkube-scan-reports/scan-prod-db-20260528.json
  startedAt: "2026-05-28T10:00:00Z"
  completedAt: "2026-05-28T10:08:42Z"

---
# 定时扫描计划
apiVersion: security.supkube.io/v1
kind: ScheduledScan
metadata:
  name: weekly-full
spec:
  schedule: "0 2 * * 0"                # 每周日 02:00
  backupSelector:
    matchLabels:
      tier: production
    maxAgeDays: 30
  engines: [clamav, yara]
  yaraRules: [...]
  action: quarantine
```

**4. 执行架构**

```
┌──────────────────────────────────────────────────────────┐
│  Restore Wizard / ScheduledScan Controller / API Trigger │
│  (任一来源都创建 RestoreSecurityScan CR)                  │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  scan-controller (supkube-backend 新 controller)          │
│  - Watch RestoreSecurityScan                              │
│  - 创建临时 ns: scan-tmp-$JOB_ID (NetworkPolicy = 全封)   │
│  - 调 velero restore → 还原 backup PVC 到临时 ns          │
│  - 等 PVC Bound 完成                                       │
│  - 拉起 scan-job (Kubernetes Job)                         │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  scan-job pod (镜像: supkube/scanner)                     │
│  ┌──────────────────────────────────────────────┐         │
│  │ initContainer: rule-loader                    │         │
│  │   挂载 ConfigMap → /rules/*.yar               │         │
│  │   挂载 ClamAV 签名库 PVC → /clamdb            │         │
│  └──────────────────────────────────────────────┘         │
│  ┌──────────────────────────────────────────────┐         │
│  │ container: scanner (并行)                      │         │
│  │   clamscan -r --database=/clamdb /scan/ &     │         │
│  │   yara-x -r /rules /scan/ &                   │         │
│  │   wait → 合并结果 → POST /api/scan-result     │         │
│  └──────────────────────────────────────────────┘         │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  scan-controller (post-scan)                              │
│  - 更新 RestoreSecurityScan.status                        │
│  - 命中 → patch Backup CR label `supkube.io/quarantine`   │
│  - 上传 report JSON 到 BSL 子目录                          │
│  - 删除 scan-tmp-$JOB_ID ns（数据/Velero Restore CR）     │
└──────────────────────────────────────────────────────────┘
```

**关键安全约束**：
- 临时 ns 用 **NetworkPolicy 默认拒绝**所有 ingress/egress（防止扫描中恶意软件 C2 出去）
- 扫描镜像**无 ServiceAccount Token**（防止恶意软件回调 K8s API）
- PVC 挂载用 **readOnly: true**（防止恶意软件持久化）
- Job 超时 24h，超时强杀 + 清理

**5. 规则管理**

| 来源 | 套餐 | 分发方式 |
|---|---|---|
| Yara-Rules 公共仓库 (ransomware/, APT/, webshells/) 精选子集（~200 条） | **内置 Advanced** | 镜像 baked 进 scan-job，每次 SupKube 发版同步刷新 |
| 客户自定义规则 (.yar 文件) | **Advanced** | UI 上传 → 自动创建 ConfigMap `supkube.io/yara-rules=true` label |
| 商业威胁情报订阅（Florian Roth Signature-Base 商业版 / VirusTotal Premium / Mandiant） | **Premium** | 定期同步到 ConfigMap；rule-loader 自动加载 |
| AI 行为分析（未来） | **Premium+** | v1.1+ |

**6. 隔离工作流**

扫描命中 → Backup CR 自动打 `supkube.io/quarantine=true` + `supkube.io/quarantine-reason=<hit-summary>` →
- **Restore Wizard** 检测到该 label → **hard-block** 还原按钮 → 显示扫描报告 + 双重确认 override（"我了解风险，仍要还原" + 输入 backup name 确认）
- **Dashboard** 显示隔离 RP 计数为红色告警卡
- 管理员可在 `/security/quarantine` 页面**显式批准**或**永久删除**

#### 替代方案（已否决）

**方案 A：只做 ClamAV（对标 Kasten）**
- ❌ 单引擎覆盖不足；对针对性攻击（APT、勒索软件 family）盲区大
- ❌ 投标硬指标不够（金融/政府越来越要求 YARA）

**方案 B：扫源 BSL 而不是临时还原**
- ❌ 备份是 tarball + restic chunks 二进制格式，**无法**直接喂 ClamAV/YARA
- ❌ 真要扫必须先解压，相当于做了还原 — 等价于本方案，没省事

**方案 C：扫描嵌入 Velero plugin（BackupItemAction）**
- ❌ 备份时扫 = 生产 ns 跑扫描 → 占生产资源 + 数据已经 in-flight 时改不动
- ❌ Velero plugin 进程内嵌 ClamAV/yara-x 让镜像膨胀到 GB 级
- ✅ post-backup mode 仍然有价值 — 但通过创建 RestoreSecurityScan CR 来触发，不要侵入 Velero plugin

**方案 D：调外部 SOC 平台（CrowdStrike / SentinelOne）**
- ⚠️ 作为 Premium **可选**集成（Webhook 推 hit 到 SIEM）— 但不能作为**默认**方案：客户不一定有 SOC

#### 为什么这是 SupKube 的活，不是 Velero 的活

参见 ADR-024 "SupKube 作为 Velero UX gap-filler"。Velero 数据面只搬字节，不做内容审查；扫描属于**协调控制平面 + UI + 编排**层 — 100% SupKube 范畴。

#### 风险与缓解

| 风险 | 缓解 |
|---|---|
| 扫描资源消耗大（CPU / IO）| 扫描 Job 限定 NodeSelector → 专用 node pool；并行度可配 |
| YARA 规则误报 | UI 显示 hit 的规则名 + 字节偏移，管理员可标记 false positive 加入白名单 ConfigMap |
| 还原 PVC 占临时空间 | scan-tmp ns 用专用 StorageClass（可配 ephemeral），扫完立删 |
| 恶意软件在扫描期间逃逸 | 临时 ns NetworkPolicy 全封 + 无 SA Token + PVC readOnly（三道防线）|
| ClamAV 签名库更新 | 用 freshclam sidecar 单独 cron 更新到共享 PVC，scan-job 挂载 |

#### 后续 Sprint 路径

- **v0.9.6** 双引擎 MVP（本 ADR 覆盖范围）
- **v1.1** 接 VirusTotal hash 查询作为第三层（轻量、快、命中率高）
- **v1.2** SOC/SIEM webhook 推送（Sentinel / Splunk / Elastic）
- **v1.3** AI 行为分析（基于扫描历史 + 文件 entropy 异常检测）

#### 与其他 ADR 的关系

- **ADR-024** SupKube 作为 UX gap-filler：本 ADR 是该哲学的**安全方向**应用
- **ADR-026** Pair-trigger controller pattern：scan-controller 复用 watch + reconcile 模式
- **v0.9.7 应用级恢复演练**：扫描确认数据"**干净**"，演练确认数据"**可用**" — 两者合并 = "**Verified Restore**" 完整闭环

---

## 10. 编码规范与约定

### 10.1 Go 包组织

- 按业务域分文件，**不**按层（不要 `controllers/services/repositories/`）
- 包名小写、短、单数（`auth`、`bsl`、不要 `auths`）
- 一个文件 < 600 行，超了按主题拆分
- 导出符号必须有 godoc 注释，重点解释"为什么"

### 10.2 命名规范

| 类型 | 规范 | 例子 |
|---|---|---|
| Go struct | UpperCamelCase | `BackupInfo` |
| Go field | UpperCamelCase + JSON tag | `Workloads int \`json:"workloads"\`` |
| Vue component | UpperCamelCase | `RestoreDrawer.vue` |
| Vue composable | `use<Domain>` | `useAuth`, `useTheme` |
| i18n key | `<page>.<element>` camelCase | `restoreDrawer.applyFix` |
| K8s label | `supkube.io/<key>` | `supkube.io/managed-by` |
| K8s annotation | 同上 | `supkube.io/auto-generated` |

### 10.3 注释风格：why-not-what

**坏**：
```go
// Loop over backups
for _, b := range backups {
```

**好**：
```go
// Match BSL 上的 tarball 名前缀。Velero 命名规则是
// <backup-name>.tar.gz，不带 prefix —— v1.11+ 才支持
// .spec.objectStoragePrefix，我们暂不依赖。
for _, b := range backups {
```

每个非平凡决策（任何"为什么不是另一种写法"）都要写在注释里。**6 个月后看代码的是未来的你**。

### 10.4 错误信息

| 角色 | 风格 |
|---|---|
| 给用户看（HTTP 4xx）| 短、可纠正、命名要素带引号：`namespace "test-app" is protected` |
| 给运维看（HTTP 5xx）| 完整 wrap 原始错误：`fmt.Errorf("create DownloadRequest: %w", err)` |
| 日志（stdout）| 带 prefix：`log.Printf("[transform-sets] migration: deleted CM %q", name)` |

### 10.5 i18n key 命名

- key 必须**双语完整**，缺哪边都不行（CI 检查 v0.9）
- 参数化字符串用 `{name}` 占位：`'Restore "{name}" submitted'`
- 不在代码里硬编码用户可见字符串，唯一例外是开发期 `console.log`

---

## 11. 工程座右铭（Engineering Mottos）

> **本节是 SupKube 团队工程文化的底层公理。**
> ADR 是"为什么这样设计的"；座右铭是"我们做任何决定时不能忘的事"。
> 每条座右铭都钉在**一次真实事故**上 —— 不允许抽象口号，必须能溯源到具体痛。

### 11.1 回归测试是必做项目（Regression testing is mandatory）

**触发事故**：v0.8.7.6 — "删除 RP 但没真删"。用户在 UI 上把 6 个 Restore Points 全部点了删除，回到 kubectl 一查：

- 9 个 VolumeSnapshotContent 全部留在集群里（deletionPolicy=Retain，Velero Data Mover 故意设的）
- Azure Blob `velero-dr/kopia/test-app/` 目录里 30+ MB Kopia 数据**还在持续占用计费**
- K8s API 累积 4 个 orphan VolumeSnapshot + 4 个 snapcontent-* VSC
- 全程 UI **零提示**，用户以为已经清干净

如果这是生产客户，结果就是月底账单里多出几百块"删不掉"的对象存储费用 —— **而且客户根本不知道是什么花的**。

**为什么测试没接住**：

1. v0.8.7 引入 Data Mover 时**功能测试**走通了（创建 → 上传 → BSL 有数据 ✓）
2. v0.8.7 删除测试也走通了（点 Delete → Backup CR 消失 ✓）
3. 但**没有一个测试覆盖"删除完成后，所有底层 K8s + BSL 资源也清干净了吗？"**
4. 上一个版本（v0.8.6）没 Data Mover，所以这种 orphan 不存在 → 没人想到要写这个测试
5. **回归测试矩阵没跟着新功能扩展**

**座右铭**：

> 设计了流程 = 写了测试用例 = 把测试用例排进 backlog
> 三件事缺一不可。
> 任何 PR 没把 "对应 TC 已更新或新增" 写进描述里 → 不准 merge。

**具体落地规则**：

1. 任何新增功能（新页面 / 新 API / 新表单行为）→ **同 PR 内**更新 `测试用例.md`，至少加 1 个 P0/P1 用例覆盖最常见路径
2. 任何修复历史 bug → **必须**在 `TC-REG-XXX` 增加对应回归用例 + 注明 sprint 版本
3. 任何"客户提出来才发现的 bug"→ ADR 修订段 + 回归用例**双归档**
4. 测试用例按"重要紧急矩阵"排序（见 §11.2）
5. 客户做 UAT / SupKube 发版前，QA 必须把 P0 全集 + 上版本新增的 P1 全集跑一遍

#### 现场案例：本次事故的回归用例

`测试用例.md` 已新增 `TC-REG-010 [P0] 删除 RP 后 K8s + BSL 资源全清` — 内容包括：

- 创建一个 Data Mover backup → 删除 → 等 60s → 检查 VSC/VS/PVB/DataUpload 全为 0 + BSL kopia/ 没新增孤儿块
- **每个发版前必跑**，因为 Velero 上游每次升级都可能改 Data Mover 的清理时序

### 11.2 重要紧急双轴排序（Eisenhower Matrix for Tests）

测试用例不是平铺的清单，而是**两轴矩阵**：

```
              紧急                  不紧急
            ┌──────────────────┬──────────────────┐
   重要     │ Q1: 立刻做        │ Q2: 排进 sprint  │
            │ (P0 冒烟)        │ (P1 覆盖)        │
            ├──────────────────┼──────────────────┤
   不重要   │ Q3: 找人做        │ Q4: 删掉它       │
            │ (P2 边缘)        │ (nice-to-have)   │
            └──────────────────┴──────────────────┘
```

- **Q1 / P0** — 阻断性，每次发版前**必跑**，失败则不准发布
- **Q2 / P1** — 关键功能，必须通过；可延期但不能跳过
- **Q3 / P2** — 应该通过，已知风险可商量延期
- **Q4 / P3** — nice-to-have；客户没问、QA 没时间 → 随时砍

**实际执行**：

- 项目时间充裕 → Mars 自己跑 Q1+Q2，类似 Roadmap 排期一样按矩阵推进
- 赶 backlog → 安排专人跑 Q1+Q2，Q3 抽样
- 客户 UAT → 客户跑 Q1+Q2（脚本化的话直接跑），P0 失败就回退

### 11.3 沉默错误是头号产品风险（Silent Errors Are P0）

**触发事故**：v0.8.6 ListBackups 里 `if err == nil { ... }` 静默吞 RBAC 错，UI 显示 volumeBytes=0，用户以为备份是空的，实际 Azure Blob 里有 32 MB。

**座右铭**：

> 任何 `if err == nil { ... }` 模式都是 silent-error 的高发地。
> 失败必须冒泡到用户能看见的层 —— 哪怕一行错误 tooltip 也行。
> 默默返回 0 / 空数组 / "成功" → 比报错危险 10 倍。

**具体落地规则**：

1. backend API 路径上的 List/Get 调用失败 → 错误进响应 sidecar 字段（如 `tarballError`、计划中的 `volumeMetaError`）
2. 前端组件 catch 后 → 至少 `ElMessage.error()` 或 tooltip 暴露
3. 永远不允许 "fail-open" 的代码路径假装一切正常

### 11.4 默认值是 UX 决策不是工程兜底（Defaults Are UX, Not Backstops）

**触发事故**：v0.8.7.6 — Policy 表单 "Custom" pill 默认 cron 是 `0 * * * *`（每小时），用户点 Custom 后什么都不输就保存，**沉默地把策略改成每小时跑**。

**座右铭**：

> 每次写 `if val == "" { val = "<safe default>" }` 都要问：
> "如果用户没注意这个默认值，他会得到他想要的吗？"
> 答案是"不会" → 别填，让 UI 强制选 / 显式 null / 保留旧值。

### 11.5 用户的"为什么"是产品 bug 的最早信号（"Why is this?" is the earliest signal）

**触发事故**：v0.8.7.x 的连续 5 次 patch 全部源自用户口语化提问 —— "为什么 Custom 没起作用？"、"为什么删了还有？"、"为什么只有一个 RP？"。**每一次"为什么"都直接命中产品 bug**。

**座右铭**：

> 用户在产品里皱眉 1 秒 = 工程师漏掉的 1 个 bug。
> 不要切到"用户提 issue → review → 排期 → 修"的低带宽流程，那会窒息这种信号。
> 保持**高带宽对话**直到产品成熟。

---

## 12. 未来演进路径

### 12.1 已规划

| 版本 | 主题 |
|---|---|
| v0.8.5 | 生产门槛包：OIDC + RBAC + 审计 + Policy 编辑 + Token/Basic fallback |
| v0.8.6 | Data Usage：实时 BSL 查询 + Dashboard Data 卡片 + Data Usage 详情页 |
| v0.9.0 | 商业卖点 + Q3 顺手包：L3 Immutable + Backup 详细错误 + Monaco YAML |
| v0.10.0 | 平台化：Kanister Blueprints + Hub-Spoke 多集群 |

详见 USER_MANUAL §10。

### 12.2 长期架构方向

**v0.10 Hub-Spoke 引入的架构变化**：
- 新增 `cluster.supkube.io/v1 Spoke` CRD（注册 Spoke 集群）
- 后端拆出 `internal/multicluster/` 包，封装 cross-cluster K8s client
- UI 加 ClusterSwitch 选择器
- Activity / Backups 列表跨集群聚合

**v0.11+ 候选方向（看客户反馈定）**：
- Prometheus + Grafana 集成（长期指标）
- 备份成功率 SLO + 告警
- 智能容量规划（Storage Profile 自动扩容建议）
- 数据保护合规报表（满足 SOC2 / GDPR 审计）

### 12.3 不会做的方向（保持产品边界）

- **不会做完整的 K8s 工作流引擎**（那是 Argo Workflows 的活）
- **不会做应用部署**（那是 Helm / ArgoCD 的活）
- **不会做监控告警系统**（最多导出 metrics 给 Prometheus）
- **不会自己实现备份引擎**（Velero 永远是数据面）

---

## 附录 A：版本变更记录

记录每次发版引入的**架构层**变更（不是 feature changelog，那个在 USER_MANUAL）。

| 版本 | 架构变化 |
|---|---|
| v0.5 | MVP：单 binary backend + Vue SPA + Helm chart |
| v0.6 | CSI snapshot 双模式（FS + CSI 互斥） |
| v0.7.5 | 引入 Backup Advisor 评分引擎 |
| v0.7.6 | useTheme 暗色模式反应式架构 |
| v0.7.9 | DeleteBackupRequest 级联删除（ADR-012）|
| v0.7.10 | RestoreDrawer 侧边抽屉（ADR-010）+ cleanupBeforeRestore（ADR-007）|
| v0.7.11 | DownloadRequest 详细错误拉取（ADR-013）|
| v0.7.12 | Pre-flight 10 类冲突检测 + Conflict/TransformRule 数据模型 |
| v0.8.0 | Action 统一抽象（ADR-002）+ Restores 页废弃（ADR-008）|
| v0.8.2 | Transform Set CRUD（ADR-003）|
| v0.8.3 | ConfigMap 单 key 修复 + 批量 Apply Fix + 启动迁移机制 |
| v0.8.4 | 三种 patch 类型分场景（ADR-006）|
| v0.8.5 | Dex OIDC 集成（ADR-001、ADR-005）+ ConfigMap-mounted nginx（ADR-016）+ Secret env 替换（ADR-017）+ RBAC 3 角色（ADR-018）+ 审计日志（ADR-019）+ Token/Basic Auth fallback（ADR-020）|
| v0.8.6 | 备份组成元数据：dataPath + 卷字节 + tarball 字节（ADR-021）|
| v0.8.7 | 跨集群 DR：Data Mover UI + Azure BSL provider + Azure tarball-size（ADR-022）|
| v0.8.7.2 | 第三方集成版本管理 + 镜像分发三件套（INTEGRATION.md + images.yaml + hack/）（ADR-023）|
| v0.8.7.3 | Backup metadata 聚合补 DataUpload + RBAC 补权限（ADR-022 修订）|
| v0.8.7.4 | Policy 表单顺序对齐 Kasten + Storage Profile 改下拉（ADR-022 修订）|
| v0.8.7.5 | Data Path 与 Action 强耦合 + L1/L2 双列布局（ADR-022 修订）|
| v0.8.7.6 | Custom 频率改 On Demand，真按 Velero paused 暂停语义（ADR-022 修订）|
| v0.8.8 | Orphan GC + Cluster Hygiene Settings + 设计哲学 ADR-024 |
| v0.8.8.1 | Backup 错误消息可见性（§11.3 沉默错误活体案例）+ TC-REG-011 |
| v0.8.9 | 双 Schedule 模型：一次 L2 Policy → Snapshot + Exported 两个 RP（ADR-025）|

---

## 附录 B：术语速查

| 缩写 | 全称 | 出现位置 |
|---|---|---|
| ADR | Architecture Decision Record | §9 |
| BSL | BackupStorageLocation (Velero CRD) | §5.1 |
| VSL | VolumeSnapshotLocation (Velero CRD) | §5.1 |
| DBR | DeleteBackupRequest (Velero CRD) | ADR-012 |
| DR | DownloadRequest (Velero CRD) | ADR-013 |
| CSI | Container Storage Interface | §1.3 |
| OIDC | OpenID Connect | §8.1 |
| JWT | JSON Web Token | §8.1 |
| JWKS | JSON Web Key Set | ADR-005 |
| RBAC | Role-Based Access Control | §8.2 |
| GVR | Group/Version/Resource | §3.1 |
| GVK | Group/Version/Kind | §3.1 |
| SPA | Single Page Application | §4 |

---

## 附录 C：参考资料

- USER_MANUAL.md — 客户视角文档（含 §11 IdP 集成指南）
- Velero 官方文档：https://velero.io/docs/main/
- Dex 文档：https://dexidp.io/docs/
- Kasten K10（设计参考）：https://docs.kasten.io/

---

*本文档随每次发版更新。新功能 / 新决策必须更新本文档对应章节后再合并 PR。*
